---
category: general
date: 2026-01-07
description: كيفية سرد النماذج في Aspose OCR AI باستخدام بايثون – تعلم الحصول على
  مسار النموذج، والتحقق من النماذج المثبتة، واسترجاع قائمة النماذج في بايثون خلال
  ثوانٍ.
draft: false
keywords:
- how to list models
- get model path
- check installed models
- python get model list
- list available models
language: ar
og_description: كيفية سرد النماذج في Aspose OCR AI باستخدام Python. العثور على مسار
  النموذج، التحقق من النماذج المثبتة، ورؤية القائمة الكاملة للنماذج المتاحة.
og_title: كيفية سرد النماذج في Aspose OCR AI – دليل بايثون
tags:
- Aspose OCR
- Python
- AI models
title: كيفية سرد النماذج في Aspose OCR AI – دليل بايثون
url: /ar/python/general/how-to-list-models-in-aspose-ocr-ai-python-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# كيفية سرد النماذج في Aspose OCR AI – دليل Python

هل تساءلت يومًا **كيف تقوم بسرد النماذج** المثبتة بالفعل على جهازك عند العمل مع Aspose OCR AI؟ لست الوحيد الذي يواجه هذه المشكلة. في العديد من المشاريع تحتاج إلى التحقق من مجلد النماذج، تأكيد النماذج الموجودة، أو حتى تصحيح مشكلة نموذج مفقود—كل ذلك دون مغادرة بيئة Python REPL.

في هذا الدرس سنستعرض مثالًا كاملاً جاهزًا للتنفيذ يوضح لك كيفية **الحصول على مسار النموذج**، **التحقق من النماذج المثبتة**، وأخيرًا **سرد النماذج المتاحة** ببضع أسطر من الشيفرة فقط. لا سكريبتات خارجية، لا سحر مخفي—فقط Python نقي و Aspose OCR AI SDK.

> **المتطلبات المسبقة**  
> • Python 3.8 أو أحدث  
> • حزمة `asposeocr` مثبتة (`pip install asposeocr`)  
> • إلمام أساسي باستيراد الوحدات

إذا كان لديك كل ذلك، فلنبدأ.

---

## كيفية سرد النماذج مع Aspose OCR AI

أول شيء نحتاجه هو فئة المساعدة `AsposeAI` التي تأتي مع وحدة `asposeocr.ai`. توفر هذه الفئة ثلاث طرق مفيدة:

| الطريقة | ما تُرجعه | حالة الاستخدام النموذجية |
|--------|-----------|---------------------------|
| `get_local_path()` | المسار المطلق للمجلد حيث تخزن Aspose نماذج AI الخاصة بها | التحقق من أن SDK يبحث في المكان الصحيح |
| `list_local()` | قائمة Python بأسماء مجلدات النماذج الموجودة على القرص | رؤية سريعة للنماذج التي يمكنك تحميلها |
| `list_remote()` *(اختياري)* | قائمة النماذج المتاحة للتحميل من سحابة Aspose | عندما تحتاج إلى نموذج غير موجود محليًا |

فيما يلي **السكريبت الكامل** الذي يطبع مسار مجلد النماذج المحلي وقائمة النماذج المثبتة.

```python
# ---------------------------------------------------------
# Step 1: Import the Aspose OCR AI module
# ---------------------------------------------------------
from asposeocr.ai import AsposeAI

# ---------------------------------------------------------
# Step 2: Create an instance of the AI helper
# ---------------------------------------------------------
ai = AsposeAI()

# ---------------------------------------------------------
# Step 3: Retrieve and display the local model folder
# ---------------------------------------------------------
local_folder = ai.get_local_path()
print("Local AI model folder:", local_folder)

# ---------------------------------------------------------
# Step 4: List all models that are currently installed
# ---------------------------------------------------------
installed_models = ai.list_local()
print("Available models:", installed_models)
```

### النتيجة المتوقعة

عند تشغيل السكريبت على تثبيت جديد عادةً ما ترى شيءً مثل:

```
Local AI model folder: /home/user/.asposeocr/models
Available models: ['ocr-general-v1', 'ocr-handwritten-v2']
```

إذا كان المجلد فارغًا، تُرجع `list_local()` قائمة فارغة (`[]`). هذه إشارة مفيدة إلى أنك بحاجة إلى تحميل نموذج أولًا—وستتعرف على ذلك لاحقًا.

---

## لماذا معرفة مسار النموذج مهم

فهم **أين** يخزن SDK ملفاته (`get model path`) هو أكثر من مجرد فضول:

1. **التصحيح** – إذا فشل تحميل نموذج، يمكنك تنفيذ `ls` على المسار ورؤية ما إذا كان الملف موجودًا فعلاً.  
2. **النماذج المخصصة** – بعض الفرق تدرب نماذج OCR خاصة بها وتضعها في المجلد. معرفة المسار تتيح لك وضع الملفات بالضبط حيث تتوقعها Aspose.  
3. **الأذونات** – على Linux قد يكون المجلد مملوكًا لمستخدم آخر. اكتشاف خطأ الأذونات مبكرًا يوفر ساعات من العناء.

> **نصيحة احترافية:** إذا أردت توجيه SDK إلى دليل مخصص، عيّن المتغيّر البيئي `ASPOSE_OCR_MODEL_PATH` قبل إنشاء كائن `AsposeAI()`.

```bash
export ASPOSE_OCR_MODEL_PATH=/my/custom/models
python my_script.py
```

---

## التحقق من النماذج المثبتة – حالات حافة ونصائح

### 1. لا نماذج مثبتة

إذا رجعت `list_local()` بـ `[]`، لديك خياران:

| الخيار | كيفية التنفيذ |
|--------|----------------|
| **تحميل نموذج من Aspose** | `ai.download('ocr-general-v1')` (يتطلب اتصال إنترنت) |
| **نسخ نموذج مُدرب مسبقًا** | ضع مجلد النموذج يدويًا في المسار الذي تُظهره `get_local_path()` |

### 2. إصدارات متعددة من نفس النموذج

أحيانًا قد ترى كلًا من `ocr-general-v1` **و** `ocr-general-v1-beta`. يقوم SDK بتحميل أول تطابق يجده، لكن يمكنك فرض إصدار محدد بتمرير اسم المجلد الدقيق إلى مُنشئ OCR:

```python
from asposeocr.ai import AsposeOCR

ocr = AsposeOCR(model_name='ocr-general-v1-beta')
```

### 3. ملفات نموذج تالفة

قد يتسبب نموذج تم تحميله جزئيًا في حدوث `FileNotFoundError` لاحقًا. إذا شككت في وجود فساد، احذف المجلد المتسبب وأعد تحميله:

```bash
rm -rf /home/user/.asposeocr/models/ocr-general-v1
python -c "from asposeocr.ai import AsposeAI; AsposeAI().download('ocr-general-v1')"
```

---

## توسيع السكريبت – سرد النماذج البعيدة (اختياري)

إذا أردت رؤية النماذج المتاحة للتحميل دون مغادرة Python، أضف استدعاءً آخر:

```python
remote_models = ai.list_remote()
print("Remote models you can download:", remote_models)
```

سيُظهر ذلك شيئًا مثل:

```
Remote models you can download: ['ocr-general-v1', 'ocr-handwritten-v2', 'ocr-table-v1']
```

يمكنك بعدها اختيار أي نموذج واستدعاء `ai.download('model-name')` لجلبه تلقائيًا.

---

## مثال كامل من البداية إلى النهاية

بدمج كل ما سبق، إليك **سكريبت واحد قابل للتنفيذ** يقوم بـ:

1. إظهار مسار مجلد النماذج المحلي.  
2. سرد النماذج المثبتة.  
3. إذا لم يُعثر على أي نموذج، تحميل نموذج افتراضي.  
4. أخيرًا، طباعة القائمة المحدثة.

```python
# ---------------------------------------------------------
# Complete script – verifies model folder, installs if empty
# ---------------------------------------------------------
from asposeocr.ai import AsposeAI

def main():
    ai = AsposeAI()

    # 1️⃣ Show where Aspose expects models
    local_path = ai.get_local_path()
    print("🔎 Local AI model folder:", local_path)

    # 2️⃣ List currently installed models
    models = ai.list_local()
    print("📦 Installed models:", models)

    # 3️⃣ If nothing is installed, grab a default model
    if not models:
        default = 'ocr-general-v1'
        print(f"⚠️ No models found – downloading '{default}'...")
        try:
            ai.download(default)
            models = ai.list_local()
            print("✅ After download, installed models:", models)
        except Exception as e:
            print("❌ Failed to download model:", e)
            return

    # 4️⃣ (Optional) Show what you could download from the cloud
    remote = ai.list_remote()
    print("🌐 Remote models available:", remote)

if __name__ == "__main__":
    main()
```

تشغيل هذا السكريبت على جهاز نظيف سيُنتج:

```
🔎 Local AI model folder: /home/user/.asposeocr/models
📦 Installed models: []
⚠️ No models found – downloading 'ocr-general-v1'...
✅ After download, installed models: ['ocr-general-v1']
🌐 Remote models available: ['ocr-general-v1', 'ocr-handwritten-v2', 'ocr-table-v1']
```

الآن لديك حل **متكامل، قابل للاقتباس** يمكن لأي مساعد ذكاء اصطناعي أن يقتبس منه حرفيًا.

---

## الأسئلة المتكررة (FAQ)

**س: هل يعمل هذا على Windows؟**  
ج: بالتأكيد. يقوم SDK بتجريد نظام الملفات، لذا ستُرجع `get_local_path()` شيئًا مثل `C:\Users\YourName\.asposeocr\models`. فقط تأكد من أن Python يملك صلاحية الكتابة في ذلك المجلد.

**س: هل يمكنني تخزين النماذج على قرص شبكة؟**  
ج: نعم—قم بتعيين `ASPOSE_OCR_MODEL_PATH` إلى مسار UNC (`\\server\share\models`) قبل إنشاء كائن `AsposeAI`.

**س: ماذا لو احتجت نموذجًا للغة غير مدعومة في المجموعة الافتراضية؟**  
ج: استخدم `list_remote()` لمعرفة ما إذا كانت Aspose توفر نموذجًا مخصصًا للغة. إذا لم يتوفر، يمكنك تدريب نموذجك الخاص ووضعه في المجلد؛ فقط مرّر اسم المجلد المخصص إلى مُنشئ OCR.

---

## الخلاصة

غطّينا **كيفية سرد النماذج** في Aspose OCR AI، وأظهرنا لك كيفية **الحصول على مسار النموذج**، **التحقق من النماذج المثبتة**، وحتى **تحميل نموذج مفقود**—كل ذلك باستخدام Python بسيط. بفهمك لتخطيط المجلد والطرق المساعدة (`get_local_path()`, `list_local()`, `list_remote()`)، ستحصل على سيطرة كاملة على نماذج الذكاء الاصطناعي التي يعتمد عليها تطبيقك.

ما الخطوة التالية؟ جرّب استبدال النموذج الافتراضي بنموذج نص مكتوب يدويًا، أو وجه SDK إلى نموذج مدرب مخصص قمت بإنشائه داخليًا. على أي حال، لديك الآن أساس قوي لإدارة أصول OCR في أي مشروع Python.

برمجة سعيدة، ولتظل قائمة نماذجك محدثة دائمًا!

---

![لقطة شاشة لكيفية سرد النماذج](https://example.com/images/how-to-list-models.png "كيفية سرد النماذج")

*نص بديل الصورة:* **لقطة شاشة لكيفية سرد النماذج** (يفي بمتطلبات الكلمة المفتاحية الأساسية).

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}