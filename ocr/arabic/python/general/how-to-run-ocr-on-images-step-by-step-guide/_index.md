---
category: general
date: 2026-01-02
description: كيفية تشغيل OCR واستخراج النص من الصورة بسرعة. تعلم كيفية تحميل الصورة
  لـ OCR، تحسين دقة OCR والحصول على نتائج موثوقة.
draft: false
keywords:
- how to run OCR
- extract text from image
- how to load image
- improve OCR accuracy
- load image for OCR
language: ar
og_description: كيفية تشغيل OCR على أي صورة. يوضح لك هذا الدليل كيفية تحميل الصورة
  للـ OCR، استخراج النص من الصورة وتحسين دقة الـ OCR باستخدام المعالجة اللاحقة بالذكاء
  الاصطناعي.
og_title: كيفية تشغيل OCR – دليل شامل لاستخراج النص بدقة
tags:
- OCR
- Python
- image processing
title: كيفية تشغيل OCR على الصور – دليل خطوة بخطوة
url: /ar/python/general/how-to-run-ocr-on-images-step-by-step-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# كيفية تشغيل OCR – دليل كامل لاستخراج النص بدقة

هل تساءلت يومًا **كيفية تشغيل OCR** على لقطة شاشة مليئة بالأخطاء الإملائية؟ لست وحدك. في العديد من المشاريع، يحتاج المطورون إلى استخراج نص نظيف وقابل للبحث من المستندات الممسوحة ضوئيًا، الإيصالات، أو حتى الميمات، وغالبًا ما يكون الناتج الخام فوضويًا. الخبر السار؟ ببضع أسطر من Python يمكنك تحميل صورة، تشغيل محرك OCR، ثم تحسين النتائج باستخدام معالج ما بعد المعالجة المدعوم بالذكاء الاصطناعي.

في هذا الدرس سنستعرض كل ما تحتاج معرفته: من **كيفية تحميل الصورة** إلى المحرك، إلى استخراج النص من الصورة، وأخيرًا تحسين دقة OCR باستخدام معالج ما بعد المعالجة الذكي. لا خدمات خارجية، مجرد مثال متكامل يمكنك تشغيله اليوم.

---

## ما ستحتاجه

- **Python 3.9+** (أي نسخة حديثة تعمل)
- نسخة من محرك OCR (للتجربة نفترض كائن `engine` عام يتبع نمط `load_image → recognize → run_postprocessor`)
- صورة نموذجية، مثل `sample_with_typos.png`، موجودة في مجلد يمكنك الإشارة إليه
- اختياريًا: بيئة افتراضية للحفاظ على نظافة التبعيات

> **نصيحة احترافية:** إذا كنت تستخدم Tesseract، قم بتثبيته عبر مدير حزم نظام التشغيل ثم غلفه بمكتبة Python مثل `pytesseract`. الكود أدناه يجرد المحرك، لذا يمكنك تبديل التنفيذ دون تعديل المنطق المحيط.

---

## الخطوة 1 – كيفية تحميل الصورة لـ OCR

أول شيء يجب القيام به هو توجيه محرك OCR إلى الملف الذي تريد قراءته. هنا يصبح مصطلح **كيفية تحميل الصورة** حرفيًا: تعطي المحرك مسارًا، وهو يجهز البت ماب للتعرف.

```python
# Step 1: Load the image into the OCR engine
ocr_engine = engine               # assume the OCR engine instance is already created
ocr_engine.load_image("YOUR_DIRECTORY/sample_with_typos.png")
```

**لماذا هذا مهم:**  
تحميل الصورة بشكل صحيح يضمن أن المحرك يرى بيانات البكسل الدقيقة التي تنوي معالجتها. تخطي التحضير المسبق (مثل تغيير الحجم أو التحويل إلى تدرج الرمادي) قد يتسبب في تفسير المحرك للأحرف بشكل خاطئ، خاصةً في المسحات ذات التباين المنخفض.

---

## الخطوة 2 – تشغيل OCR لاستخراج النص من الصورة

الآن بعد أن أصبحت الصورة جاهزة، نستدعي روتين OCR الأساسي. تُعيد الطريقة كائنًا يحتوي على الخاصية `.text` التي تحمل السلسلة الخام.

```python
# Step 2: Run the basic OCR to obtain the raw text output
raw_result = ocr_engine.recognize()   # returns an object with a .text attribute
```

**ما ستحصل عليه:**  
`raw_result.text` سيحتوي على كل كلمة يستطيع المحرك اكتشافها، بما في ذلك أي أخطاء إملائية أو شوائب ناتجة عن الضوضاء. فكر فيه كـ **استخراج خام** — الأساس لأي تحسين لاحق.

---

## الخطوة 3 – تحسين دقة OCR باستخدام معالج ما بعد المعالجة المدعوم بالذكاء الاصطناعي

معظم خطوط أنابيب OCR الحديثة توفر نقطة توصيل لمعالجة ما بعد المعالجة. في مثالنا، `run_postprocessor` يطبق نموذجًا خفيفًا من الذكاء الاصطناعي يصحح الأخطاء الشائعة، يطبع علامات الترقيم، وحتى يعيد ترتيب الكلمات عندما يكون التخطيط مربكًا.

```python
# Step 3: Apply the AI‑enhanced post‑processor to improve accuracy
enhanced_result = ocr_engine.run_postprocessor(raw_result)
```

**لماذا نستخدم معالج ما بعد المعالجة؟**  
حتى أفضل محركات OCR تتعثر أمام الخطوط المشوهة أو الخلفيات الضوضائية. طبقة مدفوعة بالذكاء الاصطناعي يمكنها التعلم من مجموعة نصوص مصححة، مما **يحسن دقة OCR** بشكل كبير دون تدخل يدوي.

---

## الخطوة 4 – طباعة كل من النتائج الخام والنتائج المحسنة بالذكاء الاصطناعي

عرض الفرق جنبًا إلى جنب يساعدك على تقييم فعالية معالج ما بعد المعالجة وتحديد ما إذا كانت هناك حاجة لتعديلات إضافية.

```python
# Step 4: Print the raw and AI‑enhanced OCR results
print("Raw OCR:      ", raw_result.text)
print("AI‑enhanced:  ", enhanced_result.text)
```

### النتيجة المتوقعة

```
Raw OCR:       Th1s 1s 4  s@mple w1th typ0s.
AI‑enhanced:   This is a sample with typos.
```

في الناتج الخام يمكنك ملاحظة الأخطاء الواضحة (`Th1s` → `This`, `4` → `a`, `s@mple` → `sample`). النسخة المحسنة بالذكاء الاصطناعي تنظف هذه الأخطاء، لتقدم جملة قابلة للقراءة البشرية.

---

## مثال كامل يعمل (جميع الخطوات مجمعة)

فيما يلي السكربت الكامل الذي يمكنك نسخه ولصقه في ملف باسم `ocr_demo.py`. تأكد من استبدال `"YOUR_DIRECTORY"` بالمسار الفعلي لصورتك.

```python
# ocr_demo.py
# Complete, runnable example that shows how to run OCR,
# extract text from image, and improve OCR accuracy.

# -------------------------------------------------
# 1️⃣ Import the OCR engine (replace with your actual import)
# -------------------------------------------------
# Example placeholder:
# from my_ocr_lib import OCRengine
# engine = OCRengine()

# For this tutorial we assume `engine` is already instantiated.
# -------------------------------------------------

# -------------------------------------------------
# 2️⃣ Load the image
# -------------------------------------------------
ocr_engine = engine                     # existing OCR engine instance
ocr_engine.load_image("YOUR_DIRECTORY/sample_with_typos.png")

# -------------------------------------------------
# 3️⃣ Recognize raw text
# -------------------------------------------------
raw_result = ocr_engine.recognize()    # returns an object with .text

# -------------------------------------------------
# 4️⃣ Post‑process to improve accuracy
# -------------------------------------------------
enhanced_result = ocr_engine.run_postprocessor(raw_result)

# -------------------------------------------------
# 5️⃣ Display both results
# -------------------------------------------------
print("Raw OCR:      ", raw_result.text)
print("AI‑enhanced:  ", enhanced_result.text)
```

شغّله باستخدام:

```bash
python ocr_demo.py
```

سترى السلاسل الخام والمنقحة مطبوعةً في وحدة التحكم، تمامًا كما في قسم “النتيجة المتوقعة” أعلاه.

---

## أسئلة شائعة وحالات خاصة

### ماذا لو كانت صوري بصيغة مختلفة (مثل PDF أو TIFF)؟

معظم محركات OCR تقبل مسار ملف، لكنها قد تحتاج إلى خطوة تحويل لملفات PDF متعددة الصفحات. يمكنك استخدام `pdf2image` لتحويل كل صفحة إلى PNG قبل تمريرها إلى المحرك.

### كيف أتعامل مع لغات غير الإنجليزية؟

مرّر رمز اللغة إلى المحرك أثناء التهيئة، مثال: `engine = OCRengine(lang='fra')`. قد يحتاج معالج ما بعد المعالجة أيضًا إلى نموذج خاص باللغة لتصحيح الحركات بشكل صحيح.

### ما زال ناتج OCR يحتوي على رموز غريبة — ماذا أفعل؟

فكّر في تحضير الصورة مسبقًا:  
- **تغيير الحجم** إلى DPI أعلى (300 dpi يعتبر قاعدة جيدة).  
- **تحويل إلى تدرج الرمادي** لتقليل الضوضاء اللونية.  
- **تطبيق العتبة** (`cv2.threshold`) لتقوية التباين.

هذه الخطوات غالبًا ما **تحسن دقة OCR** قبل تشغيل معالج الذكاء الاصطناعي.

---

## نصائح للحصول على أقصى استفادة من سير عمل OCR الخاص بك

- **المعالجة الدفعية:** كرّر عبر مجلد من الصور وخزّن كل نتيجة في CSV للتحليل لاحقًا.  
- **التخزين المؤقت:** إذا قمت بتشغيل نفس الصورة عدة مرات، احفظ النتيجة الخام لتفادي الحساب المتكرر.  
- **تحديث النماذج:** قم بإعادة تدريب أو تحديث معالج ما بعد المعالجة بانتظام باستخدام عينات مصححة حديثًا؛ النموذج يتحسن مع الوقت.  
- **تسجيل الأخطاء:** التقط الاستثناءات من `recognize()` و `run_postprocessor()` لتحديد الملفات المسببة للمشكلات لاحقًا.

---

## الخلاصة

أنت الآن تعرف **كيفية تشغيل OCR** على أي صورة، من تحميل الصورة إلى استخراج النص وأخيرًا صقل الناتج باستخدام معالج ما بعد المعالجة المدعوم بالذكاء الاصطناعي. باتباع الخطوات أعلاه ستحصل باستمرار على سلاسل أنظف وأكثر موثوقية — سواء كنت تبني ماسح إيصالات، أرشيف مستندات، أو مشروعًا هواة بسيطًا.

مستعد للتحدي التالي؟ جرّب دمج **استخراج النص من الصورة** في قاعدة بيانات قابلة للبحث، أو جرب قواعد معالجة ما بعد المعالجة المخصصة لمجالك. السماء هي الحد، ومع خط الأنابيب الصحيح نادراً ما يمر خطأ إملائي دون أن يُصحح.

برمجة سعيدة! 🚀

![how to run OCR example](https://example.com/ocr-demo.png "how to run OCR example")

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}