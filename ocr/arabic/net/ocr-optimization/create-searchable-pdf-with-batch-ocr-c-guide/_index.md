---
category: general
date: 2025-12-29
description: إنشاء ملف PDF قابل للبحث من الصور الممسوحة ضوئياً باستخدام معالجة الدُفعات
  لـ Aspose OCR. تعلم كيفية تحويل الصور إلى PDF، ومعالجة الصور مسبقًا للتعرف الضوئي
  على الأحرف، وتصحيح ميل المستندات الممسوحة.
draft: false
keywords:
- create searchable pdf
- batch ocr processing
- convert images to pdf
- preprocess images for ocr
- deskew scanned documents
language: ar
og_description: إنشاء ملف PDF قابل للبحث من الصور الممسوحة ضوئياً باستخدام معالجة
  دفعة Aspose OCR. تعلم كيفية تحويل الصور إلى PDF، ومعالجة الصور مسبقاً للتعرف الضوئي
  على الأحرف، وتصحيح انحراف المستندات الممسوحة.
og_title: إنشاء ملف PDF قابل للبحث باستخدام OCR دفعي – دليل C#
tags:
- OCR
- C#
- PDF/A
- Aspose
title: إنشاء ملف PDF قابل للبحث باستخدام OCR دفعي – دليل C#
url: /ar/net/ocr-optimization/create-searchable-pdf-with-batch-ocr-c-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# إنشاء ملف PDF قابل للبحث باستخدام OCR دفعي – دليل C#

هل احتجت يوماً إلى **إنشاء ملفات PDF قابلة للبحث** من مجموعة هائلة من الصور الممسوحة ضوئياً وشعرت بأنك عالق في الخطوة الأولى؟ لست وحدك—معظم المطورين يواجهون نفس المشكلة عندما تتعامل مع مسحات غير مرتبة، صفحات غير مستوية، أو مجرد تحويل جماعي عادي.  

الخبر السار؟ باستخدام Aspose OCR يمكنك إنشاء خط أنابيب **معالجة OCR دفعي** لا يقتصر فقط على **تحويل الصور إلى PDF** بل أيضاً **معالجة الصور مسبقاً للـ OCR** وحتى **تصحيح الميل** للمستندات الممسوحة تلقائياً. في هذا الدرس سنستعرض العملية بالكامل، من إعداد المحرك إلى تحسين الناتج، بحيث يمكنك تشغيله على مجلد من الملفات والحصول على ملفات PDF/A‑2b قابلة للبحث.

> **ما ستحصل عليه:** تطبيق وحدة تحكم C# واحد قابل للتنفيذ يأخذ مجلدًا من الصور (أو ملفات PDF)، ينظف كل صفحة، ينفّذ OCR، ويضع ملف PDF/A‑2b قابل للبحث بجوار المصدر. لا مقتطفات متفرقة، بل حل متكامل واحد.

---

## المتطلبات المسبقة

- .NET 6 SDK أو أحدث (الكود يتوافق أيضاً مع .NET Core).  
- حزمة NuGet الخاصة بـ Aspose OCR (`Aspose.OCR`).  
- مجلد يحتوي على صور ممسوحة (TIFF, JPEG, PNG) أو ملفات PDF تريد تحويلها إلى PDF قابل للبحث.  
- (اختياري) مفتاح ترخيص حقيقي—إلا أن وضع التجربة سيضيف علامة مائية، لكنه يكفي للاختبار.

إذا كان لديك كل ذلك، لنبدأ.

---

## نظرة عامة – كيف ينشئ خط الأنابيب الكامل ملف PDF قابل للبحث

1. **تفعيل وضع التجربة** (أو تحميل الترخيص).  
2. **تهيئة `OcrBatchProcessor`** – تحديد مكان قراءة الملفات، مكان كتابة ملفات PDF، الصيغة المستخدمة، وعدد الخيوط التي تعمل بالتوازي.  
3. **معالجة كل صورة مسبقاً** – تصحيح الميل، إزالة الضوضاء، وإزالة الخلفيات حتى يرى محرك OCR صفحة نظيفة.  
4. **تشغيل الدفعة** – Aspose يعالج كل ملف، ينفّذ OCR، ويكتب ملف PDF/A‑2b قابل للبحث.  
5. **إعلام الانتهاء** – رسالة بسيطة في وحدة التحكم، ويمكنك ربط مسجل أو webhook إذا رغبت.

هذا هو تدفق المستوى العالي. الكود أدناه يطبق كل خطوة مع تعليقات وافرة، حتى تتمكن من تعديل أي جزء دون كسر البرنامج ككل.

---

## الخطوة 1 – تفعيل وضع التجربة (أو تحميل الترخيص)

قبل أن تتمكن من استدعاء أي فئة من Aspose يجب إبلاغ المكتبة بأنك مرخص. للتجارب السريعة يكفي وضع التجربة.

```csharp
using Aspose.OCR;
using Aspose.OCR.Models;
using Aspose.OCR.Filters;
using Aspose.OCR.Batch;

// Activate trial mode – replace with OcrEngine.SetLicense("YourLicenseFile.lic") for production
OcrEngine.EnableTrialMode();
```

> **نصيحة احترافية:** ضع تفعيل الترخيص في أعلى ملف `Program.cs`. إذا نسيت ذلك، سيطرح المحرك استثناءً في أول مرة تستدعي فيها `Process()`.

---

## الخطوة 2 – تهيئة محرك معالجة OCR الدفعي

هنا نُعد كائن **معالجة OCR الدفعي**. لاحظ أن `InputFolder` و `OutputFolder` متطابقان في هذا المثال، لكن يمكنك فصلهما إذا رغبت.

```csharp
// Define where your source images live and where the searchable PDFs should be saved
var ocrBatch = new OcrBatchProcessor
{
    // Folder that contains the images or PDFs to be processed
    InputFolder = @"C:\Scans\Incoming",

    // Folder where searchable PDF/A‑2b files will be saved
    OutputFolder = @"C:\Scans\Processed",

    // Choose the output format – searchable PDF/A‑2b (perfect for archiving)
    OutputFormat = SaveFormat.SearchablePdf,

    // Limit the number of concurrent OCR operations to avoid CPU spikes
    MaxDegreeOfParallelism = 3,

    // Pre‑process each image: deskew, denoise, and remove background
    Preprocess = img => ImageFilters
                            .Deskew(img)          // fixes rotated pages
                            .Denoise()            // reduces speckles
                            .RemoveBackground()   // clears colored backgrounds
};
```

### لماذا هذه الإعدادات مهمة

- **`MaxDegreeOfParallelism`**: تشغيل عدد كبير من خيوط OCR قد يثقل وحدة المعالجة، خاصة على جهاز عمل متوسط. ثلاث خيوط تُعد نقطة توازن لمعظم الحواسيب المحمولة رباعية النوى.  
- **خط أنابيب `Preprocess`**: الفلاتر الثلاثة معاً تحسّن دقة OCR بشكل كبير. تصحيح الميل يعالج مشكلة “المسح المائل”، إزالة الضوضاء تحذف التشويش العشوائي، وإزالة الخلفية تضمن أن يرى المحرك النص الأسود على خلفية بيضاء فقط.  
- **`SaveFormat.SearchablePdf`**: يُنشئ ملفات PDF/A‑2b جاهزة للأرشفة وقابلة للبحث—وهو مطلب للعديد من معايير الامتثال.

---

## الخطوة 3 – تنفيذ الدفعة ومشاهدة السحر يحدث

تشغيل الدفعة بسيط كاستدعاء `Process()`. الطريقة تحجب التنفيذ حتى يكتمل كل ملف، ثم تُعيد التحكم. إذا أردت تقارير تقدم يمكنك ربط حدث `ProgressChanged` (غير موضح هنا).

```csharp
// Start processing – this will walk through every file in InputFolder
ocrBatch.Process();

// Let the user (or calling script) know we’re finished
Console.WriteLine("All files processed. Searchable PDFs are ready.");
```

عند طباعة سطر النهاية في وحدة التحكم، ستجد ملف PDF قابل للبحث لكل صورة مدخلة في `C:\Scans\Processed`. افتح أي منها في Adobe Reader، اضغط **Ctrl+F**، وستتمكن من البحث عن النص المستخرج من المسح.

---

## الخطوة 4 – برنامج كامل قابل للتنفيذ (جاهز للنسخ واللصق)

فيما يلي البرنامج **المتكامل المستقل** الذي يمكنك وضعه في مشروع وحدة تحكم جديد (`dotnet new console`). تأكد من إضافة حزمة Aspose.OCR عبر NuGet أولاً (`dotnet add package Aspose.OCR`).

```csharp
// Program.cs
using Aspose.OCR;
using Aspose.OCR.Models;
using Aspose.OCR.Filters;
using Aspose.OCR.Batch;

namespace CreateSearchablePdfDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // 1️⃣ Activate trial mode (replace with real license for production)
            OcrEngine.EnableTrialMode();

            // 2️⃣ Configure batch OCR processing
            var ocrBatch = new OcrBatchProcessor
            {
                InputFolder = @"C:\Scans\Incoming",   // 👉 change to your source folder
                OutputFolder = @"C:\Scans\Processed", // 👉 change to your target folder
                OutputFormat = SaveFormat.SearchablePdf,
                MaxDegreeOfParallelism = 3,
                Preprocess = img => ImageFilters
                                        .Deskew(img)          // fixes rotated pages
                                        .Denoise()            // cleans up noise
                                        .RemoveBackground()   // strips colored backgrounds
            };

            // 3️⃣ Run the batch
            ocrBatch.Process();

            // 4️⃣ Notify completion
            Console.WriteLine("All files processed. Searchable PDFs are ready.");
        }
    }
}
```

### المخرجات المتوقعة

```
All files processed. Searchable PDFs are ready.
```

بعد الانتهاء، سيفتح المجلد `C:\Scans\Processed` مجموعة من ملفات `.pdf`—كل منها قابل للبحث ومتوافق مع PDF/A‑2b. افتح أي ملف، اكتب كلمة تعرف أنها موجودة في المسح الأصلي، وستظهر النتيجة فوراً.

---

## أسئلة شائعة وتعامل مع الحالات الخاصة

### ماذا لو كان مجلد المصدر يحتوي على ملفات PDF بالفعل؟

يمكن لـ Aspose OCR استيعاب ملفات PDF مباشرة؛ سيقوم بتحويل كل صفحة إلى صورة، يطبق نفس فلاتر **المعالجة المسبقة**، ويضيف طبقة OCR. لا تحتاج إلى كود إضافي.

### كيف أغيّر صيغة الإخراج إلى PDF عادي (غير قابل للبحث)?

استبدل `SaveFormat.SearchablePdf` بـ `SaveFormat.Pdf`. ستفقد طبقة النص القابلة للبحث، لكن ستبقى الجودة البصرية كما هي.

### مسحاتي ملونة—هل تؤثر إزالة الخلفية على ذلك؟

`RemoveBackground()` تستهدف الخلفيات غير البيضاء مع الحفاظ على النص الرئيسي. إذا أردت الحفاظ على الرسومات الملونة، يمكنك حذف هذا الفلتر:

```csharp
.Preprocess = img => ImageFilters.Deskew(img).Denoise()
```

### أعمل على خادم بذاكرة RAM محدودة—هل يمكنني تقليل عدد الخيوط؟

بالطبع. اضبط `MaxDegreeOfParallelism` إلى `1` أو `2`. ستستغرق الدفعة وقتاً أطول، لكن استهلاك الذاكرة سيقل.

---

## ملخص بصري (اختياري)

إذا كنت تفضّل مخططًا سريعًا، تخيّل هذا التدفق:

![Create searchable pdf workflow – shows input folder → preprocessing → OCR → searchable PDF output](/images/ocr-workflow.png)

*نص بديل للصورة:* **مخطط سير عمل إنشاء PDF قابل للبحث** – يوضح معالجة OCR الدفعي، التحويل، وخطوات تصحيح الميل.

---

## الخاتمة

أصبح لديك الآن حل **متكامل وجاهز للإنتاج** لإنشاء ملفات **PDF قابلة للبحث** من أي مجموعة من الصور الممسوحة. من خلال الاستفادة من **معالجة OCR الدفعي**، يمكنك **تحويل الصور إلى PDF**، **معالجة الصور مسبقاً للـ OCR**، وتصحيح الميل تلقائياً—كل ذلك ببضع أسطر من C#.

ما الخطوة التالية؟ جرّب إضافة نظام تسمية مخصص، ربط إطار تسجيل لتقارير درجات ثقة OCR، أو تجربة فلاتر `ImageFilters` أخرى مثل `Sharpen()` للنص الضعيف. واجهة Aspose OCR مرنة بما يكفي لتتطور مع احتياجاتك.

برمجة سعيدة، ولتكن ملفات PDF الخاصة بك دائمًا قابلة للبحث!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}