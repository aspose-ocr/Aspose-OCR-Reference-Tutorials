---
category: general
date: 2026-01-13
description: كيفية التعرف الضوئي على النص العربي في C# – تعلم كيفية التعرف الضوئي
  على النص العربي، استخراج النص العربي، والتعرف على النص العربي من الصور باستخدام
  Aspose OCR.
draft: false
keywords:
- how to ocr arabic
- extract arabic text
- recognize arabic text
- load image for ocr
- arabic language ocr
language: ar
og_description: كيفية التعرف الضوئي على النص العربي في C# – اكتشف الطريقة خطوة بخطوة
  للتعرف الضوئي على النص العربي، استخراج النص العربي، والتعرف على النص العربي باستخدام
  Aspose OCR.
og_title: كيفية التعرف الضوئي على الحروف العربية في C# – دليل كامل
tags:
- OCR
- C#
- Aspose
title: كيفية إجراء OCR للغة العربية في C# – دليل شامل
url: /ar/net/text-recognition/how-to-ocr-arabic-in-c-complete-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# كيفية التعرف الضوئي على الحروف العربية في C# – دليل كامل

هل احتجت يومًا إلى **كيفية التعرف الضوئي على الحروف العربية** لكن شعرت بالحيرة عند سؤال “من أين أبدأ؟” لست وحدك. قد يكون التعرف الضوئي على الحروف العربية صعبًا بسبب النص من اليمين إلى اليسار، والروابط الحرفية، ومجموعة الأحرف الغنية. الخبر السار؟ باستخدام Aspose OCR يمكنك استخراج النص العربي من صورة ببضع أسطر من كود C#.

في هذا الدرس سنستعرض كل ما تحتاج معرفته: من تحميل صورة للتعرف الضوئي إلى التعرف على النص العربي، ومعالجة المشكلات الشائعة، وطباعة النتيجة إلى وحدة التحكم. لا حاجة لأي وثائق خارجية—كل شيء هنا. في النهاية ستتمكن من **استخراج النص العربي** من أي صورة، سواء كانت علامة شارع، مستندًا ممسوحًا ضوئيًا، أو لقطة شاشة.

## المتطلبات المسبقة

- .NET 6.0 أو أحدث (تعمل الواجهة البرمجية أيضًا مع .NET Framework 4.6+)
- رخصة Aspose OCR صالحة (يمكنك البدء بمفتاح تقييم مجاني)
- ملف صورة يحتوي على أحرف عربية (مثال: `arabic_sign.jpg`)
- Visual Studio 2022 أو أي بيئة تطوير متوافقة مع C#

إذا كان لديك هذه المتطلبات بالفعل، عظيم—لنبدأ.

## الخطوة 1: تثبيت حزمة Aspose  OCR عبر NuGet

أولًا وقبل كل شيء. المكتبة موجودة على NuGet، لذا أضفها إلى مشروعك:

```bash
dotnet add package Aspose.OCR
```

هذا الأمر الواحد يجلب لك كل ما تحتاجه: محرك OCR الأساسي، حزم اللغات، وأدوات معالجة الصور. لا حاجة للبحث اليدوي عن ملفات DLL.

## الخطوة 2: تحميل الصورة للتعرف الضوئي

قبل أن يقوم المحرك بسحره، يحتاج إلى صورة bitmap. طريقة `OcrImage.FromFile` تقرأ الملف وتجهزه للمعالجة. إليك الكود:

```csharp
using Aspose.OCR;

class ArabicDemo
{
    static void Main()
    {
        // Step 2: Load the image that contains Arabic text
        OcrImage image = OcrImage.FromFile("YOUR_DIRECTORY/arabic_sign.jpg");
        
        // The rest of the steps follow…
    }
}
```

> **نصيحة احترافية:** استخدم مسارًا مطلقًا أو تأكد من نسخ الصورة إلى دليل الإخراج (`Copy to Output Directory = Copy always`). وإلا ستحصل على استثناء “الملف غير موجود”.

## الخطوة 3: إنشاء مثيل محرك OCR

الآن نقوم بإنشاء مثيل `OcrEngine` الأساسي. هذا الكائن يحتوي على جميع خيارات التكوين، مثل اللغة، DPI، ومرشحات ما قبل المعالجة.

```csharp
// Step 1: Create an OCR engine instance
OcrEngine ocrEngine = new OcrEngine();
```

قد تتساءل لماذا ننشئ المحرك *بعد* تحميل الصورة. من الناحية التقنية يمكنك فعل ذلك بأي طريقة، لكن فصل الخطوتين يجعل الكود أكثر قابلية للقراءة ويسهل استبدال مصدر الصورة لاحقًا (مثلاً من تدفق أو URL).

## الخطوة 4: التعرف على النص العربي

جوهر الدرس: إخبار المحرك **بالتعرف على النص العربي**. توفر Aspose تعداد `OcrLanguage`—فقط مرر `OcrLanguage.Arabic` إلى طريقة `Recognize`.

```csharp
// Step 3: Recognize the text using Arabic language support
OcrResult ocrResult = ocrEngine.Recognize(image, OcrLanguage.Arabic);
```

تحت الغطاء، يطبق المحرك نماذج حروف مخصصة للغة، لذا تحصل على دقة أعلى مقارنةً باستدعاء OCR عام. إذا كنت بحاجة إلى التعرف على عدة لغات في نفس الصورة، يمكنك دمجها باستخدام عامل OR البتّي (`|`).

## الخطوة 5: إخراج النص المتعرف عليه

أخيرًا، اعرض النتيجة. `ocrResult.Text` يحتوي على تمثيل النص العادي، مع الحفاظ على فواصل الأسطر.

```csharp
// Step 4: Output the recognized text to the console
System.Console.WriteLine(ocrResult.Text);
```

عند تشغيل البرنامج، يجب أن ترى شيئًا مشابهًا لـ:

```
مركز المدينة
```

هذه هي العبارة العربية التي كانت على العلامة الأصلية. 🎉

## مثال كامل وجاهز للتنفيذ

فيما يلي البرنامج الكامل الذي يمكنك نسخه ولصقه في مشروع وحدة تحكم جديد. يتضمن جميع الخطوات السابقة، بالإضافة إلى بعض الفحوصات الوقائية.

```csharp
using System;
using Aspose.OCR;

class ArabicDemo
{
    static void Main()
    {
        // 1️⃣ Create the OCR engine
        OcrEngine ocrEngine = new OcrEngine();

        // 2️⃣ Load the image that contains Arabic text
        string imagePath = "YOUR_DIRECTORY/arabic_sign.jpg";
        if (!System.IO.File.Exists(imagePath))
        {
            Console.WriteLine($"Error: Image not found at '{imagePath}'.");
            return;
        }

        OcrImage image = OcrImage.FromFile(imagePath);

        // 3️⃣ Recognize Arabic text (the core of how to OCR Arabic)
        OcrResult ocrResult = ocrEngine.Recognize(image, OcrLanguage.Arabic);

        // 4️⃣ Show the extracted Arabic text
        Console.WriteLine("=== Recognized Arabic Text ===");
        Console.WriteLine(ocrResult.Text);
    }
}
```

**المخرجات المتوقعة** (حسب محتوى الصورة):

```
=== Recognized Arabic Text ===
مركز المدينة
```

إذا ظهرت المخرجات مشوهة، تحقق من أن الصورة عالية الدقة (≥300  DPI) وأن النص غير مشوه بشكل مفرط. يمكن أن تساعد المعالجة المسبقة (مثل التثنائي) أيضًا في تحسين الدقة، لكن ذلك خارج نطاق هذا الدليل السريع.

## أسئلة شائعة وحالات خاصة

### ماذا لو كانت الصورة تحتوي على العربية والإنجليزية معًا؟

مرر علم لغة مركب:

```csharp
OcrResult result = ocrEngine.Recognize(image, OcrLanguage.Arabic | OcrLanguage.English);
```

سيقوم المحرك بتبديل النماذج أثناء التشغيل، مما يمنحك نتيجة مختلطة بين اللغتين.

### صورتي هي صفحة PDF—هل ما زال بإمكاني **تحميل الصورة للتعرف الضوئي**؟

نعم. حوّل صفحة PDF إلى صورة أولاً (باستخدام Aspose.PDF أو أي مكتبة تحويل PDF إلى صورة)، ثم مرّر الـ bitmap الناتج إلى `OcrImage.FromFile`.

### النص يظهر مقلوبًا أو بدون تشكيل—ماذا يحدث؟

العربية تُكتب من اليمين إلى اليسار، وبعض محركات OCR تحتاج إلى تحديد اتجاه التخطيط صراحة. تتعامل Aspose مع ذلك تلقائيًا، ولكن إذا لاحظت مشاكل، فعّل الخاصية `RightToLeft` على المحرك:

```csharp
ocrEngine.RightToLeft = true;
```

### كيف أحسن الدقة للصور منخفضة الجودة؟

- زيادة DPI الصورة (يفضل 300+).  
- استخدم `ocrEngine.Preprocess` لتطبيق الشحذ أو التثنائي.  
- قص الخلفية غير الضرورية قبل استدعاء `Recognize`.

## نصائح وحيل (مستوى احترافي)

- **قم بتخزين المحرك في الذاكرة المؤقتة** إذا كنت تعالج العديد من الصور دفعة واحدة؛ إنشاء مثيل جديد في كل مرة يضيف عبئًا.  
- **قم بتحرير** `OcrImage` عند الانتهاء (`image.Dispose()`) لتحرير الذاكرة الأصلية.  
- بالنسبة لكتل نصية كبيرة، فكر في **البث** (streaming) للنتيجة بدلاً من تحميل السلسلة بالكامل في الذاكرة (`OcrResult.GetStream()`).

## مواضيع ذات صلة قد ترغب في استكشافها لاحقًا

- **استخراج النص العربي** من ملفات PDF باستخدام Aspose.PDF + OCR.  
- بناء **خط أنابيب OCR متعدد اللغات** يكتشف اللغة تلقائيًا.  
- دمج نتائج OCR مع **Azure Cognitive Search** للحصول على محتوى عربي قابل للبحث.

## الخاتمة

لقد غطينا سير العمل الكامل لـ **كيفية التعرف الضوئي على الحروف العربية** في C#: تثبيت Aspose OCR، **تحميل الصورة للتعرف الضوئي**، إنشاء محرك، **التعرف على النص العربي**، وأخيرًا **استخراج النص العربي** من النتيجة. الكود قصير، الخطوات واضحة، والآن لديك ما يكفي من المعرفة لتكييف الحل مع سيناريوهات أكثر تعقيدًا.

جرّبه مع صورك الخاصة—سواء كانت علامة شارع، إيصالًا، أو عقدًا ممسوحًا. بمجرد أن ترى الأحرف العربية تظهر في وحدة التحكم، ستدرك أنك أتقنت الأجزاء الأساسية لـ **التعرف الضوئي على اللغة العربية**.

هل لديك أسئلة، أو اكتشفت تعديلًا ذكيًا؟ اترك تعليقًا أدناه، وتمنياتنا لك بالبرمجة السعيدة!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}