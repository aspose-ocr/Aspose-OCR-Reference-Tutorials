---
category: general
date: 2025-12-29
description: كيفية استخدام Aspose OCR لتحويل نص الصورة واستخراج النص الكوري. دليل
  خطوة بخطوة لاستخراج نص الصورة والتعرف على النص الكوري في C#.
draft: false
keywords:
- how to use aspose
- convert image text
- extract text image
- extract korean text
- recognize korean text
language: ar
og_description: تعلم كيفية استخدام Aspose OCR لتحويل نص الصورة، واستخراج النص الكوري،
  والتعرف على النص الكوري من الصور مع مثال كامل بلغة C#.
og_title: كيفية استخدام Aspose OCR – التعرف على النص الكوري في C#
tags:
- Aspose
- OCR
- C#
- Image Processing
title: كيفية استخدام Aspose OCR في C# – التعرف على النص الكوري من الصور
url: /ar/net/text-recognition/how-to-use-aspose-ocr-in-c-recognize-korean-text-from-images/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# كيفية استخدام Aspose OCR في C# – التعرف على النص الكوري من الصور

هل تساءلت يومًا **كيف تستخدم Aspose** لاستخراج الأحرف الكورية من صورة؟ ربما لديك لقطة شاشة لعلامة شارع، أو إيصال ممسوح، أو ميم تحتاج إلى تحويله إلى نص قابل للبحث. الخبر السار هو أن Aspose OCR يجعل ذلك سهلًا للغاية، ولا تحتاج إلى التعامل مع حيل معالجة الصور منخفضة المستوى.

في هذا الدرس سنستعرض **مثالًا كاملاً قابلاً للتنفيذ** يوضح لك كيفية **تحويل نص الصورة**، **استخراج نص الصورة**، وبشكل خاص **استخراج النص الكوري** باستخدام مكتبة Aspose OCR. في النهاية ستحصل على تطبيق كونسول يطبع السلسلة الكورية التي تم التعرف عليها، وستفهم لماذا كل سطر مهم.

## ما ستحتاجه

- **.NET 6+** (أي مجموعة تطوير .NET حديثة تعمل – Visual Studio، Rider، أو سطر أوامر `dotnet`)
- **Aspose.OCR for .NET** حزمة NuGet  
  ```bash
  dotnet add package Aspose.OCR
  ```
- ملف صورة يحتوي على أحرف كورية (مثال: `korean_sign.jpg`).  
- قليل من معرفة C# – إذا كتبت برنامج "Hello World" من قبل فأنت جاهز.

> **نصيحة احترافية:** يدعم Aspose OCR أكثر من 50 لغة مباشرةً. سنركز على الكورية لأن خط الهانغول غالبًا ما يسبب مشاكل لمحركات OCR العامة.

## الخطوة 1 – تثبيت وإضافة مرجع Aspose OCR

أولاً، أضف المكتبة إلى مشروعك. أمر NuGet أعلاه يقوم بالعمل الشاق، ولكن إذا كنت تفضل الواجهة الرسومية، فقط ابحث عن *Aspose.OCR* في مدير الحزم NuGet.

```csharp
// No code needed here – the package reference is enough.
// The using directives below will bring the types into scope.
using Aspose.OCR;
using Aspose.OCR.Models;
```

> **لماذا هذا مهم:** جمل `using` تمنحك الوصول إلى `OcrEngine` و `Language` وفئة المساعدة `Image`. بدونها سيشتكي المترجم من أنواع غير معروفة.

## الخطوة 2 – تحميل الصورة التي تريد معالجتها

يعمل Aspose OCR مع غلافه الخاص `Image`، والذي يمكنه قراءة JPEG، PNG، BMP، والعديد من الصيغ الأخرى. وجهه إلى الملف الذي يحتوي على النص الكوري.

```csharp
// Step 2: Load the image containing Korean characters
var imagePath = Path.Combine(Environment.CurrentDirectory, "korean_sign.jpg");
var image = Image.Load(imagePath);
```

إذا لم يكن الملف في نفس مجلد الملف التنفيذي، عدّل المسار وفقًا لذلك. استدعاء `Image.Load` يقوم **بتحويل نص الصورة** إلى تمثيل داخلي يمكن لمحرك OCR فهمه.

![مثال على كيفية استخدام Aspose OCR](/images/aspose-ocr-korean.png "كيفية استخدام Aspose OCR للتعرف على النص الكوري")

*نص بديل للصورة: “مثال على كيفية استخدام Aspose OCR يظهر علامة شارع كورية.”*

## الخطوة 3 – ضبط محرك OCR للغة الكورية

يحتاج المحرك إلى معرفة اللغة التي يبحث عنها؛ وإلا سيفترض الإنجليزية وسيغفل أحرف الهانغول.

```csharp
// Step 3: Create and configure the OCR engine
var ocrEngine = new OcrEngine
{
    // Tell Aspose we want to recognize Korean (Hangul)
    Language = Language.Korean
};
```

> **لماذا هذا مهم:** ضبط `Language = Language.Korean` يخبر المحرك بتحميل حزمة اللغة الكورية، مما يحسن الدقة بشكل كبير لأحرف الهانغول. تخطي هذه الخطوة غالبًا ما ينتج عنه مخرجات مشوشة.

## الخطوة 4 – تشغيل عملية التعرف

الآن نطلب من Aspose فعليًا قراءة الصورة. طريقة `Recognize` تُعيد كائن `OcrResult` يحتوي على السلسلة المستخرجة ودرجات الثقة.

```csharp
// Step 4: Run OCR on the loaded image
OcrResult ocrResult = ocrEngine.Recognize(image);
```

إذا كنت بحاجة إلى **استخراج نص الصورة** من صورة أكبر (مثل لقطة شاشة تحتوي على عدة عناصر واجهة)، يمكنك أولاً قص المنطقة المطلوبة باستخدام `image.Crop(...)` قبل استدعاء `Recognize`. هذه حيلة مفيدة عندما تهتم بجزء محدد فقط من الصورة.

## الخطوة 5 – إخراج النص الكوري الذي تم التعرف عليه

أخيرًا، اعرض النتيجة. في تطبيق حقيقي قد تخزنها في قاعدة بيانات أو تُرسلها إلى واجهة برمجة تطبيقات ترجمة، لكن لهذا الدرس يكفي طباعة النتيجة في الكونسول.

```csharp
// Step 5: Print the recognized Korean text
Console.WriteLine("Recognized Korean text:");
Console.WriteLine(ocrResult.Text);
```

### النتيجة المتوقعة

```
Recognized Korean text:
서울특별시 강남구 테헤란로 123
```

بالطبع، سيعكس الإخراج الفعلي أي أحرف كورية كانت موجودة في `korean_sign.jpg`.

## مثال كامل يعمل

فيما يلي **البرنامج الكامل** يمكنك نسخه ولصقه في مشروع كونسول جديد (`dotnet new console`). تأكد من أن ملف الصورة موجود بجوار ملف `.exe` المُجمّع أو عدّل المسار.

```csharp
using System;
using System.IO;
using Aspose.OCR;
using Aspose.OCR.Models;

class Program
{
    static void Main()
    {
        // 1️⃣ Install Aspose.OCR via NuGet before running this code.

        // 2️⃣ Load the image that contains Korean text.
        var imagePath = Path.Combine(Environment.CurrentDirectory, "korean_sign.jpg");
        var image = Image.Load(imagePath);

        // 3️⃣ Create the OCR engine and set it to recognize Korean.
        var ocrEngine = new OcrEngine
        {
            Language = Language.Korean   // 👈 This enables Hangul support.
        };

        // 4️⃣ Run the OCR process.
        OcrResult ocrResult = ocrEngine.Recognize(image);

        // 5️⃣ Output the extracted Korean string.
        Console.WriteLine("Recognized Korean text:");
        Console.WriteLine(ocrResult.Text);
    }
}
```

شغّل البرنامج باستخدام `dotnet run` وسترى الأحرف الكورية تظهر في الكونسول.

## أسئلة شائعة وحالات خاصة

### ماذا لو أعاد OCR أحرفًا مشوشة؟

- **تحقق من إعداد اللغة.** نسيان `Language.Korean` هو الخطأ الأكثر شيوعًا.
- **حسّن جودة الصورة.** الصور الأكثر وضوحًا، DPI أعلى، وإضاءة مناسبة تعزز الدقة.
- **قم بتمهيد الصورة مسبقًا.** يوفر Aspose OCR فلاتر مدمجة (`image.Binarize()`, `image.Deskew()`) يمكنها تنظيف المسحات الضوضائية.

### هل يمكنني **تحويل نص الصورة** بشكل جماعي؟

بالطبع. غلف الخطوات السابقة داخل حلقة `foreach` تتنقل عبر مجلد الصور. إليك مقتطف سريع:

```csharp
foreach (var file in Directory.GetFiles(@"C:\KoreanImages", "*.jpg"))
{
    var img = Image.Load(file);
    var result = ocrEngine.Recognize(img);
    File.WriteAllText(Path.ChangeExtension(file, ".txt"), result.Text);
}
```

هذا السكربت **يستخرج نص الصورة** من كل ملف ويكتب ملفًا بامتداد `.txt` بجانبه.

### كيف أتعامل مع لغات متعددة في نفس الصورة؟

يمكن لـ Aspose OCR اكتشاف اللغة تلقائيًا إذا ضبطت `Language = Language.Auto`. ومع ذلك، قد يكون الاكتشاف التلقائي أبطأ وأقل دقة قليلًا من تحديد اللغة بدقة. إذا كنت تعلم أن الصورة تحتوي على كل من الكورية والإنجليزية، يمكنك تشغيل تمريرين—أولًا بـ `Language.Korean`، ثم بـ `Language.English`—وضم النتائج.

## نصائح لـ OCR جاهز للإنتاج

- **خزن OcrEngine في الذاكرة المؤقتة.** إنشاء محرك جديد لكل طلب يضيف عبئًا. احتفظ بـ singleton إذا كنت تعالج الكثير من الصور.
- **حدّد حجم الصورة.** الصور الكبيرة تستهلك الذاكرة؛ قلل الحجم إلى عرض ~1500 px قبل تمريرها إلى المحرك.
- **تعامل مع الاستثناءات.** غلف استدعاء `Recognize` داخل try/catch للتعامل بلطف مع الملفات التالفة.

## الخلاصة

لقد غطينا للتو **كيفية استخدام Aspose** لـ **تحويل نص الصورة**، **استخراج نص الصورة**، وبشكل خاص **استخراج النص الكوري** ببضع أسطر من كود C#. الخطوات بسيطة:

1. تثبيت Aspose OCR.  
2. تحميل صورتك.  
3. ضبط المحرك للكورية.  
4. تشغيل `Recognize`.  
5. إخراج النتيجة.

الآن يمكنك دمج هذا المقتطف في سير عمل أكبر—معالجة دفعات، أرشفة مستندات، أو حتى تطبيقات ترجمة فورية. تريد التعمق أكثر؟ جرّب إضافة طرق `Image.Preprocess()` من Aspose، جرب لغات مختلفة، أو دمج النتيجة مع Azure Cognitive Services للترجمة.

هل لديك المزيد من الأسئلة حول **التعرف على النص الكوري** أو ميزات Aspose الأخرى؟ اترك تعليقًا، وتمنياتنا لك ببرمجة سعيدة!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}