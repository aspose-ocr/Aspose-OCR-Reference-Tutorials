---
category: general
date: 2026-02-09
description: استخراج النص من الصورة باستخدام C# و OCR دون اتصال. مثال كامل لـ OCR
  في C# يوضح كيفية تحميل الصورة للـ OCR، والتعرف على النص السيريلي واستخراج النص من
  جواز السفر.
draft: false
keywords:
- extract text from image
- c# ocr example
- load image for ocr
- recognize cyrillic text
- recognize text from passport
language: ar
og_description: استخراج النص من الصورة باستخدام OCR غير متصل بـ C#. تعلم مثال خطوة
  بخطوة بلغة C# يقوم بتحميل صورة للتعرف الضوئي على الأحرف، يتعرف على النص السيريلي
  ويستخرج النص من جواز السفر.
og_title: استخراج النص من الصورة في C# – دليل OCR دون اتصال
tags:
- OCR
- C#
- Aspose
title: استخراج النص من الصورة في C# – مثال على OCR دون اتصال
url: /ar/net/text-recognition/extract-text-from-image-in-c-offline-ocr-example/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# استخراج النص من الصورة في C# – مثال OCR دون اتصال

هل احتجت يومًا إلى **استخراج النص من الصورة** لكنك علق بسبب واجهات برمجة التطبيقات التي تعتمد على الشبكة؟ لست وحدك. يواجه العديد من المطورين صعوبة عندما يحاول خدمة OCR تنزيل حزم اللغات أثناء التشغيل، خاصةً في البيئات المقيدة.

في هذا الدليل سنستعرض **c# ocr example** يعمل بالكامل دون اتصال، يحمل صورة للـ OCR، ويتعرف على النص السيريلي من جواز السفر. في النهاية ستحصل على برنامج جاهز للتنفيذ يطبع محتوى النص العادي لأي صورة مدعومة مباشرةً إلى وحدة التحكم.

## ما ستتعلمه

- كيفية إعداد Aspose.OCR للمعالجة دون اتصال.  
- الكود الدقيق لـ **load image for OCR** من القرص.  
- كيفية تكوين المحرك لـ **recognize cyrillic text**.  
- مثال **c# ocr example** كامل وجاهز للنسخ واللصق يستخراج النص من صورة على نمط جواز السفر.  

لا يلزم أي خبرة سابقة مع Aspose؛ فقط .NET 6 (أو أحدث) SDK و Visual Studio 2022 (أو VS Code) يكفيان.

---

![استخراج النص من الصورة باستخدام Aspose OCR على صورة جواز سفر](/images/ocr-passport.jpg "استخراج النص من الصورة")

## الخطوة 1: إعداد المشروع لاستخراج النص من الصورة

قبل كتابة أي كود، تأكد من إضافة حزمة Aspose.OCR NuGet إلى مشروعك:

```bash
dotnet add package Aspose.OCR
```

> **نصيحة احترافية:** استخدم العلامة `--version` لتثبيت أحدث إصدار ثابت (مثال: `13.9.0`). هذا يضمن التوافق مع .NET 6.

إنشاء تطبيق وحدة تحكم جديد بسيط كالتالي:

```bash
dotnet new console -n OfflineOcrDemo
cd OfflineOcrDemo
```

الآن لديك بيئة نظيفة حيث سنقوم **استخراج النص من الصورة** دون الحاجة إلى الاتصال بالإنترنت.

## الخطوة 2: تحميل الصورة للـ OCR – قراءة صورة جواز السفر

أول شيء يحتاجه محرك OCR هو صورة bitmap أو stream تمثل الصورة. في سيناريونا سنقوم **load image for OCR** من ملف محلي اسمه `cyrillic_passport.jpg`.

```csharp
using Aspose.OCR;
using Aspose.OCR.Models;
using System;

// Step 2: Load the image file (this is the “load image for ocr” part)
var imagePath = @"YOUR_DIRECTORY\cyrillic_passport.jpg";

// Validate the file exists – helpful when the path is wrong.
if (!System.IO.File.Exists(imagePath))
{
    Console.WriteLine($"❌ Image not found at {imagePath}");
    return;
}

// ImageStream abstracts the underlying format; it works with JPEG, PNG, etc.
var image = ImageStream.FromFile(imagePath);
```

> **لماذا هذا مهم:** توفير stream بدلاً من `Bitmap` الخام يسمح لـ Aspose باكتشاف الصيغة داخليًا، مما يقلل من الشيفرة الزائدة والأخطاء المحتملة.

## الخطوة 3: تكوين وضع عدم الاتصال واختيار اللغة السيريالية

يمكن لـ Aspose.OCR تنزيل نماذج اللغات أثناء التشغيل، لكن ذلك يناقض هدف الحل دون الاتصال. قم بإيقاف استدعاءات الشبكة وأخبر المحرك صراحةً أي لغة يجب استخدامها.

```csharp
// Step 3: Create the OCR engine and switch to offline mode
var ocrEngine = new OcrEngine
{
    Configuration =
    {
        OfflineMode = true,               // No network traffic – perfect for secure environments
        Language = new[] { OcrLanguage.Cyrillic } // We want to **recognize cyrillic text**
    }
};
```

> **حالة خاصة:** إذا احتجت لاحقًا إلى التعرف على الأحرف اللاتينية في نفس المستند، ما عليك سوى إضافة `OcrLanguage.English` إلى المصفوفة. سيتعامل المحرك مع اكتشاف متعدد اللغات تلقائيًا.

## الخطوة 4: تشغيل محرك OCR والتعرف على النص السيريلي

الآن نقوم فعليًا **recognize text from passport**‑style images. تُعيد طريقة `Recognize` كائن نتيجة غني يحتوي على النص العادي، درجات الثقة، ومربعات الإحاطة.

```csharp
// Step 4: Perform the OCR operation
OcrResult result = ocrEngine.Recognize(image);

// Step 5: Output the plain text – this is where we finally **extract text from image**
Console.WriteLine("📝 Extracted Text:");
Console.WriteLine("-------------------");
Console.WriteLine(result.PlainText);
```

### النتيجة المتوقعة في وحدة التحكم

```
📝 Extracted Text:
-------------------
ПАСПОРТ РФ
Иванов Иван Иванович
01.01.1990
...
```

إذا كان الناتج مشوشًا، تحقق مرة أخرى من وضوح الصورة المصدر وأن حزمة اللغة `OfflineMode` للسيريلي موجودة في مجلد تثبيت Aspose (عادةً `\Aspose.OCR\resources\languages`).

## مثال كامل لـ C# OCR – الكود المصدر الكامل

فيما يلي **c# ocr example** بالكامل. انسخه إلى `Program.cs` وشغّله باستخدام `dotnet run`. كل ما تحتاجه لـ **extract text from image** موجود هنا.

```csharp
using Aspose.OCR;
using Aspose.OCR.Models;
using System;

class OfflineExample
{
    static void Main()
    {
        // --------------------------------------------------------------
        // Step 1: Create the OCR engine (offline mode)
        // --------------------------------------------------------------
        var ocrEngine = new OcrEngine
        {
            Configuration =
            {
                OfflineMode = true,                     // No network calls
                Language = new[] { OcrLanguage.Cyrillic } // Recognize Cyrillic text
            }
        };

        // --------------------------------------------------------------
        // Step 2: Load the image for OCR (passport photo)
        // --------------------------------------------------------------
        var imagePath = @"YOUR_DIRECTORY\cyrillic_passport.jpg";

        if (!System.IO.File.Exists(imagePath))
        {
            Console.WriteLine($"❌ Image not found at {imagePath}");
            return;
        }

        var image = ImageStream.FromFile(imagePath);

        // --------------------------------------------------------------
        // Step 3: Recognize the text
        // --------------------------------------------------------------
        var result = ocrEngine.Recognize(image);

        // --------------------------------------------------------------
        // Step 4: Output the plain text (the final extraction)
        // --------------------------------------------------------------
        Console.WriteLine("📝 Extracted Text:");
        Console.WriteLine("-------------------");
        Console.WriteLine(result.PlainText);
    }
}
```

### تشغيل المثال

```bash
dotnet run
```

يجب أن ترى وحدة التحكم تطبع تفاصيل جواز السفر بالسيريلي. هذه هي اللحظة التي تدرك فيها أن خط أنابيب **extract text from image** يعمل.

## المشكلات الشائعة وكيفية إصلاحها

| Symptom | Likely Cause | Fix |
|---------|--------------|-----|
| فارغ `PlainText` | نموذج لغة غير صحيح أو الصورة مظلمة جدًا | تأكد من أن لغة `OfflineMode` تشمل `Cyrillic` وزد تباين الصورة |
| `System.DllNotFoundException` | الملفات الثنائية الأصلية لـ Aspose OCR مفقودة | أعد تثبيت حزمة NuGet أو انسخ `Aspose.OCR.Native.dll` إلى مجلد الإخراج |
| أداء بطيء على الصور الكبيرة | المحرك يعالج الدقة الكاملة | قلل حجم الصورة إلى ≤ 1500 px عرض قبل تمريرها إلى `ImageStream` |
| حروف مشوشة | الصورة مدورة بشكل غير صحيح | استخدم `Image.RotateFlip(RotateFlipType.Rotate90FlipNone)` قبل إنشاء الـ stream |

## الخطوات التالية – توسيع سير عمل OCR دون اتصال

- **Load image for OCR** من `MemoryStream` عند التعامل مع الملفات المرفوعة في ASP.NET Core.  
- التبديل إلى **recognize text from passport** في وضع الدفعات عبر التكرار على مجلد من مسحات جواز السفر.  
- دمج النتيجة مع **regular expressions** لاستخراج حقول مثل رقم جواز السفر أو تاريخ الميلاد.  
- تجربة `ocrEngine.Configuration.UseParallelProcessing = true` لتسريع المعالجة على أنوية متعددة.

---

### الخلاصة

لقد أظهرنا لك الآن كيفية **extract text from image** باستخدام خط أنابيب OCR في C# يعمل بالكامل دون اتصال. المثال القصير المستقل **c# ocr example** يحمل صورة، يكوّن المحرك لـ **recognize cyrillic text**، ويطبع بيانات جواز السفر المستخرجة — كل ذلك دون أي طلب شبكة.

لا تتردد في تعديل الكود، إضافة المزيد من اللغات، أو ربط الناتج بقاعدة بيانات. السماء هي الحد عندما تتقن أساسيات تحميل صورة للـ OCR والتعرف على النص من صورة على نمط جواز السفر.

هل لديك أسئلة أو ترغب في مشاركة تعديلك؟ اترك تعليقًا أدناه، وبرمجة سعيدة!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}