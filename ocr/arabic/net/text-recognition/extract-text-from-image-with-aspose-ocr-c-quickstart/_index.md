---
category: general
date: 2026-02-13
description: استخراج النص من الصورة باستخدام Aspose OCR في C#. تعلم كيفية قراءة النص
  من ملف JPG وتشغيل OCR على الصورة مع مثال كامل قابل للتنفيذ.
draft: false
keywords:
- extract text from image
- read text from jpg
- run OCR on image
- Aspose OCR C#
- OCR language packs
language: ar
og_description: استخراج النص من الصورة باستخدام Aspose OCR في C#. يوضح هذا الدليل
  كيفية قراءة النص من ملف JPG وتشغيل OCR على الصورة مع مثال كامل للكود.
og_title: استخراج النص من الصورة باستخدام Aspose OCR – دليل البدء السريع لـ C#
tags:
- C#
- OCR
- Aspose
title: استخراج النص من الصورة باستخدام Aspose OCR – بدء سريع C#
url: /ar/net/text-recognition/extract-text-from-image-with-aspose-ocr-c-quickstart/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# استخراج النص من الصورة باستخدام Aspose OCR – دليل سريع C#

هل احتجت يومًا إلى **استخراج النص من الصورة** لكن لم تكن متأكدًا من المكتبة التي تختارها؟ لست وحدك—المطورون يواجهون صعوبة مستمرة في قراءة النص من ملفات jpg، خاصة عندما يكون المحتوى بلغة غير لاتينية. الخبر السار؟ مع Aspose OCR يمكنك تشغيل OCR على ملفات الصورة ببضع أسطر من كود C# فقط، وتقوم المكتبة بتنزيل حزم اللغات عند الحاجة.

في هذا الدرس سنستعرض مثالًا كاملاً من البداية إلى النهاية يوضح لك كيفية **استخراج النص من الصورة** باستخدام Aspose OCR، وتقييد التعرف على اللغة إلى الروسية، وطباعة النتيجة على وحدة التحكم. بنهاية الدرس ستكون قادرًا على قراءة النص من ملفات jpg، وتشغيل OCR على ملفات الصورة بأي حجم، وتكييف الكود للغات أخرى مع تغييرات قليلة.

> **ما ستتعلمه**
> * كيفية تثبيت وإحالة Aspose OCR في مشروع .NET.  
> * الخطوات الدقيقة لـ **استخراج النص من الصورة**—تهيئة المحرك، اختيار اللغة، واستدعاء `RecognizeImage`.  
> * لماذا قد ترغب في قفل المحرك على حزمة لغة واحدة (السرعة، الدقة).  
> * المشكلات الشائعة مثل الملفات المفقودة أو الصيغ غير المدعومة، وكيفية التعامل معها بسلاسة.  

## المتطلبات المسبقة

قبل أن نبدأ، تأكد من وجود ما يلي على جهازك:

| المتطلب | السبب |
|-------------|--------|
| .NET 6.0 SDK or later | Aspose OCR تستهدف .NET Standard 2.0+، لذا يوفّر .NET 6 أحدث ميزات وقت التشغيل. |
| Visual Studio 2022 (or any IDE you like) | مفيد للتصحيح، لكنه ليس ضروريًا بشكل صارم. |
| An image file (`cyrillic_sample.jpg`) that contains Cyrillic text | ملف صورة (`cyrillic_sample.jpg`) يحتوي على نص سيريلي. سنستخدم هذا الملف لتوضيح **قراءة النص من jpg**. |
| Internet connection (first run only) | Aspose OCR يقوم بتنزيل حزم اللغات عند الطلب. |

إذا كان أيٌ منها غير موجود، احصل عليه الآن—ليس هناك حاجة لإعادة تشغيل بعد تثبيت SDK.

## الخطوة 1: تثبيت حزمة Aspose OCR عبر NuGet

أول شيء تحتاجه هو مكتبة Aspose OCR. افتح طرفية في مجلد مشروعك وشغّل الأمر التالي:

```bash
dotnet add package Aspose.OCR
```

هذا الأمر يجلب أحدث نسخة مستقرة (حتى فبراير 2026 هي 23.12) ويضيفها إلى ملف `.csproj` الخاص بك. الحزمة تتضمن محرك OCR الأساسي ومُنزِّل خفيف الوزن لحزم اللغات، لذا لن تحتاج إلى تضمين ملفات ضخمة مع تطبيقك.

> **نصيحة احترافية:** إذا كنت تعمل خلف بروكسي مؤسسي، اضبط متغيّر البيئة `http_proxy` قبل تشغيل الأمر لتجنب أخطاء التحميل.

## الخطوة 2: إنشاء هيكل تطبيق وحدة تحكم

لنقم بإعداد تطبيق وحدة تحكم بسيط سيستضيف منطق OCR الخاص بنا. افتح `Program.cs` (أو أنشئ ملفًا جديدًا) والصق الهيكل الأساسي أدناه. لاحظ توجيهات `using` في الأعلى—هذه تجلب مساحات أسماء Aspose OCR إلى النطاق.

```csharp
using System;
using Aspose.OCR;
using Aspose.OCR.Enums;

namespace AsposeOcrDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // We'll fill this in in the next steps.
        }
    }
}
```

في هذه المرحلة يُجمع المشروع، لكنه لا يفعل شيئًا بعد. الأقسام التالية ستُكمل سير عمل **run OCR on image**.

## الخطوة 3: تهيئة محرك OCR (استخراج النص من الصورة)

لـ **استخراج النص من الصورة**، تحتاج أولًا إلى كائن `OcrEngine`. Aspose OCR يقوم بتنزيل موارد اللغة بشكل كسول في المرة الأولى التي تُحتاج فيها، مما يحافظ على صغر حجم الملف التنفيذي الأولي.

```csharp
// Step 3: Initialize the OCR engine (resources are downloaded on demand)
var ocrEngine = new OcrEngine();
```

لماذا نهيئه هنا بدلاً من حقل ثابت؟ القيام بذلك داخل `Main` يضمن ظهور أي استثناءات (مثل الاعتمادات الأصلية المفقودة) مبكرًا، مما يسهل عملية التصحيح.

## الخطوة 4: تقييد التعرف على اللغة المطلوبة (قراءة النص من JPG)

إذا كنت تعرف لغة النص الذي تقوم بمسحه—مثلاً الروسية—يمكنك تحسين كل من السرعة والدقة عن طريق ضبط خاصية `Language`. هذا مفيد بشكل خاص عندما تقوم **بقراءة النص من jpg** التي تحتوي على أحرف سيريلي.

```csharp
// Step 4: Limit recognition to the Russian language pack (ISO code "ru")
ocrEngine.Language = OcrLanguage.Russian;
```

خلف الكواليس، Aspose OCR سيقوم بتنزيل حزمة اللغة الروسية في المرة الأولى التي تصل فيها إلى هذا السطر. عمليات التشغيل اللاحقة تعيد استخدام الحزمة المخزنة مؤقتًا، لذا لا توجد تكلفة شبكة بعد التنزيل الأول.

> **لماذا قفل اللغة؟**  
> * **الأداء:** يتخطى المحرك فحص الأحرف خارج الأبجدية المحددة.  
> * **الدقة:** تُطبق خوارزميات خاصة باللغة (مثل تردد الكلمات الشائعة)، مما يقلل من الأخطاء في التعرف.  

إذا كنت بحاجة لدعم لغات متعددة، يمكنك تمرير قائمة مفصولة بفواصل، مثل `OcrLanguage.English | OcrLanguage.Russian`.

## الخطوة 5: تنفيذ OCR على ملف JPG المستهدف (Run OCR on Image)

الآن نقوم فعليًا بـ **run OCR on image**. قدم المسار الكامل لملف JPG الخاص بك—Aspose OCR يقبل صيغًا متعددة (`.png`, `.bmp`, `.tif`, إلخ)، لكننا سنستخدم `.jpg` لهذا العرض.

```csharp
// Step 5: Perform OCR on the image containing Cyrillic text
string imagePath = @"YOUR_DIRECTORY/cyrillic_sample.jpg";
var recognizedResult = ocrEngine.RecognizeImage(imagePath);
```

إذا لم يُعثر على الملف، فإن `RecognizeImage` يطرح استثناء `FileNotFoundException`. لجعل الدرس قويًا، غلف الاستدعاء بكتلة try‑catch:

```csharp
try
{
    var recognizedResult = ocrEngine.RecognizeImage(imagePath);
    Console.WriteLine("✅ OCR succeeded!");
    Console.WriteLine("Extracted text:");
    Console.WriteLine(recognizedResult.Text);
}
catch (Exception ex)
{
    Console.Error.WriteLine($"❌ Error during OCR: {ex.Message}");
}
```

طريقة `RecognizeImage` تُعيد كائن `OcrResult` حيث تحتوي خاصية `Text` على النص المستخرج. يمكنك أيضًا الوصول إلى `Boxes` للحصول على بيانات الصناديق المحيطة إذا احتجت معلومات تخطيط لاحقًا.

## الخطوة 6: التحقق من المخرجات

عند تشغيل البرنامج (`dotnet run`)، يجب أن ترى شيئًا مشابهًا لـ:

```
✅ OCR succeeded!
Extracted text:
Пример текста на кириллице
```

إذا كان المخرج مشوشًا، تحقق مرة أخرى من وضوح الصورة وأنك اخترت اللغة الصحيحة. الصور الضبابية أو ذات التباين المنخفض هي السبب الأكثر شيوعًا لنتائج OCR ضعيفة.

### حالات الحافة والأسئلة الشائعة

| الحالة | ما الذي يجب فعله |
|-----------|------------|
| **الصورة تحتوي على لغات متعددة** | اضبط `ocrEngine.Language` على مجموعة، مثل `OcrLanguage.English | OcrLanguage.Russian`. |
| **دفعة كبيرة من الصور** | أعد استخدام نفس كائن `OcrEngine` عبر الملفات؛ فهو يخزن بيانات اللغة مؤقتًا. |
| **التشغيل على خادم بدون واجهة** | لا تحتاج إلى واجهة مستخدم—Aspose OCR يعمل بشكل جيد في Docker أو Azure Functions. |
| **الحاجة إلى دقة أعلى** | قم بتعديل `ocrEngine.Options` (مثال: `ocrEngine.Options.Denoise = true`). |
| **صيغة ملف غير مدعومة** | حوّل الصورة إلى صيغة مدعومة (PNG أو JPG) قبل استدعاء `RecognizeImage`. |

## مثال كامل يعمل

فيما يلي البرنامج الكامل الجاهز للنسخ واللصق والذي يدمج جميع الخطوات السابقة. احفظه باسم `Program.cs` وشغّله من سطر الأوامر.

```csharp
using System;
using Aspose.OCR;
using Aspose.OCR.Enums;

namespace AsposeOcrDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // 1️⃣ Initialize the OCR engine (downloads language packs on first use)
            var ocrEngine = new OcrEngine();

            // 2️⃣ Restrict recognition to Russian – speeds up processing and boosts accuracy
            ocrEngine.Language = OcrLanguage.Russian;

            // 3️⃣ Path to the JPG you want to read text from
            string imagePath = @"YOUR_DIRECTORY/cyrillic_sample.jpg";

            // 4️⃣ Perform OCR and handle possible errors
            try
            {
                var result = ocrEngine.RecognizeImage(imagePath);
                Console.WriteLine("✅ OCR completed successfully.");
                Console.WriteLine("🖼️ Extracted text:");
                Console.WriteLine(result.Text);
            }
            catch (Exception ex)
            {
                Console.Error.WriteLine($"❌ Failed to extract text from image: {ex.Message}");
            }
        }
    }
}
```

**المخرجات المتوقعة في وحدة التحكم** (بافتراض أن صورة العينة تحتوي على العبارة “Пример текста на кириллице”):

```
✅ OCR completed successfully.
🖼️ Extracted text:
Пример текста на кириллице
```

إذا استبدلت الصورة بصورة إنجليزية وغيرت `ocrEngine.Language = OcrLanguage.English;`، فإن نفس الكود سيقوم **بقراءة النص من jpg** بالإنجليزية دون أي تغييرات إضافية.

## إضافي: تشغيل OCR على ملفات متعددة

غالبًا ما ستحتاج إلى **run OCR on image** لمجموعات. إليك مقتطف سريع يمر عبر مجلد:

```csharp
string folder = @"YOUR_DIRECTORY";
foreach (var file in System.IO.Directory.GetFiles(folder, "*.jpg"))
{
    try
    {
        var result = ocrEngine.RecognizeImage(file);
        Console.WriteLine($"[{System.IO.Path.GetFileName(file)}] => {result.Text}");
    }
    catch (Exception ex)
    {
        Console.Error.WriteLine($"Error processing {file}: {ex.Message}");
    }
}
```

المحرك يعيد استخدام حزمة اللغة التي تم تنزيلها مسبقًا، لذا تعمل الدفعة بكفاءة.

## الخاتمة

أصبح لديك الآن نمط قوي وجاهز للإنتاج لـ **استخراج النص من الصورة** باستخدام Aspose OCR في C#. يغطي الدرس كل شيء من تثبيت حزمة NuGet إلى معالجة الأخطاء وتوسيع الاستخدام لملفات متعددة. سواء كنت **تقرأ النص من jpg**، أو تمسح ملفات PDF، أو تبني خط أنابيب لأتمتة المستندات، فإن النهج نفسه ينطبق—فقط استبدل حزمة اللغة أو عدّل خيارات OCR.

هل أنت مستعد للخطوة التالية؟ جرّب:

* تجربة لغات أخرى (مثال: `OcrLanguage.ChineseSimplified`).  
* استخراج معلومات التخطيط عبر `recognizedResult.Boxes`.  
* دمج تدفق OCR في API ASP.NET Core بحيث يمكن للخدمات الأخرى طلب استخراج النص عند الحاجة.

برمجة سعيدة، ولتكن صورك دائمًا واضحة بما يكفي للحصول على OCR مثالي!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}