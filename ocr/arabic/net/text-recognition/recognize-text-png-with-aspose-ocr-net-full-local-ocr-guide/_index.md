---
category: general
date: 2025-12-30
description: تعرّف على كيفية التعرف على ملفات PNG النصية دون اتصال باستخدام Aspose
  OCR .NET. استخرج النص من الصورة، وشغّل OCR محليًا وتعامل مع الأحرف الصينية في دقائق.
draft: false
keywords:
- recognize text png
- extract text from image
- run ocr locally
- extract chinese characters
- aspose ocr .net
language: ar
og_description: دليل خطوة بخطوة للتعرف على نص ملفات PNG دون اتصال باستخدام Aspose
  OCR .NET. استخراج النص من الصورة، تشغيل OCR محليًا ودعم الأحرف الصينية.
og_title: التعرف على نص PNG باستخدام Aspose OCR – دليل .NET الكامل
tags:
- OCR
- .NET
- Aspose
- Image Processing
title: التعرف على نص PNG باستخدام Aspose OCR .NET – دليل OCR المحلي الكامل
url: /ar/net/text-recognition/recognize-text-png-with-aspose-ocr-net-full-local-ocr-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# التعرف على نص PNG – دليل Aspose OCR .NET الكامل

هل احتجت يومًا إلى **التعرف على نص PNG** لكنك علقّت مع الخدمات السحابية فقط؟ لست وحدك. في العديد من البيئات المنظمة لا يمكنك إرسال الصور إلى API خارجي، لذا يصبح تشغيل OCR محليًا مهارة ضرورية.  

في هذا الدليل سنوضح لك بالضبط كيفية **التعرف على نص PNG** على جهاز Windows باستخدام مكتبة Aspose OCR لـ .NET. على طول الطريق ستتعلم أيضًا كيفية **استخراج النص من صورة**، **تشغيل OCR محليًا**، وحتى **استخراج الأحرف الصينية** دون اتصال بالإنترنت.  

بنهاية البرنامج التعليمي ستحصل على تطبيق console جاهز للتنفيذ يطبع نتيجة OCR في وحدة التحكم، وستفهم سبب كل خطوة من خطوات التكوين. لا خدمات خارجية، لا سحر مخفي—فقط كود .NET نقي.

---

## ما ستحتاجه

قبل أن نغوص، تأكد من تثبيت المتطلبات التالية:

- **.NET 6.0 SDK** أو أحدث (الكود يعمل مع .NET 5+ أيضًا).  
- **Visual Studio 2022** (الإصدار Community يكفي) أو أي محرر يمكنه تجميع C#.  
- حزمة **Aspose.OCR for .NET** من NuGet (الإصدار 23.12 في وقت كتابة هذا الدليل).  
- مجلد يحتوي على ملفات بيانات اللغة التي يحتاجها Aspose OCR للمعالجة دون اتصال.  
- صورة PNG تجريبية تحتوي على نص صيني (أو أي لغة تخطط لاختبارها).

إذا كان أي من هذه غير مألوف لك، لا تقلق—تثبيت SDK وإضافة حزمة NuGet هو عمل بنقرتين في Visual Studio.

---

## الخطوة 1: إعداد المشروع وتثبيت Aspose OCR

### إنشاء مشروع console جديد

```bash
dotnet new console -n OcrDemo
cd OcrDemo
```

### إضافة حزمة Aspose OCR من NuGet

```bash
dotnet add package Aspose.OCR --version 23.12.0
```

هذا كل شيء. الحزمة تجلب مساحة الاسم `Aspose.OCR` التي سنستخدمها لـ **التعرف على نص PNG**.

---

## الخطوة 2: تحضير موارد اللغة دون اتصال

يمكن لـ Aspose OCR العمل بالكامل دون اتصال، لكن عليك توجيه المحرك إلى مجلد يحتوي على ملفات نموذج اللغة (`*.dat`). قم بتحميل حزمة اللغة من بوابة Aspose واستخرجها إلى موقع تتحكم فيه، على سبيل المثال:

```
C:\Aspose\OCR\Resources
```

> **نصيحة احترافية:** حافظ على هيكل المجلد مسطح؛ يجب أن يكون كل ملف نموذج مباشرة تحت `Resources`.

---

## الخطوة 3: كتابة كود OCR (مثال كامل)

أنشئ ملفًا باسم `Program.cs` (استبدل الملف الافتراضي) والصق الكود التالي. كل سطر مُعلق لتتمكن من فهم سبب أهميته.

```csharp
using System;
using Aspose.OCR;

namespace OcrDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // ------------------------------------------------------------------
            // 1️⃣ Initialize the OCR engine and force offline mode.
            //    This prevents any accidental web calls – perfect for secure
            //    environments where you must **run OCR locally**.
            // ------------------------------------------------------------------
            var ocrEngine = new OcrEngine
            {
                OfflineMode = true               // No internet required
            };

            // ------------------------------------------------------------------
            // 2️⃣ Tell the engine where to find the language data files.
            //    Replace the path with the folder you created in Step 2.
            // ------------------------------------------------------------------
            ocrEngine.ResourcesPath = @"C:\Aspose\OCR\Resources";

            // ------------------------------------------------------------------
            // 3️⃣ Load the specific language model you need.
            //    Here we load Simplified Chinese because our sample image
            //    contains Chinese characters. Change this to LanguageModel.English
            //    (or another enum) if you work with other scripts.
            // ------------------------------------------------------------------
            ocrEngine.LoadLanguage(LanguageModel.ChineseSimplified);

            // ------------------------------------------------------------------
            // 4️⃣ Perform OCR on a PNG image.
            //    The Recognize method returns an OcrResult object that holds
            //    the extracted text, confidence scores, etc.
            // ------------------------------------------------------------------
            string imagePath = @"C:\Aspose\OCR\Samples\chinese_doc.png";
            var ocrResult = ocrEngine.Recognize(imagePath);

            // ------------------------------------------------------------------
            // 5️⃣ Output the recognized text to the console.
            //    This is the simplest way to **extract text from image** files.
            // ------------------------------------------------------------------
            Console.WriteLine("=== OCR RESULT ===");
            Console.WriteLine(ocrResult.Text);
            Console.WriteLine("==================");

            // Optional: Show confidence (useful for debugging)
            Console.WriteLine($"Confidence: {ocrResult.Confidence:P2}");
        }
    }
}
```

### لماذا كل خطوة مهمة

- **OfflineMode = true** – يضمن أن المكتبة لا تتصل بسحابة Aspose أبداً، مما يلبي متطلبات “تشغيل OCR محليًا”.  
- **ResourcesPath** – يحتاج المحرك إلى ملفات البيانات لفك تشفير الأحرف. بدونها ستحصل على استثناء `FileNotFoundException`.  
- **LoadLanguage** – تحميل اللغة المطلوبة فقط يقلل استهلاك الذاكرة ويسرّع عملية التعرف.  
- **Recognize** – يقبل أي صيغة صورة يدعمها .NET (`png`، `jpeg`، `bmp`). في هذا الدليل نركز على **التعرف على نص PNG** لأن PNG يحافظ على جودة غير مضغوطة، وهو مثالي لـ OCR.  
- **Confidence** – فحص سريع للمنطق؛ القيم فوق 80 % عادةً ما تعني أن الاستخراج موثوق.

---

## الخطوة 4: بناء وتشغيل التطبيق

من جذر المشروع، نفّذ:

```bash
dotnet run
```

إذا تم إعداد كل شيء بشكل صحيح، سترى شيئًا مشابهًا لـ:

```
=== OCR RESULT ===
中华人民共和国成立了
==================
Confidence: 93.45%
```

هذا الإخراج يؤكد أنك نجحت في **استخراج الأحرف الصينية** من صورة PNG دون الحاجة للإنترنت مطلقًا.

---

## الخطوة 5: التغييرات الشائعة وحالات الحافة

### استخراج نص إنجليزي أو متعدد اللغات

إذا كنت بحاجة إلى **استخراج النص من صورة** تحتوي على الإنجليزية والصينية معًا، يمكنك تحميل عدة لغات:

```csharp
ocrEngine.LoadLanguages(LanguageModel.ChineseSimplified, LanguageModel.English);
```

سيتحول المحرك تلقائيًا بين الخطوط أثناء التعرف.

### التعامل مع الصور الكبيرة

بالنسبة لملفات PNG ذات الدقة العالية جدًا، قد تواجه ضغطًا على الذاكرة. حل بسيط هو تقليل حجم الصورة قبل تمريرها إلى المحرك:

```csharp
using System.Drawing;
using System.Drawing.Imaging;

// Load, resize, and save a temporary copy
using (var original = new Bitmap(imagePath))
{
    int maxDim = 2000; // max width or height
    float scale = Math.Min((float)maxDim / original.Width, (float)maxDim / original.Height);
    int newW = (int)(original.Width * scale);
    int newH = (int)(original.Height * scale);

    using (var resized = new Bitmap(original, newW, newH))
    {
        string tempPath = Path.Combine(Path.GetTempPath(), "resized.png");
        resized.Save(tempPath, ImageFormat.Png);
        ocrResult = ocrEngine.Recognize(tempPath);
    }
}
```

### التعامل مع المسحات ذات الجودة المنخفضة

إذا انخفضت قيمة الثقة إلى أقل من 70 %، فكر في تطبيق فلاتر ما قبل المعالجة (مثل الثنائيات، إزالة الضوضاء). Aspose OCR يوفر طريقة `Preprocess` يمكن ربطها قبل `Recognize`.

---

## نصائح احترافية للاستخدام في الإنتاج

- **Cache the OcrEngine** – إنشاء محرك جديد لكل طلب يضيف عبئًا. احتفظ بنسخة Singleton إذا كنت تبني خدمة ويب.  
- **Secure the ResourcesPath** – احفظ ملفات اللغة في دليل بصلاحيات مقيدة لتجنب العبث.  
- **Log the Confidence** – احفظ قيمة الثقة جنبًا إلى جنب مع النص المستخرج؛ هذا لا يقدر بثمن عند الحاجة لتدقيق دقة OCR.  
- **Version Lock** – الـ API ثابت، لكن قم بتثبيت نسخة NuGet (`23.12.0`) في ملف `csproj` لتجنب تغييرات غير متوقعة.

---

## الخلاصة

أصبح لديك الآن حل كامل ومستقل يمكنه **التعرف على نص PNG** باستخدام Aspose OCR .NET، **استخراج النص من صورة**، **تشغيل OCR محليًا**، و**استخراج الأحرف الصينية** دون أي تبعيات خارجية. الكود جاهز للدمج في تطبيق أكبر، والشروحات تعطيك السياق لتكييفه مع لغات أو صيغ صور أخرى.

هل أنت مستعد للخطوة التالية؟ جرّب دمج محرك OCR في API بسيط باستخدام ASP.NET Core بحيث يمكنك رفع PNG عبر HTTP والحصول على النص المستخرج فورًا. أو جرب المعالجة الدفعية—تكرار عبر مجلد من الصور وكتابة كل نتيجة إلى ملف CSV. السماء هي الحد، وأنت الآن تملك الأساسيات لتذهب بعيدًا.

برمجة سعيدة، ولتكن نتائج OCR دائمًا واضحة كالكريستال! 🚀

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}