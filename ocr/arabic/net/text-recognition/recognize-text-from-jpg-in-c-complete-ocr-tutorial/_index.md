---
category: general
date: 2025-12-29
description: تعلم كيفية التعرف على النص من ملفات JPG باستخدام مثال OCR بلغة C#. استخراج
  النص من الصورة، تحويل الصورة إلى نص، وتحميل الصورة للتعرف الضوئي على الأحرف في دقائق.
draft: false
keywords:
- recognize text from jpg
- extract text from image
- c# ocr example
- convert image to text
- load image for ocr
language: ar
og_description: التعرف على النص من ملفات JPG باستخدام C#. يوضح هذا الدليل كيفية استخراج
  النص من الصورة، تحويل الصورة إلى نص، وتحميل الصورة للتعرف الضوئي على الأحرف مع مثال
  كامل للكود.
og_title: التعرف على النص من JPG في C# – دليل OCR كامل
tags:
- OCR
- C#
- Image Processing
title: التعرف على النص من JPG في C# – دليل OCR كامل
url: /ar/net/text-recognition/recognize-text-from-jpg-in-c-complete-ocr-tutorial/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# التعرف على النص من JPG في C# – دليل OCR كامل

هل احتجت يوماً إلى **التعرف على النص من ملفات JPG** لكن لم تكن متأكدًا من المكتبة التي تختارها؟ لست وحدك. يواجه العديد من المطورين نفس المشكلة عندما يحاولون استخراج النص من ملفات الصور لأول مرة، خاصةً عندما يكون المصدر JPEG.

في هذا الدليل سنرشدك عبر **مثال C# OCR** يقوم بتحميل ملف JPG، تشغيل التعرف الضوئي على الأحرف، وطباعة النتيجة في وحدة التحكم. في النهاية ستتمكن من **استخراج النص من الصورة**، **تحويل الصورة إلى نص**، وحتى تعديل الكود لتعامل مع صيغ أخرى. لا إطالة—حل عملي يمكنك نسخه ولصقه.

## ما ستتعلمه

- كيفية تمكين وضع التجربة لـ Aspose.OCR (أو التبديل إلى مفتاح مرخص)
- الخطوات الدقيقة لـ **load image for OCR** في مشروع C#
- كيفية استدعاء محرك OCR واسترجاع السلسلة المعترف بها
- نصائح للتعامل مع المشكلات الشائعة مثل JPG منخفض الدقة أو تسرب الذاكرة
- إلى أين تتجه بعد ذلك إذا كنت بحاجة إلى ملفات PDF متعددة الصفحات أو قواميس مخصصة للغات

**المتطلبات المسبقة**  
ستحتاج إلى .NET 6+ (أو .NET Framework 4.6+)، Visual Studio 2022 (أو بيئة التطوير المفضلة لديك)، وحزمة Aspose.OCR عبر NuGet. إذا لم تقم بتثبيت الحزمة بعد، نفّذ:

```bash
dotnet add package Aspose.OCR
```

الآن بعد أن تم إعداد الأساسيات، دعنا نغوص في الكود.

![مثال التعرف على النص من JPG](/images/recognize-text-from-jpg.png "لقطة شاشة تُظهر مخرجات وحدة التحكم C# بعد التعرف على النص من ملف JPG")

## الخطوة 1 – تمكين وضع التجربة (أو تطبيق الترخيص الخاص بك)

قبل أن يتمكن محرك OCR من القيام بأي شيء، يتطلب Aspose تمكين وضع التجربة أو تحميل ملف ترخيص صالح. تخطي هذه الخطوة سيتسبب في رمي استثناء أثناء التشغيل.

```csharp
using Aspose.OCR;

// Enable the free trial – remove this line once you have a license
OcrEngine.EnableTrialMode();
```

*لماذا هذا مهم*: يزيل وضع التجربة علامة “التقييم” المائية ويفتح مجموعة الميزات الكاملة لفترة محدودة. إذا أضفت ترخيصًا لاحقًا، ما عليك سوى استبدال استدعاء `EnableTrialMode` بـ `OcrEngine.SetLicense("YourLicenseFile.lic");`.

## الخطوة 2 – إنشاء كائن محرك OCR

فئة `OcrEngine` هي قلب المكتبة. إنشاء نسخة واحدة لكل تطبيق عادةً يكفي، لكن يمكنك إنشاء عدة نسخ إذا كنت بحاجة إلى إعدادات لغة مختلفة.

```csharp
// Initialize the OCR engine
var ocrEngine = new OcrEngine();
```

*نصيحة احترافية*: إذا كنت تخطط لمعالجة العديد من الصور في حلقة، أعد استخدام كائن `ocrEngine` نفسه. هذا يقلل الحمل ويُسرّع معالجة الدُفعات.

## الخطوة 3 – تحميل صورة JPG التي تريد معالجتها

هنا نقوم بـ **load image for OCR**. يعمل Aspose.OCR مع فئة `Image` من نفس النطاق، لذا لا تحتاج إلى System.Drawing.

```csharp
// Replace the path with your actual JPG location
var imagePath = @"C:\Images\sample.jpg";
var image = Image.Load(imagePath);
```

*ماذا لو لم يكن الملف JPG؟*  
يمكن لـ Aspose التعامل مع PNG و BMP و TIFF وحتى صفحات PDF. فقط غيّر امتداد الملف، وستقوم استدعاء `Image.Load` نفسه بالعمل.

## الخطوة 4 – التعرف على النص من الصورة المحملة

الآن نستدعي طريقة `Recognize`. تُعيد كائن `OcrResult` الذي يحتوي على السلسلة المستخرجة، درجات الثقة، ومعلومات التخطيط.

```csharp
// Run OCR on the loaded image
var ocrResult = ocrEngine.Recognize(image);
```

*لماذا نستخدم متغيرًا منفصلًا*: تخزين النتيجة يتيح لك فحص `ocrResult.Confidence` أو `ocrResult.TextBlocks` لاحقًا، وهو مفيد للتصحيح أو المعالجة اللاحقة.

## الخطوة 5 – عرض (أو تخزين) النص المعترف به

أخيرًا، نقوم بطباعة النص المعترف به إلى وحدة التحكم. في تطبيق حقيقي قد تكتب ذلك إلى قاعدة بيانات، ملف، أو ترسله عبر API.

```csharp
// Print the extracted text
Console.WriteLine("=== Recognized Text ===");
Console.WriteLine(ocrResult.Text);
```

**الناتج المتوقع**

```
=== Recognized Text ===
Hello, world!
This is a sample JPG image.
```

إذا كان الناتج مشوشًا، حاول زيادة دقة الصورة أو تطبيق مرشح ما قبل المعالجة (مثل الشحذ أو التحويل إلى ثنائي). كما يقدم Aspose.OCR `ImagePreprocessor` لتعديلات أكثر تقدمًا.

## مثال عملي كامل

بجمع كل ذلك معًا، إليك برنامج مستقل يمكنك تجميعه وتشغيله الآن:

```csharp
using System;
using Aspose.OCR;

class Program
{
    static void Main()
    {
        // 1️⃣ Enable trial mode (remove when you have a license)
        OcrEngine.EnableTrialMode();

        // 2️⃣ Create OCR engine
        var ocrEngine = new OcrEngine();

        // 3️⃣ Load the JPG image
        var imagePath = @"C:\Images\sample.jpg"; // 👉 Change to your file
        var image = Image.Load(imagePath);

        // 4️⃣ Recognize the text
        var ocrResult = ocrEngine.Recognize(image);

        // 5️⃣ Show the result
        Console.WriteLine("=== Recognized Text ===");
        Console.WriteLine(ocrResult.Text);
    }
}
```

انسخ الكود إلى مشروع تطبيق Console جديد، عدّل `imagePath`، واضغط **F5**. يجب أن ترى النص المستخرج يُطبع في نافذة وحدة التحكم.

## المشكلات الشائعة وكيفية إصلاحها

| المشكلة | سبب حدوثها | الحل السريع |
|-------|----------------|-----------|
| **حروف غير مفهومة** | JPG منخفض الدقة أو ضغط عالي | استخدم مصدرًا بدقة أعلى، أو استدعِ `image = ImagePreprocessor.Binarize(image);` قبل التعرف |
| **استثناء نفاد الذاكرة** | معالجة العديد من الصور الكبيرة في حلقة دون تحرير الذاكرة | غلف `Image.Load` و `ocrEngine` بعبارات `using` أو استدعِ `image.Dispose();` بعد كل تكرار |
| **لغة غير صحيحة** | اللغة الافتراضية هي الإنجليزية؛ صورتك تحتوي على لغة أخرى | عيّن `ocrEngine.Language = OcrLanguage.French;` (أو أي لغة مدعومة) قبل `Recognize` |
| **أداء بطيء** | معالجة أحادية الخيط للعديد من الملفات | قم بالتوازي باستخدام `Parallel.ForEach` وأعد استخدام نسخة `ocrEngine` واحدة لكل خيط |

## توسيع المثال

- **معالجة دفعات**: التكرار على مجلد من ملفات JPG، جمع كل `ocrResult.Text`، وكتابتها إلى ملف CSV.  
- **تحويل PDF**: بعد استخراج النص، يمكنك تمريره إلى مكتبة PDF (مثل Aspose.PDF) لإنشاء ملفات PDF قابلة للبحث.  
- **اكتشاف اللغة**: دمج Aspose.OCR مع مكتبة اكتشاف اللغة لتحديد لغة OCR المناسبة تلقائيًا.  

## الخلاصة

أصبح لديك الآن مثال **C# OCR** قوي يُ **يتعرف على النص من ملفات JPG**، **يستخرج النص من الصورة**، و **يحول الصورة إلى نص** ببضع أسطر من الكود فقط. من خلال إتقان خطوات **load image for OCR**، يمكنك تعديل هذا النمط لأي صيغة صورة أو دمجه في خطوط معالجة مستندات أكبر.

هل أنت مستعد للتحدي التالي؟ جرّب إضافة معالجة مسبقة للصور لتحسين الدقة، أو استكشف قدرات OCR متعددة اللغات من Aspose. إذا واجهت أي عائق، راجع الوثائق الرسمية لـ Aspose.OCR أو اترك تعليقًا أدناه—برمجة سعيدة!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}