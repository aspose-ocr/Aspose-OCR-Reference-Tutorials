---
category: general
date: 2026-02-09
description: كيفية استخدام OCR بسرعة مع Aspose OCR، التعرف على النص من الصورة واستخراج
  النص من PNG مع ضبط الوضع وحدود ذاكرة GPU.
draft: false
keywords:
- how to use ocr
- recognize text from image
- extract text from png
- how to set mode
- set gpu memory limit
language: ar
og_description: كيفية استخدام OCR بفعالية – تعلم التعرف على النص من الصورة، استخراج
  النص من PNG، ضبط الوضع، والتحكم في حد ذاكرة GPU في Java.
og_title: كيفية استخدام OCR مع تسريع GPU في Java
tags:
- OCR
- Java
- GPU
- Aspose
title: كيفية استخدام OCR مع تسريع GPU في جافا – دليل خطوة بخطوة
url: /ar/java/advanced-ocr-techniques/how-to-use-ocr-with-gpu-acceleration-in-java-step-by-step-gu/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# كيفية استخدام OCR مع تسريع GPU في جافا – دليل برمجة كامل

هل تساءلت يومًا **كيف تستخدم OCR** لاستخراج النص من صورة دون كتابة ملايين السطور من الشيفرة؟ لست وحدك. في العديد من المشاريع—مسح الفواتير، معالجة الإيصالات، أو مجرد رقمنة المستندات القديمة—يحتاج المطورون إلى طريقة موثوقة **للتعرف على النص من ملفات الصورة**، خاصة PNG التي غالبًا ما تحتوي على رسومات نظيفة وعالية الدقة.  

الخبر السار؟ Aspose OCR يجعل ذلك سهلًا للغاية، ومع بعض التعديلات البسيطة في الإعدادات يمكنك حتى تفويض الجزء الثقيل إلى الـ GPU. في هذا الدرس سنستعرض العملية بالكامل: من تحميل PNG، إلى **تعيين الوضع** للمعالجة على GPU، إلى **تحديد حد ذاكرة GPU**، وأخيرًا طباعة النص المستخرج. في النهاية ستحصل على برنامج جافا قابل للتنفيذ يفعل ما تحتاجه بالضبط.

## ما ستتعلمه

- كيفية تثبيت واستيراد Aspose OCR لجافا.
- كيفية **التعرف على النص من ملفات الصورة** باستخدام المكتبة.
- كيفية **استخراج النص من PNG** بكفاءة.
- كيفية **تعيين الوضع** إلى GPU والتحكم في استهلاك الذاكرة باستخدام **setGpuMemoryLimit**.
- المشكلات الشائعة ونصائح للاستخدام في العالم الحقيقي.

### المتطلبات المسبقة

- جافا 8 أو أحدث (الكود يتوافق مع JDK 11 أيضًا).
- بطاقة NVIDIA GPU مع برنامج تشغيل متوافق مع CUDA إذا كنت تريد تسريع GPU.
- ملف JAR الخاص بـ Aspose OCR لجافا (حمّله من موقع Aspose أو أضفه عبر Maven/Gradle).
- صورة PNG تجريبية (مثال: `sample1.png`) موجودة في مجلد يمكنك الإشارة إليه.

---

## كيفية استخدام OCR – تمكين وضع GPU

أول شيء عليك فعله هو إخبار Aspose OCR بأنك تريد تشغيله على الـ GPU بدلاً من الـ CPU. هنا يأتي دور كلمة **how to set mode**.

```java
// Step 1: Create the OCR engine
OcrEngine ocrEngine = new OcrEngine();

// Step 2: Grab the configuration object
OcrEngineConfiguration config = ocrEngine.getConfiguration();

// Step 3: Switch processing mode to GPU
config.setProcessingMode(ProcessingMode.GPU);   // requires a CUDA‑compatible driver

// (Optional) Step 4: Limit GPU memory usage to 1024 MB
config.setGpuMemoryLimit(1024);                 // set gpu memory limit (MB)
```

**لماذا هذا مهم:**  
معالجة الـ GPU يمكن أن تكون أسرع بكثير للدفعات الكبيرة أو الصور عالية الدقة، لكنها تستهلك ذاكرة الفيديو. باستدعاء `setGpuMemoryLimit`، تمنع تطبيقك من استهلاك كامل ذاكرة الـ GPU، وهو أمر حاسم عندما يعمل الجهاز نفسه على مهام أخرى (مثل واجهة مستخدم أو نموذج تعلم آلي).

---

## التعرف على النص من الصورة باستخدام Aspose OCR

الآن بعد أن تم تكوين المحرك، نحتاج إلى توجيهه إلى الملف الذي نريد قراءته. هذا هو جوهر **recognize text from image**.

```java
// Step 5: Define the image to be processed
ImageRecognitionResult imageInfo = new ImageRecognitionResult();
imageInfo.setImagePath("YOUR_DIRECTORY/sample1.png");

// Step 6: Run the OCR operation
RecognitionResult ocrResult = ocrEngine.recognize(imageInfo);
```

**ماذا يحدث خلف الكواليس؟**  
Aspose OCR يحمل ملف PNG، يسبق معالجته (تحويل إلى ثنائي، تصحيح الميل، إلخ)، ثم يشغل شبكة OCR العصبية على الـ GPU. كائن النتيجة يحتوي على النص الخام بالإضافة إلى درجات الثقة لكل سطر.

---

## استخراج النص من PNG مع حد ذاكرة GPU

بعد التعرف، استخراج السلسلة النصية العادية أمر سهل، لكن كثيرًا ما ينسى المطورون التحقق من المخرجات. إليك كيفية **extract text from PNG** بأمان وعرضه.

```java
// Step 7: Output the recognized text
System.out.println("Recognized text:");
System.out.println(ocrResult.getText());
```

**المخرجات المتوقعة (مثال):**

```
Recognized text:
Invoice #12345
Date: 2026-02-09
Total: $1,250.00
Thank you for your business!
```

إذا احتوت الصورة على ضوضاء أو خطوط غير مألوفة، قد ترى أحرفًا مشوشة. في هذه الحالة، فكر في تعديل خيارات ما قبل المعالجة (مثل `config.setLanguage(Language.ENGLISH)` أو `config.setAutoSkewCorrection(true)`).

---

## مثال كامل قابل للتنفيذ

فيما يلي البرنامج الكامل بجافا الذي يجمع كل شيء معًا. انسخه إلى ملف باسم `GpuExample.java`، عدل مسار الصورة، وشغله باستخدام `javac`/`java` أو من خلال بيئة التطوير المتكاملة الخاصة بك.

```java
import com.aspose.ocr.*;
import com.aspose.ocr.configuration.*;

public class GpuExample {
    public static void main(String[] args) throws Exception {

        // Step 1: Specify the image to be processed
        ImageRecognitionResult imageInfo = new ImageRecognitionResult();
        imageInfo.setImagePath("YOUR_DIRECTORY/sample1.png");

        // Step 2: Create the OCR engine and enable GPU processing
        OcrEngine ocrEngine = new OcrEngine();
        OcrEngineConfiguration config = ocrEngine.getConfiguration();

        // Step 3: Set processing mode to GPU (requires CUDA driver)
        config.setProcessingMode(ProcessingMode.GPU);

        // Step 4 (optional): Limit GPU memory usage to 1024 MB
        config.setGpuMemoryLimit(1024);

        // Step 5: Perform recognition
        RecognitionResult ocrResult = ocrEngine.recognize(imageInfo);

        // Step 6: Print the extracted text
        System.out.println("Recognized text:");
        System.out.println(ocrResult.getText());
    }
}
```

**تشغيل البرنامج**

```bash
javac -cp "path/to/aspose-ocr.jar" GpuExample.java
java -cp ".:path/to/aspose-ocr.jar" GpuExample
```

تأكد من أن ملف الـ JAR موجود في مسار الـ classpath؛ وإلا ستحصل على استثناء `ClassNotFoundException`.

---

## نصائح احترافية ومشكلات شائعة

- **إصدار برنامج تشغيل GPU:** علمًا أن العلم `ProcessingMode.GPU` سيطرح استثناءً إذا كان برنامج تشغيل CUDA مفقودًا أو غير متوافق. تحقق باستخدام `nvidia-smi`.
- **تخصيص الذاكرة:** إذا كنت تعالج العديد من الصور في وقت واحد، زد قيمة `setGpuMemoryLimit` أو نفّذ المهام بشكل متسلسل لتجنب أخطاء نفاد الذاكرة.
- **صيغة الصورة:** بينما PNG يعمل بشكل ممتاز، قد تتسبب JPEG ذات الضغط العالي في أخطاء التعرف. فكر في تحويلها إلى PNG غير مضغوط قبل OCR.
- **دعم اللغات:** افتراضيًا يفترض Aspose OCR اللغة الإنجليزية. للغات أخرى، استدعِ `config.setLanguage(Language.SPANISH)` (أو التعداد المناسب) قبل `recognize`.
- **اختبار الأداء:** نفّذ اختبارًا سريعًا (`System.nanoTime()`) مع وبدون GPU لتتحقق من أن الزيادة في السرعة تبرر التعقيد الإضافي.

---

## الأسئلة المتكررة

**هل يعمل هذا على macOS أو Linux؟**  
نعم—Aspose OCR متعدد المنصات. فقط تأكد من وجود GPU متوافق مع CUDA وبرنامج تشغيل مناسب لنظام التشغيل الخاص بك.

**ماذا لو لم أمتلك GPU؟**  
يمكنك ببساطة حذف سطر `setProcessingMode(ProcessingMode.GPU)`؛ سيعود المحرك تلقائيًا إلى وضع CPU.

**هل يمكنني معالجة ملفات PDF مباشرة؟**  
Aspose OCR يركز على الصور النقطية. بالنسبة للـ PDF، استخرج كل صفحة كصورة أولًا (مثلاً باستخدام Aspose PDF) ثم مرّر ملفات PNG إلى تدفق OCR.

---

## الخلاصة

باختصار، **how to use OCR** مع Aspose في جافا يتلخص في ثلاث خطوات واضحة: تكوين المحرك (بما في ذلك **how to set mode** و **set GPU memory limit**)، توجيهه إلى ملف PNG الخاص بك، وقراءة السلسلة النصية الناتجة. الشيفرة أعلاه هي حل كامل من البداية إلى النهاية يمكنك إدراجه في أي مشروع جافا.

الآن بعد أن أتقنت **recognize text from image** و **extract text from PNG**، يمكنك توسيع سير العمل: معالجة دفعات من المجلدات، تخزين النتائج في قاعدة بيانات، أو حتى تمرير النص إلى خطوط أنابيب NLP لاحقة. السماء هي الحد—فقط تذكّر مراقبة ذاكرة الـ GPU وتوافق برنامج التشغيل.

هل لديك أسئلة إضافية حول OCR، تسريع GPU، أو ميزات Aspose؟ لا تتردد بترك تعليق أو استكشاف وثائق Aspose OCR الرسمية لمزيد من خيارات التخصيص. برمجة سعيدة! 🚀

![how to use ocr diagram](https://example.com/images/ocr-gpu-diagram.png "how to use ocr diagram")

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}