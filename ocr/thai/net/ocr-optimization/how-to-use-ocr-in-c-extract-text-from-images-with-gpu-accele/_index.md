---
category: general
date: 2025-12-29
description: วิธีใช้ OCR ใน C# เพื่อดึงข้อความจากภาพ แสดงจำนวนอักขระ และเพิ่มประสิทธิภาพด้วยการเร่งความเร็วด้วย
  GPU โดยใช้ Aspose OCR.
draft: false
keywords:
- how to use OCR
- extract text image
- display character count
- gpu acceleration ocr
- c# ocr aspose
language: th
og_description: วิธีใช้ OCR ใน C# เพื่อดึงข้อความจากภาพ, แสดงจำนวนอักขระ, และเร่งการประมวลผลด้วย
  GPU โดยใช้ Aspose OCR.
og_title: วิธีใช้ OCR ใน C# – การสกัดข้อความอย่างรวดเร็วด้วย GPU
tags:
- OCR
- C#
- Aspose
- GPU
title: วิธีใช้ OCR ใน C# – แยกข้อความจากภาพด้วยการเร่งความเร็วด้วย GPU
url: /th/net/ocr-optimization/how-to-use-ocr-in-c-extract-text-from-images-with-gpu-accele/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# วิธีใช้ OCR ใน C# – คู่มือครบวงจร

เคยสงสัย **วิธีใช้ OCR** ในโปรเจกต์ .NET โดยไม่ต้องเขียนโค้ดหลายพันบรรทัดหรือไม่? บางทีคุณอาจสแกนไฟล์ TIFF ขนาดใหญ่และต้องการข้อความอย่างรวดเร็ว, หรือแค่ต้องการนับอักขระสำหรับแดชบอร์ดรายงาน ไม่ว่าคุณจะมาที่นี่เพื่ออะไร เราก็พร้อมช่วยคุณ ในบทเรียนนี้เราจะอธิบายขั้นตอนการดึงข้อความจากภาพ, แสดงจำนวนอักขระ, และเร่งความเร็วกระบวนการด้วย **GPU acceleration OCR** – ทั้งหมดนี้ด้วยไลบรารี **C# Aspose OCR**.

เรายังจะพูดถึงหัวข้อรองที่คุณอาจกำลังมองหา: **extract text image**, **display character count**, และเทคนิค **c# ocr aspose**. เมื่อจบคุณจะได้แอปคอนโซลที่พร้อมรันและสามารถประมวลผลสแกนขนาดใหญ่ได้ในพริบตา.

---

## สิ่งที่คุณจะได้เรียนรู้

- ตั้งค่า Aspose OCR ในโปรเจกต์ C# (ไม่มีความลับของ NuGet)
- เปิดใช้งาน **GPU acceleration OCR** สำหรับไฟล์ขนาดใหญ่
- โหลดภาพและ **extract text from the image**
- **Display character count** พร้อมเวลาในการประมวลผล
- จัดการกับปัญหาทั่วไป เช่น ไดรเวอร์ GPU หายหรือรูปแบบภาพที่ไม่รองรับ

> **ข้อกำหนดเบื้องต้น:** .NET 6+ (หรือ .NET Framework 4.7.2) และ GPU ที่รองรับ หากไม่มี GPU โค้ดจะสลับไปใช้โหมด CPU อย่างอ่อนโยน

---

![วิธีใช้ OCR พร้อมการเร่งความเร็วด้วย GPU ใน C#](ocr-gpu.png "ตัวอย่างการใช้ OCR แสดงการใช้ GPU")

*ข้อความแทนภาพ: ภาพประกอบการใช้ OCR พร้อมการเร่งความเร็วด้วย GPU*

---

## ขั้นตอนที่ 1: ติดตั้ง Aspose OCR และเตรียมโปรเจกต์

### ทำไมจึงสำคัญ

ก่อนที่คุณจะ **ใช้ OCR** ได้ ไลบรารีต้องถูกอ้างอิง Aspose OCR มาพร้อมกับแพ็กเกจ NuGet เดียวที่บรรจุไบนารีเนทีฟสำหรับ CPU และ GPU ทำให้คุณไม่ต้องตามหา DLL ด้วยตนเอง

```csharp
// In your terminal or Package Manager Console
dotnet add package Aspose.OCR
```

> **เคล็ดลับ:** หากคุณใช้ .NET Framework ให้ใช้ NuGet UI ใน Visual Studio เพื่อหลีกเลี่ยงปัญหาเวอร์ชัน

### โครงสร้างโปรเจกต์เต็ม

สร้างแอปคอนโซลใหม่และวางโค้ด `Program.cs` ด้านล่างนี้ ซึ่งรวม `using` ที่จำเป็นทั้งหมดไว้แล้ว คุณจึงไม่ต้องเดาว่าต้องนำเข้าอะไร

```csharp
using System;
using Aspose.OCR;
using Aspose.OCR.Gpu;
using Aspose.OCR.ImageProcessing; // optional, for advanced pre‑processing

namespace AsposeOcrDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // Call the helper that does the heavy lifting
            RunOcr(@"YOUR_DIRECTORY/large_scanned_page.tif");
        }

        static void RunOcr(string imagePath)
        {
            // Step 2: Create and configure the OCR engine (see next section)
        }
    }
}
```

บันทึกไฟล์, รีสโตร์แพ็กเกจ, แล้วคุณพร้อมสำหรับขั้นตอนต่อไป

---

## ขั้นตอนที่ 2: วิธีใช้ OCR Engine พร้อมการเร่งความเร็วด้วย GPU

### ทำไมต้องเปิด GPU?

การประมวลผล TIFF ขนาดหลายเมกะพิกเซลบน CPU อาจใช้เวลาหลายนาที GPU acceleration OCR จะย้ายการทำงานระดับพิกเซลไปที่การ์ดจอ ทำให้เวลาเหลือเพียงเศษส่วนของเดิม

```csharp
static void RunOcr(string imagePath)
{
    // Create an OCR engine instance
    var ocrEngine = new OcrEngine();

    // Enable GPU acceleration – if a compatible device is found
    ocrEngine.UseGpu = true;
    ocrEngine.GpuDeviceId = 0; // 0 = first GPU; change if you have multiple

    // Optional sanity check – fall back to CPU if GPU init fails
    try
    {
        // This call forces the engine to initialize GPU resources
        ocrEngine.InitializeGpu();
        Console.WriteLine("✅ GPU acceleration enabled.");
    }
    catch (Exception ex)
    {
        Console.WriteLine($"⚠️ GPU init failed ({ex.Message}), switching to CPU.");
        ocrEngine.UseGpu = false;
    }

    // Load the image (this also validates format)
    var inputImage = Image.Load(imagePath);
    
    // Perform OCR – the heavy lifting happens here
    var ocrResult = ocrEngine.Recognize(inputImage);

    // Step 3: Display results (character count & processing time)
    DisplayResult(ocrResult);
}
```

> **เหตุผลที่ทำงานได้:** `UseGpu` สลับ pipeline ภายใน `InitializeGpu()` ทำการตรวจสอบล่วงหน้าเพื่อให้คุณจับปัญหาไดรเวอร์ก่อนเรียก `Recognize` ที่ใช้เวลานาน

---

## ขั้นตอนที่ 3: ดึงข้อความจากภาพและแสดงจำนวนอักขระ

เมื่อเอนจินพร้อมทำงานแล้ว ให้ **extract text from the image** และแสดงจำนวนอักขระที่ได้รับการจดจำ นี่เป็นส่วนที่นักพัฒนามักมองข้าม แต่สำคัญต่อการตรวจสอบและการวิเคราะห์ต่อไป

```csharp
static void DisplayResult(OcrResult ocrResult)
{
    // The raw OCR text
    string extractedText = ocrResult.Text;

    // Character count – includes spaces and line breaks
    int charCount = extractedText.Length;

    // Processing time in milliseconds (provided by Aspose)
    long processingMs = ocrResult.ProcessingTime;

    // Output to console – easy to pipe to a file or logger
    Console.WriteLine($"🖋️ Extracted {charCount} characters in {processingMs} ms");
    Console.WriteLine("----- Begin OCR Text -----");
    Console.WriteLine(extractedText);
    Console.WriteLine("------ End OCR Text ------");
}
```

**ผลลัพธ์ที่คาดหวัง** (ตัวอย่างสำหรับสแกน 2 หน้า):

```
✅ GPU acceleration enabled.
🖋️ Extracted 12,345 characters in 842 ms
----- Begin OCR Text -----
Lorem ipsum dolor sit amet, consectetur...
... (rest of the page) ...
------ End OCR Text ------
```

หาก GPU ไม่พร้อมใช้งาน คุณจะเห็นคำเตือนและผลลัพธ์เดียวกัน แต่จะช้ากว่า

---

## ขั้นตอนที่ 4: การจัดการไฟล์ขนาดใหญ่และกรณีขอบ

### ถ้าภาพมีขนาดใหญ่มากล่ะ?

Aspose OCR สามารถสตรีมหน้าได้ แต่คุณยังต้องมี RAM เพียงพอ วิธีที่ดีคือทำการลด DPI ที่ไม่จำเป็นก่อนทำการจดจำ

```csharp
// Optional pre‑processing: downscale to 300 DPI if original > 600 DPI
if (inputImage.DpiX > 600 || inputImage.DpiY > 600)
{
    inputImage = inputImage.Resize(0.5, 0.5); // 50% reduction
    Console.WriteLine("🔎 Image downscaled for faster OCR.");
}
```

### ไดรเวอร์ GPU หายไป?

`try/catch` รอบ `InitializeGpu()` จะจับปัญหาส่วนใหญ่แล้ว แต่คุณยังสามารถสอบถามอุปกรณ์ที่มีอยู่ได้

```csharp
var gpuInfo = GpuDeviceManager.GetDevices();
if (gpuInfo.Count == 0)
{
    Console.WriteLine("⚡ No GPU detected – defaulting to CPU.");
    ocrEngine.UseGpu = false;
}
```

### รูปแบบภาพที่ไม่รองรับ?

Aspose รองรับ TIFF, PNG, JPEG, BMP และรูปแบบแปลกอื่น ๆ หากคุณเจอ `UnsupportedFormatException` ให้แปลงไฟล์ก่อนด้วยเครื่องมืออย่าง ImageMagick หรือเมธอด `Image.Save` ในตัวเพื่อบันทึกเป็น PNG

---

## ขั้นตอนที่ 5: สรุป – ตัวอย่างทำงานเต็มรูปแบบ

คัดลอกและวางโปรแกรมทั้งหมดด้านล่างนี้ลงใน `Program.cs` เป็นเดโมที่ทำงานได้ทันที (เพียงเปลี่ยนเส้นทางไฟล์)

```csharp
using System;
using Aspose.OCR;
using Aspose.OCR.Gpu;
using Aspose.OCR.ImageProcessing;

namespace AsposeOcrDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // Adjust the path to point at your scanned TIFF or JPEG
            RunOcr(@"YOUR_DIRECTORY/large_scanned_page.tif");
        }

        static void RunOcr(string imagePath)
        {
            var ocrEngine = new OcrEngine
            {
                UseGpu = true,
                GpuDeviceId = 0
            };

            try
            {
                ocrEngine.InitializeGpu();
                Console.WriteLine("✅ GPU acceleration enabled.");
            }
            catch (Exception ex)
            {
                Console.WriteLine($"⚠️ GPU init failed ({ex.Message}), switching to CPU.");
                ocrEngine.UseGpu = false;
            }

            var inputImage = Image.Load(imagePath);

            // Optional downscale for gigantic files
            if (inputImage.DpiX > 600 || inputImage.DpiY > 600)
            {
                inputImage = inputImage.Resize(0.5, 0.5);
                Console.WriteLine("🔎 Image downscaled for faster OCR.");
            }

            var ocrResult = ocrEngine.Recognize(inputImage);
            DisplayResult(ocrResult);
        }

        static void DisplayResult(OcrResult ocrResult)
        {
            string extractedText = ocrResult.Text;
            int charCount = extractedText.Length;
            long processingMs = ocrResult.ProcessingTime;

            Console.WriteLine($"🖋️ Extracted {charCount} characters in {processingMs} ms");
            Console.WriteLine("----- Begin OCR Text -----");
            Console.WriteLine(extractedText);
            Console.WriteLine("------ End OCR Text ------");
        }
    }
}
```

รันด้วย `dotnet run` แล้วดูคอนโซลแสดง **character count** และข้อความ OCR นั่นคือวงจร **วิธีใช้ OCR** ตั้งแต่ต้นจนจบ

---

## สรุป

เราได้อธิบาย **วิธีใช้ OCR** ใน C# เพื่อ **extract text from images**, **display character count**, และเร่งความเร็วด้วย **GPU acceleration OCR** ผ่านไลบรารี **c# ocr aspose** สิ่งที่ควรจำ:

1. ติดตั้ง Aspose OCR ผ่าน NuGet และอ้างอิง namespace ที่ถูกต้อง  
2. เปิดใช้งาน GPU แต่ต้องมี fallback ไป CPU เสมอ  
3. โหลดภาพ, ปรับขนาดถ้าจำเป็น, แล้วเรียก `Recognize`  
4. ดึง `ocrResult.Text` และ `ocrResult.ProcessingTime` เพื่อ **display character count** และเมตริกประสิทธิภาพ  

จากนี้คุณสามารถต่อยอดได้—บันทึกข้อความลงฐานข้อมูล, ส่งต่อไปยังดัชนีการค้นหา, หรือทำการตรวจจับภาษาในสตริงที่ดึงมา หากต้องประมวลผล PDF ให้แปลงแต่ละหน้าเป็นภาพ; โค้ดเดียวกันทำงานได้

**ขั้นตอนต่อไป** ที่คุณอาจสนใจ:

- ใช้ **extract text image** จาก PDF หลายหน้าโดยใช้ `PdfConverter`  
- ปรับตั้งค่า OCR (แพ็คเกจภาษา, การลดนอยส์) เพื่อความแม่นยำที่ดีกว่า  
- ขยายโซลูชันใน Azure Functions หรือ AWS Lambda ด้วยอินสแตนซ์ที่เปิดใช้งาน GPU  

ลองทำ, ทดสอบ, แล้วปรับปรุงต่อไป นั่นแหละคือวิธีที่โครงการ OCR ในโลกจริงถูกสร้างขึ้น ขอให้เขียนโค้ดสนุกและสแกนของคุณอ่านได้เสมอ!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}