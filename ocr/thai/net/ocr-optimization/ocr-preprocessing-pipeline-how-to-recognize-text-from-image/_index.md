---
category: general
date: 2026-01-02
description: เรียนรู้การสร้าง pipeline การเตรียมข้อมูลล่วงหน้าสำหรับ OCR ที่ทำการแก้ไขการเอียงของภาพโดยอัตโนมัติ,
  เตรียมภาพสำหรับ OCR และอ่านข้อความจากไฟล์ JPG ด้วย Aspose.OCR – คู่มือแบบทีละขั้นตอน.
draft: false
keywords:
- ocr preprocessing pipeline
- recognize text from image
- auto deskew image
- preprocess image for ocr
- read text from jpg
language: th
og_description: ค้นพบกระบวนการเตรียมข้อมูล OCR ที่ทำการแก้ไขการเอียงของภาพโดยอัตโนมัติและให้คุณสามารถจดจำข้อความจากไฟล์ภาพเช่น
  JPG ได้ โค้ดเต็ม คำอธิบาย และเคล็ดลับ
og_title: กระบวนการเตรียมข้อมูล OCR – คู่มือ C# ฉบับสมบูรณ์
tags:
- OCR
- C#
- Image Processing
title: กระบวนการเตรียมข้อมูล OCR – วิธีการจดจำข้อความจากภาพใน C#
url: /th/net/ocr-optimization/ocr-preprocessing-pipeline-how-to-recognize-text-from-image/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# การประมวลผลล่วงหน้าของ OCR – คู่มือ C# ฉบับสมบูรณ์

เคยประสบปัญหาในการ **recognize text from image** ไฟล์ที่เอียง, มีเสียงรบกวน, หรืออ่านยากไหม? คุณไม่ได้อยู่คนเดียว ในหลายโครงการจริง ๆ ภาพดิบที่ได้จากสแกนเนอร์หรือกล้องโทรศัพท์ต้องการการดูแลเล็กน้อยก่อนที่เครื่อง OCR จะทำงานได้  

นั่นคือจุดที่ **ocr preprocessing pipeline** เข้ามาช่วย โดยการปรับแนวอัตโนมัติ (auto‑deskew) ของภาพ, ลดจุดสัญญาณรบกวนพื้นหลัง, และทำความสะอาดภาพโดยรวม คุณจะเพิ่มความแม่นยำอย่างมาก ในบทแนะนำนี้เราจะเดินผ่านตัวอย่างที่ทำงานได้เต็มรูปแบบที่ **preprocesses image for OCR**, ปรับแนวอัตโนมัติของภาพ, และสุดท้าย **reads text from jpg** ด้วย Aspose.OCR

> **สิ่งที่คุณจะได้เรียนรู้:** แอปคอนโซล C# ที่พร้อมรันซึ่งโหลด JPG ที่เอียงและมีเสียงรบกวน, ผ่านกระบวนการประมวลผลล่วงหน้าที่ชาญฉลาด, แล้วพิมพ์ข้อความที่สกัดออกมาในคอนโซล

## Prerequisites

- .NET 6 SDK หรือใหม่กว่า (โค้ดนี้ยังคอมไพล์ได้กับ .NET Core ด้วย)
- Visual Studio 2022 หรือ IDE ใดก็ได้ที่คุณชอบ
- Aspose.OCR NuGet package (`Install-Package Aspose.OCR`)
- ตัวอย่างรูปภาพเช่น `skewed_noisy.jpg` ที่วางไว้ในโฟลเดอร์ที่คุณอ้างอิงได้

ไม่มีไลบรารีภายนอกอื่นที่จำเป็น; ทุกอย่างที่เหลืออยู่ภายใน Aspose.OCR

---

## Step 1 – Set Up the Project and Load Your Image

เริ่มต้นโดยสร้างโปรเจกต์คอนโซลใหม่และเพิ่มการอ้างอิง Aspose.OCR จากนั้นโหลดภาพที่คุณต้องการประมวลผล

```csharp
using Aspose.OCR;
using System.Drawing;

class PreprocessExample
{
    static void Main()
    {
        // Load the image that needs OCR
        var imagePath = "YOUR_DIRECTORY/skewed_noisy.jpg";
        var image = new Bitmap(imagePath);
```

> **ทำไมจึงสำคัญ:** คลาส `Bitmap` ให้เราเข้าถึงพิกเซลโดยตรง ซึ่งเครื่อง OCR ต้องการสำหรับขั้นตอนการประมวลผลล่วงหน้า หากเส้นทางไฟล์ผิดคุณจะได้รับ `FileNotFoundException` ดังนั้นตรวจสอบตำแหน่งไฟล์ให้แน่ใจ

---

## Step 2 – Create the OCR Engine Instance

ต่อไปให้สร้างอินสแตนซ์ของ `OcrEngine` ซึ่งอ็อบเจกต์นี้จะเป็นหัวใจของ **ocr preprocessing pipeline** ทั้งหมด

```csharp
        // Create the OCR engine instance
        var ocrEngine = new OcrEngine();
```

> **เคล็ดลับ:** คุณสามารถใช้ `OcrEngine` เดียวกันสำหรับหลายภาพ; เพียงรีเซ็ต `RecognitionOptions` ทุกครั้งที่ใช้งาน

---

## Step 3 – Configure the Preprocess Settings (The Core of the Pipeline)

ที่นี่เราจะเปิดใช้สองฟีเจอร์ที่ทรงพลังที่สุด: **auto deskew image** และ **noise reduction** ทั้งสองเป็นส่วนของ pipeline ที่เตรียมภาพให้พร้อมสำหรับการสกัดข้อความที่แม่นยำ

```csharp
        // Configure recognition options with the new preprocessing pipeline
        var recognitionOptions = new RecognitionOptions
        {
            Preprocess = new PreprocessSettings
            {
                EnableSmartDeskew = true,          // Auto‑detect and correct rotation
                EnableNoiseReduction = true,      // Apply AI‑based denoising
                NoiseReductionLevel = NoiseLevel.Medium
            },
            Language = Language.English
        };
```

> **วิธีการทำงาน:**  
> - `EnableSmartDeskew` ตรวจสอบมุมฐานของภาพและหมุนกลับไปที่ 0° ซึ่งสำคัญมากสำหรับสแกนที่เอียง  
> - `EnableNoiseReduction` ใช้ฟิลเตอร์ AI ขนาดเบาที่ลบจุดสัญญาณรบกวนโดยไม่ทำลายอักขระที่บอบบาง  
> - `NoiseReductionLevel` ให้คุณเลือกความสมดุลระหว่างความเร็วและคุณภาพ; `Medium` เป็นค่าที่เหมาะสมสำหรับ JPG ส่วนใหญ่

---

## Step 4 – Run the OCR and Capture the Result

ตอนนี้เราจะส่งภาพและตัวเลือกไปยังเอนจิน วิธีนี้จะคืนค่าอ็อบเจกต์ `OcrResult` ที่บรรจุสตริงที่สกัดออกมาและคะแนนความเชื่อมั่น

```csharp
        // Perform OCR on the image using the configured options
        var ocrResult = ocrEngine.Recognize(image, recognitionOptions);
```

> **กรณีขอบ:** หากภาพเป็นค่าว่างเปล่า `ocrResult.Text` จะเป็นสตริงว่าง คุณอาจต้องตรวจสอบ `ocrResult.HasText` ก่อนดำเนินการต่อในโค้ดการผลิต

---

## Step 5 – Output the Recognized Text

สุดท้ายพิมพ์ผลลัพธ์ไปยังคอนโซล ซึ่งแสดงให้เห็นว่าเราสามารถ **recognize text from image** ไฟล์ได้ด้วยเพียงไม่กี่บรรทัดของโค้ด

```csharp
        // Output the recognized text
        System.Console.WriteLine("=== Extracted Text ===");
        System.Console.WriteLine(ocrResult.Text);
    }
}
```

**ผลลัพธ์ที่คาดหวัง (ตัวอย่าง):**

```
=== Extracted Text ===
Invoice #12345
Date: 01/01/2024
Total: $1,250.00
Thank you for your business!
```

หากภาพมีสัญญาณรบกวนหรือหมุนผิดมาก คุณจะเห็นอักขระแปลก ๆ ขึ้น แต่ด้วย **ocr preprocessing pipeline** ปัญหาเหล่านั้นจะลดลงอย่างมาก

---

## Step 6 – Full Working Example (Copy‑Paste Ready)

ด้านล่างเป็นไฟล์ซอร์สเต็มรูปแบบพร้อมคอมไพล์ แทนที่ `YOUR_DIRECTORY` ด้วยพาธจริงของ JPG ของคุณ

```csharp
using Aspose.OCR;
using System.Drawing;

class PreprocessExample
{
    static void Main()
    {
        // 1️⃣ Load the image that needs OCR
        var imagePath = "YOUR_DIRECTORY/skewed_noisy.jpg";
        var image = new Bitmap(imagePath);

        // 2️⃣ Create the OCR engine instance
        var ocrEngine = new OcrEngine();

        // 3️⃣ Configure the preprocessing pipeline (auto deskew + noise reduction)
        var recognitionOptions = new RecognitionOptions
        {
            Preprocess = new PreprocessSettings
            {
                EnableSmartDeskew = true,          // Auto‑detect and correct rotation
                EnableNoiseReduction = true,      // AI‑based denoising
                NoiseReductionLevel = NoiseLevel.Medium
            },
            Language = Language.English
        };

        // 4️⃣ Run OCR with the configured pipeline
        var ocrResult = ocrEngine.Recognize(image, recognitionOptions);

        // 5️⃣ Print the extracted text
        System.Console.WriteLine("=== Extracted Text ===");
        System.Console.WriteLine(ocrResult.Text);
    }
}
```

บันทึกไฟล์นี้เป็น `Program.cs`, รัน `dotnet run`, แล้วดูข้อความที่ทำความสะอาดแล้วแสดงในคอนโซล

---

## Step 7 – Going Further – Tweaking the Pipeline

**ocr preprocessing pipeline** มีความยืดหยุ่น นี่คือตัวอย่างการปรับเปลี่ยนที่พบบ่อย:

| การเปลี่ยนแปลง | เมื่อใช้ | โค้ดตัวอย่าง |
|----------------|----------|--------------|
| **การลดสัญญาณรบกวนสูงขึ้น** (เช่น `NoiseLevel.High`) | สแกนที่มีเม็ดฝุ่นมากจากกล้องความละเอียดต่ำ | `NoiseReductionLevel = NoiseLevel.High` |
| **ปิดการปรับแนวอัตโนมัติ** | ภาพที่จัดแนวได้สมบูรณ์แล้ว | `EnableSmartDeskew = false` |
| **รองรับหลายภาษา** | เอกสารมีทั้งภาษาอังกฤษและสเปน | `Language = Language.English | Language.Spanish` |
| **ปรับสเกล DPI เอง** | ตัวอักษรเล็กมากต้องการอัปสแคล | `recognitionOptions.Dpi = 300;` |

การทดลองกับการตั้งค่าเหล่านี้จะช่วยให้คุณปรับขั้นตอน **preprocess image for OCR** ให้ตรงกับลักษณะเฉพาะของชุดข้อมูลของคุณ

---

## Conclusion

เราได้สร้าง **ocr preprocessing pipeline** ใน C# ที่ **auto deskews image**, ลดสัญญาณรบกวน, และสุดท้าย **recognize text from image** เช่น JPGs โดยการกำหนด `PreprocessSettings` ภายใน `RecognitionOptions` ของ Aspose.OCR เราได้เปลี่ยนภาพที่เอียงและมีจุดรบกวนให้กลายเป็นข้อความที่ค้นหาได้อย่างสะอาดด้วยเพียงไม่กี่บรรทัด

> **ประเด็นสำคัญ:**  
> - ทำความสะอาดภาพก่อนเสมอ – เครื่อง OCR ทำงานได้ดีที่สุดกับอินพุตที่ตรงและมีสัญญาณรบกวนน้อย  
> - pipeline สามารถกำหนดค่าได้เต็มที่; ปรับการปรับแนวและการลดสัญญาณรบกวนตามความต้องการของคุณ  
> - รูปแบบเดียวกันนี้ทำงานได้กับ PDF, TIFF หรือแหล่งบิตแมปใด ๆ ที่คุณส่งเข้า Aspose.OCR

พร้อมก้าวต่อไปหรือยัง? ลองประมวลผลไฟล์หลายไฟล์พร้อมกันผ่าน pipeline, หรือผสานโค้ดนี้เข้ากับ Web API เพื่อให้ผู้ใช้อัปโหลดรูปและรับข้อความทันที คุณยังสามารถสำรวจฟีเจอร์การแปลงเอกสารของ Aspose เพื่อแปลงข้อความที่สกัดเป็น PDF ที่ค้นหาได้

ขอให้เขียนโค้ดสนุกและผลลัพธ์ OCR ของคุณแม่นยำเสมอ! 🚀

---

![Diagram of an ocr preprocessing pipeline showing steps: load image → smart deskew → noise reduction → OCR → output text](ocr-preprocessing-pipeline.png "แผนภาพกระบวนการประมวลผลล่วงหน้าของ OCR แสดงขั้นตอน: โหลดภาพ → ปรับแนวอัจฉริยะ → ลดสัญญาณรบกวน → OCR → แสดงข้อความ")

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}