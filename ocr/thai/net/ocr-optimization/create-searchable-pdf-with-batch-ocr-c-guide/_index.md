---
category: general
date: 2025-12-29
description: สร้างไฟล์ PDF ที่ค้นหาได้จากภาพสแกนโดยใช้การประมวลผลแบบแบตช์ของ Aspose
  OCR เรียนรู้วิธีแปลงภาพเป็น PDF, เตรียมภาพล่วงหน้าสำหรับ OCR, และแก้ไขการเอียงของเอกสารสแกน.
draft: false
keywords:
- create searchable pdf
- batch ocr processing
- convert images to pdf
- preprocess images for ocr
- deskew scanned documents
language: th
og_description: สร้างไฟล์ PDF ที่ค้นหาได้จากภาพสแกนโดยใช้การประมวลผลแบบแบตช์ของ Aspose
  OCR เรียนรู้วิธีแปลงภาพเป็น PDF, เตรียมภาพล่วงหน้าสำหรับ OCR, และแก้ไขการเอียงของเอกสารสแกน
og_title: สร้าง PDF ที่ค้นหาได้ด้วย OCR แบบกลุ่ม – คู่มือ C#
tags:
- OCR
- C#
- PDF/A
- Aspose
title: สร้าง PDF ที่ค้นหาได้ด้วย OCR แบบกลุ่ม – คู่มือ C#
url: /th/net/ocr-optimization/create-searchable-pdf-with-batch-ocr-c-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# สร้าง PDF ที่ค้นหาได้ด้วยการทำ OCR เป็นชุด – คู่มือ C#

เคยต้อง **สร้างไฟล์ PDF ที่ค้นหาได้** จากภาพสแกนจำนวนมากแต่รู้สึกติดขัดตั้งแต่ขั้นตอนแรกหรือไม่? คุณไม่ได้อยู่คนเดียว—นักพัฒนาส่วนใหญ่เจออุปสรรคเดียวกันเมื่อต้องจัดการกับสแกนที่รก หน้าไม่เรียบ หรือการแปลงเป็นชุดจำนวนมาก  

ข่าวดีคือ? ด้วย Aspose OCR คุณสามารถสร้าง **pipeline การทำ OCR เป็นชุด** ที่ไม่เพียงแต่ **แปลงภาพเป็น pdf** แต่ยัง **เตรียมภาพก่อน OCR** และแม้กระทั่ง **แก้ไขการเอียงของเอกสารสแกน** อัตโนมัติ ในบทแนะนำนี้เราจะเดินผ่านทุกขั้นตอน ตั้งแต่การตั้งค่า engine จนถึงการปรับแต่งผลลัพธ์ เพื่อให้คุณสามารถรันบนโฟลเดอร์ของไฟล์และได้ PDF/A‑2b ที่ค้นหาได้เป็นผลลัพธ์

> **สิ่งที่คุณจะได้:** แอปคอนโซล C# ที่ทำงานได้ครบชุด รับโฟลเดอร์ของภาพ (หรือ PDF) ทำความสะอาดแต่ละหน้า รัน OCR แล้วสร้างไฟล์ PDF/A‑2b ที่ค้นหาได้ไว้ข้างไฟล์ต้นฉบับ ไม่มีโค้ดสั้น ๆ แยกกัน เพียงโซลูชันเดียวที่ต่อเนื่อง

---

## ข้อกำหนดเบื้องต้น

- .NET 6 SDK หรือใหม่กว่า (โค้ดนี้ยังคอมไพล์ได้กับ .NET Core)  
- แพคเกจ NuGet Aspose OCR (`Aspose.OCR`)  
- โฟลเดอร์ของภาพสแกน (TIFF, JPEG, PNG) หรือ PDF ที่ต้องการแปลงเป็น PDF ที่ค้นหาได้  
- (ออปชัน) คีย์ไลเซนส์จริง—หากไม่มีโหมดทดลองจะใส่ลายน้ำ แต่ก็ใช้ทดสอบได้  

ถ้าคุณมีทั้งหมดนี้แล้ว ไปต่อกันเลย

---

## ภาพรวม – วิธีที่ pipeline ทั้งหมดสร้าง PDF ที่ค้นหาได้

1. **เปิดใช้งานโหมดทดลอง** (หรือโหลดไลเซนส์ของคุณ)  
2. **กำหนดค่า `OcrBatchProcessor`** – ระบุที่มาของไฟล์ ที่จะบันทึก PDF รูปแบบที่ใช้ และจำนวนเธรดที่รันพร้อมกัน  
3. **เตรียมภาพแต่ละไฟล์** – แก้ไขการเอียง, กำจัดสัญญาณรบกวน, และลบพื้นหลังเพื่อให้ engine OCR เห็นหน้าเพียว ๆ  
4. **รัน batch** – Aspose ประมวลผลทุกไฟล์, ทำ OCR, แล้วบันทึกเป็น PDF/A‑2b ที่ค้นหาได้  
5. **แจ้งการเสร็จสิ้น** – ข้อความคอนโซลง่าย ๆ แต่คุณสามารถต่อ logger หรือ webhook ได้  

นี่คือการไหลของขั้นตอนระดับสูง โค้ดด้านล่างทำตามแต่ละขั้นตอนพร้อมคอมเมนต์มากมาย เพื่อให้คุณปรับแต่งส่วนใดส่วนหนึ่งได้โดยไม่ทำให้ระบบทั้งหมดพัง

---

## ขั้นตอนที่ 1 – เปิดใช้งานโหมดทดลอง (หรือโหลดไลเซนส์)

ก่อนที่คุณจะเรียกใช้คลาสของ Aspose ใด ๆ คุณต้องบอกไลบรารีว่าคุณมีไลเซนส์ สำหรับการทดลองอย่างรวดเร็ว โหมดทดลองก็เพียงพอ

```csharp
using Aspose.OCR;
using Aspose.OCR.Models;
using Aspose.OCR.Filters;
using Aspose.OCR.Batch;

// Activate trial mode – replace with OcrEngine.SetLicense("YourLicenseFile.lic") for production
OcrEngine.EnableTrialMode();
```

> **เคล็ดลับระดับมืออาชีพ:** วางการเปิดใช้งานไลเซนส์ไว้ที่ส่วนบนสุดของ `Program.cs` หากลืม engine จะโยน exception ครั้งแรกที่คุณเรียก `Process()`

---

## ขั้นตอนที่ 2 – กำหนดค่า engine การทำ OCR เป็นชุด

ตรงนี้เราตั้งค่าอ็อบเจ็กต์ **batch OCR processing** ดูที่ `InputFolder` และ `OutputFolder` ที่ตัวอย่างนี้เหมือนกัน แต่คุณสามารถแยกออกได้หากต้องการ

```csharp
// Define where your source images live and where the searchable PDFs should be saved
var ocrBatch = new OcrBatchProcessor
{
    // Folder that contains the images or PDFs to be processed
    InputFolder = @"C:\Scans\Incoming",

    // Folder where searchable PDF/A‑2b files will be saved
    OutputFolder = @"C:\Scans\Processed",

    // Choose the output format – searchable PDF/A‑2b (perfect for archiving)
    OutputFormat = SaveFormat.SearchablePdf,

    // Limit the number of concurrent OCR operations to avoid CPU spikes
    MaxDegreeOfParallelism = 3,

    // Pre‑process each image: deskew, denoise, and remove background
    Preprocess = img => ImageFilters
                            .Deskew(img)          // fixes rotated pages
                            .Denoise()            // reduces speckles
                            .RemoveBackground()   // clears colored backgrounds
};
```

### ทำไมการตั้งค่าเหล่านี้ถึงสำคัญ

- **`MaxDegreeOfParallelism`**: การรัน OCR thread มากเกินไปอาจทำให้ CPU ทำงานเต็มที่ โดยเฉพาะบนเครื่องทำงานระดับกลาง การใช้สามเธรดเป็นจุดที่เหมาะสมสำหรับแล็ปท็อปคอร์ด 4‑คอร์ส่วนใหญ่  
- **pipeline `Preprocess`**: ตัวกรองสามตัวนี้ช่วยเพิ่มความแม่นยำของ OCR อย่างมาก Deskew แก้ไขปัญหา “สแกนเอียง” ปกติ, denoise กำจัดสัญญาณรบกวนสุ่ม, และ background removal ทำให้ engine เห็นเฉพาะข้อความสีดำบนพื้นขาว  
- **`SaveFormat.SearchablePdf`**: สร้างไฟล์ PDF/A‑2b ที่พร้อมเก็บรักษาและค้นหาได้—เป็นข้อกำหนดของมาตรฐานหลาย ๆ อย่าง

---

## ขั้นตอนที่ 3 – เรียกใช้ batch และดูผลลัพธ์มหัศจรรย์

การรัน batch ง่ายแค่เรียก `Process()` เมธอดนี้จะบล็อกจนกว่าทุกไฟล์จะเสร็จ แล้วคืนค่า หากต้องการแสดงความคืบหน้า คุณสามารถเชื่อมต่อกับอีเวนต์ `ProgressChanged` (ไม่ได้แสดงในที่นี้)

```csharp
// Start processing – this will walk through every file in InputFolder
ocrBatch.Process();

// Let the user (or calling script) know we’re finished
Console.WriteLine("All files processed. Searchable PDFs are ready.");
```

เมื่อคอนโซลพิมพ์บรรทัดสุดท้าย คุณจะพบ PDF ที่ค้นหาได้สำหรับแต่ละภาพต้นฉบับใน `C:\Scans\Processed` เปิดไฟล์ใดก็ได้ใน Adobe Reader กด **Ctrl+F** แล้วคุณจะสามารถค้นหาข้อความที่เพิ่งถูกดึงจากสแกนได้

---

## ขั้นตอนที่ 4 – โปรแกรมเต็มที่พร้อมรัน (คัดลอก‑วางได้เลย)

ด้านล่างเป็นโปรแกรม **ครบวงจร** ที่คุณสามารถวางลงในโปรเจกต์คอนโซลใหม่ (`dotnet new console`) อย่าลืมเพิ่มแพคเกจ Aspose.OCR ก่อน (`dotnet add package Aspose.OCR`)

```csharp
// Program.cs
using Aspose.OCR;
using Aspose.OCR.Models;
using Aspose.OCR.Filters;
using Aspose.OCR.Batch;

namespace CreateSearchablePdfDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // 1️⃣ Activate trial mode (replace with real license for production)
            OcrEngine.EnableTrialMode();

            // 2️⃣ Configure batch OCR processing
            var ocrBatch = new OcrBatchProcessor
            {
                InputFolder = @"C:\Scans\Incoming",   // 👉 change to your source folder
                OutputFolder = @"C:\Scans\Processed", // 👉 change to your target folder
                OutputFormat = SaveFormat.SearchablePdf,
                MaxDegreeOfParallelism = 3,
                Preprocess = img => ImageFilters
                                        .Deskew(img)          // fixes rotated pages
                                        .Denoise()            // cleans up noise
                                        .RemoveBackground()   // strips colored backgrounds
            };

            // 3️⃣ Run the batch
            ocrBatch.Process();

            // 4️⃣ Notify completion
            Console.WriteLine("All files processed. Searchable PDFs are ready.");
        }
    }
}
```

### ผลลัพธ์ที่คาดหวัง

```
All files processed. Searchable PDFs are ready.
```

หลังจากรันเสร็จ ไปที่ `C:\Scans\Processed` คุณจะเห็นไฟล์ `.pdf` ชุดหนึ่ง—แต่ละไฟล์สามารถค้นหาได้และเป็น PDF/A‑2b compliant เปิดไฟล์ใดก็ได้ พิมพ์คำที่คุณรู้ว่ามีในสแกนต้นฉบับ แล้ว **ว้าว** ข้อความจะถูกไฮไลท์

---

## คำถามทั่วไป & การจัดการกรณีขอบ

### ถ้าโฟลเดอร์ต้นฉบับของฉันมี PDF อยู่แล้วจะทำอย่างไร?

Aspose OCR สามารถรับ PDF โดยตรง; มันจะเรสเตอร์ไลซ์แต่ละหน้า, ใช้ filter `preprocess` เดียวกัน, แล้วฝัง layer OCR ไม่ต้องเขียนโค้ดเพิ่ม

### จะเปลี่ยนรูปแบบผลลัพธ์เป็น PDF ธรรมดา (ไม่ค้นหาได้) อย่างไร?

เปลี่ยน `SaveFormat.SearchablePdf` เป็น `SaveFormat.Pdf` คุณจะเสีย layer ข้อความที่ค้นหาได้ แต่คุณภาพภาพจะคงเดิม

### สแกนของฉันเป็นสี—การลบพื้นหลังจะกระทบสีหรือไม่?

`RemoveBackground()` มุ่งลบพื้นหลังที่ไม่ใช่สีขาวโดยยังคงข้อความหลักไว้ หากต้องการเก็บกราฟิกสี คุณสามารถละเว้น filter นี้ได้:

```csharp
.Preprocess = img => ImageFilters.Deskew(img).Denoise()
```

### ฉันรันบนเซิร์ฟเวอร์ที่มี RAM จำกัด—ลดจำนวนเธรดได้หรือไม่?

ได้เลย ตั้งค่า `MaxDegreeOfParallelism` เป็น `1` หรือ `2` batch จะใช้เวลานานขึ้นแต่การใช้หน่วยความจำจะต่ำลง

---

## สรุปภาพรวม (ตัวเลือก)

หากคุณชอบแผนภาพสั้น ๆ ให้นึกภาพ flow นี้:

![Create searchable pdf workflow – shows input folder → preprocessing → OCR → searchable PDF output](/images/ocr-workflow.png)

*Image alt text:* **Create searchable pdf workflow diagram** – illustrates batch OCR processing, conversion, and deskew steps.

---

## สรุป

ตอนนี้คุณมีโซลูชัน **ครบวงจรพร้อมใช้งานในระดับ production** เพื่อ **สร้างไฟล์ PDF ที่ค้นหาได้** จากชุดภาพสแกนใด ๆ ด้วยการใช้ **batch OCR processing** คุณสามารถ **แปลงภาพเป็น pdf**, **เตรียมภาพก่อน OCR**, และ **แก้ไขการเอียงของเอกสารสแกน** อัตโนมัติ—ทั้งหมดด้วยไม่กี่บรรทัดของ C#  

ขั้นตอนต่อไป? ลองเพิ่มรูปแบบการตั้งชื่อไฟล์เอง, เชื่อมต่อ framework การบันทึก log เพื่อเก็บคะแนนความเชื่อมั่นของ OCR, หรือทดลองใช้ `ImageFilters` อื่น ๆ เช่น `Sharpen()` สำหรับข้อความที่จาง Aspose OCR API มีความยืดหยุ่นพอที่จะเติบโตตามความต้องการของคุณ  

ขอให้เขียนโค้ดสนุกและ PDF ของคุณค้นหาได้เสมอ!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}