---
category: general
date: 2026-02-09
description: ดึงข้อความจากภาพโดยใช้ C# OCR แบบออฟไลน์ ตัวอย่าง OCR ด้วย C# ที่สมบูรณ์แสดงวิธีโหลดภาพสำหรับ
  OCR, จดจำข้อความซีริลลิกและดึงข้อความจากหนังสือเดินทาง.
draft: false
keywords:
- extract text from image
- c# ocr example
- load image for ocr
- recognize cyrillic text
- recognize text from passport
language: th
og_description: แยกข้อความจากภาพด้วย C# OCR แบบออฟไลน์ เรียนรู้ตัวอย่าง OCR ด้วย C#
  ทีละขั้นตอนที่โหลดภาพเพื่อทำ OCR, จดจำข้อความซีริลลิกและดึงข้อความจากหนังสือเดินทาง.
og_title: สกัดข้อความจากรูปภาพใน C# – คู่มือ OCR แบบออฟไลน์
tags:
- OCR
- C#
- Aspose
title: ดึงข้อความจากภาพใน C# – ตัวอย่าง OCR แบบออฟไลน์
url: /th/net/text-recognition/extract-text-from-image-in-c-offline-ocr-example/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# ดึงข้อความจากรูปภาพใน C# – ตัวอย่าง OCR แบบออฟไลน์

เคยต้องการ **ดึงข้อความจากรูปภาพ** แต่ติดขัดกับ API ที่ต้องพึ่งพาเครือข่ายหรือไม่? คุณไม่ได้เป็นคนเดียว นักพัฒนาหลายคนเจอปัญหาเมื่อบริการ OCR พยายามดาวน์โหลดแพ็คภาษาในขณะทำงาน โดยเฉพาะในสภาพแวดล้อมที่จำกัด

ในคู่มือนี้ เราจะพาไปผ่าน **c# ocr example** ที่ทำงานแบบออฟไลน์ทั้งหมด โหลดรูปภาพสำหรับ OCR และจดจำข้อความซีริลลิกจากพาสปอร์ต เมื่อเสร็จคุณจะได้โปรแกรมพร้อมรันที่พิมพ์เนื้อหาข้อความธรรมดาของรูปภาพที่รองรับใด ๆ ลงคอนโซลโดยตรง

## สิ่งที่คุณจะได้เรียนรู้

- วิธีตั้งค่า Aspose.OCR สำหรับการประมวลผลแบบออฟไลน์.  
- โค้ดที่แน่นอนเพื่อ **load image for OCR** จากดิสก์.  
- วิธีกำหนดค่าเอนจินให้ **recognize cyrillic text**.  
- ตัวอย่าง **c# ocr example** ที่สมบูรณ์พร้อมคัดลอก‑วาง ซึ่งดึงข้อความจากรูปแบบพาสปอร์ต  

ไม่จำเป็นต้องมีประสบการณ์กับ Aspose มาก่อน; เพียงแค่ SDK .NET 6 (หรือใหม่กว่า) และ Visual Studio 2022 (หรือ VS Code) ก็เพียงพอ

---

![ดึงข้อความจากรูปภาพโดยใช้ Aspose OCR บนรูปพาสปอร์ต](/images/ocr-passport.jpg "ดึงข้อความจากรูปภาพ")

## ขั้นตอนที่ 1: ตั้งค่าโปรเจกต์เพื่อดึงข้อความจากรูปภาพ

ก่อนเขียนโค้ดใด ๆ ให้แน่ใจว่าแพ็กเกจ Aspose.OCR NuGet ถูกเพิ่มลงในโปรเจกต์ของคุณ:

```bash
dotnet add package Aspose.OCR
```

> **เคล็ดลับ:** ใช้แฟล็ก `--version` เพื่อล็อกเวอร์ชันที่เสถียรล่าสุด (เช่น `13.9.0`). สิ่งนี้รับประกันความเข้ากันได้กับ .NET 6.

การสร้างแอปคอนโซลใหม่ง่ายเพียงแค่:

```bash
dotnet new console -n OfflineOcrDemo
cd OfflineOcrDemo
```

ตอนนี้คุณมีพื้นฐานที่สะอาดซึ่งเราจะ **ดึงข้อความจากรูปภาพ** โดยไม่ต้องเชื่อมต่ออินเทอร์เน็ตเลย

## ขั้นตอนที่ 2: โหลดรูปภาพสำหรับ OCR – อ่านรูปพาสปอร์ต

สิ่งแรกที่เอนจิน OCR ต้องการคือบิตแมพหรือสตรีมที่แสดงภาพ ในสถานการณ์ของเรา เราจะ **load image for OCR** จากไฟล์ในเครื่องชื่อ `cyrillic_passport.jpg`.

```csharp
using Aspose.OCR;
using Aspose.OCR.Models;
using System;

// Step 2: Load the image file (this is the “load image for ocr” part)
var imagePath = @"YOUR_DIRECTORY\cyrillic_passport.jpg";

// Validate the file exists – helpful when the path is wrong.
if (!System.IO.File.Exists(imagePath))
{
    Console.WriteLine($"❌ Image not found at {imagePath}");
    return;
}

// ImageStream abstracts the underlying format; it works with JPEG, PNG, etc.
var image = ImageStream.FromFile(imagePath);
```

> **ทำไมเรื่องนี้สำคัญ:** การให้สตรีมแทน `Bitmap` ดิบทำให้ Aspose จัดการตรวจจับรูปแบบภายใน ลดโค้ดซ้ำซ้อนและข้อบกพร่องที่อาจเกิดขึ้น

## ขั้นตอนที่ 3: กำหนดค่าโหมดออฟไลน์และเลือกภาษาซีริลลิก

Aspose.OCR สามารถดาวน์โหลดโมเดลภาษาแบบเรียลไทม์ได้ แต่จะทำให้วัตถุประสงค์ของโซลูชันออฟไลน์หายไป ปิดการเรียกเครือข่ายและบอกเอนจินอย่างชัดเจนว่าต้องใช้ภาษาใด

```csharp
// Step 3: Create the OCR engine and switch to offline mode
var ocrEngine = new OcrEngine
{
    Configuration =
    {
        OfflineMode = true,               // No network traffic – perfect for secure environments
        Language = new[] { OcrLanguage.Cyrillic } // We want to **recognize cyrillic text**
    }
};
```

> **กรณีขอบ:** หากคุณต้องการจดจำอักขระละตินในเอกสารเดียวกันในภายหลัง เพียงเพิ่ม `OcrLanguage.English` ลงในอาเรย์ เอนจินจะจัดการการตรวจจับหลายภาษาโดยอัตโนมัติ

## ขั้นตอนที่ 4: รันเอนจิน OCR และจดจำข้อความซีริลลิก

ตอนนี้เราจริง ๆ **recognize text from passport**‑style images. เมธอด `Recognize` จะคืนค่าอ็อบเจ็กต์ผลลัพธ์ที่มีข้อความธรรมดา, คะแนนความมั่นใจ, และกล่องขอบเขต

```csharp
// Step 4: Perform the OCR operation
OcrResult result = ocrEngine.Recognize(image);

// Step 5: Output the plain text – this is where we finally **extract text from image**
Console.WriteLine("📝 Extracted Text:");
Console.WriteLine("-------------------");
Console.WriteLine(result.PlainText);
```

### ผลลัพธ์ที่คาดหวังในคอนโซล

```
📝 Extracted Text:
-------------------
ПАСПОРТ РФ
Иванов Иван Иванович
01.01.1990
...
```

หากผลลัพธ์ดูเป็นอักขระผสมกัน ให้ตรวจสอบว่าภาพต้นฉบับชัดเจนและว่าแพ็คภาษา `OfflineMode` สำหรับซีริลลิกมีอยู่ในโฟลเดอร์การติดตั้งของ Aspose (โดยปกติ `\Aspose.OCR\resources\languages`).

## ตัวอย่าง C# OCR สมบูรณ์ – โค้ดเต็ม

ด้านล่างคือ **c# ocr example** อย่างเต็มรูปแบบ คัดลอก‑วางลงใน `Program.cs` แล้วรัน `dotnet run`. ทุกอย่างที่คุณต้องการเพื่อ **ดึงข้อความจากรูปภาพ** อยู่ที่นี่

```csharp
using Aspose.OCR;
using Aspose.OCR.Models;
using System;

class OfflineExample
{
    static void Main()
    {
        // --------------------------------------------------------------
        // Step 1: Create the OCR engine (offline mode)
        // --------------------------------------------------------------
        var ocrEngine = new OcrEngine
        {
            Configuration =
            {
                OfflineMode = true,                     // No network calls
                Language = new[] { OcrLanguage.Cyrillic } // Recognize Cyrillic text
            }
        };

        // --------------------------------------------------------------
        // Step 2: Load the image for OCR (passport photo)
        // --------------------------------------------------------------
        var imagePath = @"YOUR_DIRECTORY\cyrillic_passport.jpg";

        if (!System.IO.File.Exists(imagePath))
        {
            Console.WriteLine($"❌ Image not found at {imagePath}");
            return;
        }

        var image = ImageStream.FromFile(imagePath);

        // --------------------------------------------------------------
        // Step 3: Recognize the text
        // --------------------------------------------------------------
        var result = ocrEngine.Recognize(image);

        // --------------------------------------------------------------
        // Step 4: Output the plain text (the final extraction)
        // --------------------------------------------------------------
        Console.WriteLine("📝 Extracted Text:");
        Console.WriteLine("-------------------");
        Console.WriteLine(result.PlainText);
    }
}
```

### การรันตัวอย่าง

```bash
dotnet run
```

คุณควรเห็นคอนโซลพิมพ์รายละเอียดพาสปอร์ตเป็นภาษาซีริลลิก นั่นคือช่วงเวลาที่คุณรู้ว่ากระบวนการ **ดึงข้อความจากรูปภาพ** ของคุณทำงานได้

## ปัญหาที่พบบ่อย & วิธีแก้

| อาการ | สาเหตุที่เป็นไปได้ | วิธีแก้ |
|---------|--------------|-----|
| `PlainText` ว่าง | โมเดลภาษาผิดหรือภาพมืดเกินไป | ตรวจสอบให้แน่ใจว่าแพ็คภาษา `OfflineMode` มี `Cyrillic` และเพิ่มความคอนทราสต์ของภาพ |
| `System.DllNotFoundException` | ไฟล์ไบนารีเนทีฟของ Aspose OCR ขาดหาย | ติดตั้งแพ็กเกจ NuGet ใหม่หรือคัดลอก `Aspose.OCR.Native.dll` ไปยังโฟลเดอร์เอาต์พุต |
| ประสิทธิภาพช้าเมื่อใช้ภาพขนาดใหญ่ | เอนจินประมวลผลความละเอียดเต็ม | ลดขนาดภาพให้กว้าง ≤ 1500 px ก่อนส่งให้ `ImageStream` |
| อักขระแสดงผิดรูป | ภาพหมุนผิดทิศทาง | ใช้ `Image.RotateFlip(RotateFlipType.Rotate90FlipNone)` ก่อนสร้างสตรีม |

## ขั้นตอนต่อไป – ขยายการทำงานของ OCR แบบออฟไลน์

- **Load image for OCR** จาก `MemoryStream` เมื่อจัดการไฟล์อัปโหลดใน ASP.NET Core.  
- เปลี่ยนเป็น **recognize text from passport** ในโหมดแบตช์โดยวนลูปโฟลเดอร์ของสแกนพาสปอร์ต.  
- รวมผลลัพธ์กับ **regular expressions** เพื่อดึงฟิลด์เช่นหมายเลขพาสปอร์ตหรือวันเกิด.  
- ทดลองใช้ `ocrEngine.Configuration.UseParallelProcessing = true` เพื่อเพิ่มความเร็วแบบหลายคอร์.  

---

### สรุป

เราเพิ่งแสดงวิธี **ดึงข้อความจากรูปภาพ** ด้วย pipeline OCR ของ C# ที่ทำงานแบบออฟไลน์เต็มรูปแบบ ตัวอย่าง **c# ocr example** สั้นและอิสระนี้โหลดรูปภาพ กำหนดค่าเอนจินให้ **recognize cyrillic text** และพิมพ์ข้อมูลพาสปอร์ตที่ดึงออกมา — ทั้งหมดโดยไม่ต้องมีการเรียกเครือข่ายแม้ครั้งเดียว

คุณสามารถปรับแต่งโค้ด เพิ่มภาษาอื่น ๆ หรือเชื่อมต่อผลลัพธ์เข้าฐานข้อมูลได้ตามต้องการ ท้องฟ้าเป็นขอบเขตเมื่อคุณเชี่ยวชาญพื้นฐานการโหลดรูปภาพสำหรับ OCR และการจดจำข้อความจากรูปแบบพาสปอร์ต

มีคำถามหรืออยากแชร์การปรับแต่งของคุณ? แสดงความคิดเห็นด้านล่าง แล้วขอให้เขียนโค้ดอย่างสนุกสนาน!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}