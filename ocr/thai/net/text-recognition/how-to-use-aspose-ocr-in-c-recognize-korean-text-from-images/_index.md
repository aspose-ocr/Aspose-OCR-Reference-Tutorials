---
category: general
date: 2025-12-29
description: วิธีใช้ Aspose OCR เพื่อแปลงข้อความในรูปภาพและดึงข้อความภาษาเกาหลีออกมา
  คู่มือขั้นตอนต่อขั้นตอนในการดึงข้อความจากรูปภาพและจดจำข้อความภาษาเกาหลีด้วย C#
draft: false
keywords:
- how to use aspose
- convert image text
- extract text image
- extract korean text
- recognize korean text
language: th
og_description: เรียนรู้วิธีใช้ Aspose OCR เพื่อแปลงข้อความจากภาพ, ดึงข้อความภาษาเกาหลี,
  และจดจำข้อความภาษาเกาหลีจากรูปภาพพร้อมตัวอย่าง C# ที่ครบถ้วน
og_title: วิธีใช้ Aspose OCR – แยกข้อความภาษาเกาหลีใน C#
tags:
- Aspose
- OCR
- C#
- Image Processing
title: วิธีใช้ Aspose OCR ใน C# – แยกข้อความภาษาเกาหลีจากรูปภาพ
url: /th/net/text-recognition/how-to-use-aspose-ocr-in-c-recognize-korean-text-from-images/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# วิธีใช้ Aspose OCR ใน C# – แยกข้อความภาษาเกาหลีจากรูปภาพ

เคยสงสัย **วิธีใช้ Aspose** เพื่อดึงอักขระภาษาเกาหลีออกจากภาพหรือไม่? บางทีคุณอาจมีภาพหน้าจอของป้ายถนน, ใบเสร็จสแกน, หรือมีมที่ต้องการแปลงเป็นข้อความที่ค้นหาได้ ข่าวดีคือ Aspose OCR ทำให้เรื่องนี้ง่ายดายและคุณไม่ต้องต่อสู้กับเทคนิคการประมวลผลภาพระดับต่ำ.

ในบทแนะนำนี้เราจะพาคุณผ่าน **ตัวอย่างที่สมบูรณ์และสามารถรันได้** ที่แสดงวิธี **แปลงข้อความจากรูปภาพ**, **ดึงข้อความจากรูปภาพ**, และโดยเฉพาะ **ดึงข้อความภาษาเกาหลี** ด้วยไลบรารี Aspose OCR. เมื่อเสร็จคุณจะมีแอปคอนโซลที่พิมพ์สตริงภาษาเกาหลีที่ถูกจดจำ และคุณจะเข้าใจว่าทำไมแต่ละบรรทัดถึงสำคัญ.

## สิ่งที่คุณต้องเตรียม

- **.NET 6+** (SDK .NET ล่าสุดใดก็ได้ – Visual Studio, Rider หรือ `dotnet` CLI)
- **Aspose.OCR for .NET** NuGet package  
  ```bash
  dotnet add package Aspose.OCR
  ```
- ไฟล์รูปภาพที่มีอักขระภาษาเกาหลี (เช่น `korean_sign.jpg`).
- ความรู้พื้นฐานเล็กน้อยของ C# – หากคุณเคยเขียน “Hello World” มาก่อนก็พร้อมใช้งานแล้ว.

> **เคล็ดลับ:** Aspose OCR รองรับมากกว่า 50 ภาษาโดยไม่ต้องตั้งค่าเพิ่มเติม เราจะเน้นที่ภาษาเกาหลีเพราะสคริปต์ Hangul ของมันมักทำให้เครื่อง OCR ทั่วไปสับสน.

## ขั้นตอนที่ 1 – ติดตั้งและอ้างอิง Aspose OCR

ขั้นแรกให้เพิ่มไลบรารีเข้าไปในโปรเจกต์ของคุณ คำสั่ง NuGet ด้านบนทำงานหนักให้แล้ว แต่ถ้าคุณชอบใช้ UI เพียงค้นหา *Aspose.OCR* ใน NuGet Package Manager.

```csharp
// No code needed here – the package reference is enough.
// The using directives below will bring the types into scope.
using Aspose.OCR;
using Aspose.OCR.Models;
```

> **ทำไมสิ่งนี้สำคัญ:** คำสั่ง `using` ให้คุณเข้าถึง `OcrEngine`, `Language` และคลาสช่วยเหลือ `Image`. หากไม่มีมันคอมไพเลอร์จะบอกว่าไม่รู้จักประเภทเหล่านั้น.

## ขั้นตอนที่ 2 – โหลดรูปภาพที่คุณต้องการประมวลผล

Aspose OCR ทำงานกับตัวห่อ `Image` ของมันเอง ซึ่งสามารถอ่านไฟล์ JPEG, PNG, BMP และรูปแบบอื่น ๆ มากมาย ให้ชี้ไปที่ไฟล์ที่มีข้อความภาษาเกาหลี

```csharp
// Step 2: Load the image containing Korean characters
var imagePath = Path.Combine(Environment.CurrentDirectory, "korean_sign.jpg");
var image = Image.Load(imagePath);
```

หากไฟล์ไม่ได้อยู่ในโฟลเดอร์เดียวกับไฟล์ executable ของคุณ ให้ปรับเส้นทางให้เหมาะสม การเรียก `Image.Load` ทำการ **แปลงข้อความจากรูปภาพ** เป็นการแสดงผลภายในที่เครื่อง OCR สามารถเข้าใจได้.

![ตัวอย่างการใช้ aspose OCR](/images/aspose-ocr-korean.png "วิธีใช้ aspose OCR เพื่อจดจำข้อความภาษาเกาหลี")

*ข้อความแทนรูปภาพ: “ตัวอย่างการใช้ aspose OCR แสดงป้ายถนนภาษาเกาหลี.”*

## ขั้นตอนที่ 3 – ตั้งค่า OCR Engine สำหรับภาษาเกาหลี

Engine ต้องรู้ว่าต้องมองหาภาษาใด; หากไม่กำหนดจะใช้ค่าเริ่มต้นเป็นภาษาอังกฤษและจะพลาดอักขระ Hangul

```csharp
// Step 3: Create and configure the OCR engine
var ocrEngine = new OcrEngine
{
    // Tell Aspose we want to recognize Korean (Hangul)
    Language = Language.Korean
};
```

> **ทำไมสิ่งนี้สำคัญ:** การตั้งค่า `Language = Language.Korean` บอก engine ให้โหลดแพ็คเกจภาษาเกาหลี ซึ่งจะเพิ่มความแม่นยำอย่างมากสำหรับ glyphs ของ Hangul การข้ามขั้นตอนนี้มักทำให้ผลลัพธ์เป็นข้อความผิดพลาด.

## ขั้นตอนที่ 4 – เรียกกระบวนการจดจำ

ตอนนี้เราจะขอให้ Aspose อ่านรูปภาพจริง ๆ วิธี `Recognize` จะคืนค่าเป็นอ็อบเจกต์ `OcrResult` ที่บรรจุสตริงที่ดึงออกมาและคะแนนความมั่นใจ

```csharp
// Step 4: Run OCR on the loaded image
OcrResult ocrResult = ocrEngine.Recognize(image);
```

หากคุณต้องการ **ดึงข้อความจากรูปภาพ** จากภาพที่ใหญ่กว่า (เช่น ภาพหน้าจอที่มีหลายองค์ประกอบ UI) คุณสามารถตัดส่วนที่ต้องการโดยใช้ `image.Crop(...)` ก่อนเรียก `Recognize`. นี่เป็นเทคนิคที่สะดวกเมื่อคุณสนใจเฉพาะส่วนของภาพเท่านั้น.

## ขั้นตอนที่ 5 – แสดงข้อความภาษาเกาหลีที่จดจำได้

สุดท้ายให้แสดงผลลัพธ์ ในแอปจริงคุณอาจเก็บไว้ในฐานข้อมูลหรือส่งต่อไปยัง API แปลภาษา แต่สำหรับบทแนะนำนี้การพิมพ์ออกคอนโซลทำให้เรื่องง่าย

```csharp
// Step 5: Print the recognized Korean text
Console.WriteLine("Recognized Korean text:");
Console.WriteLine(ocrResult.Text);
```

### ผลลัพธ์ที่คาดหวัง

```
Recognized Korean text:
서울특별시 강남구 테헤란로 123
```

ผลลัพธ์จริงของคุณจะสะท้อนอักขระภาษาเกาหลีที่อยู่ใน `korean_sign.jpg` ตามแน่นอน.

## ตัวอย่างทำงานเต็มรูปแบบ

ด้านล่างเป็น **โปรแกรมเต็ม** ที่คุณสามารถคัดลอก‑วางลงในโปรเจกต์คอนโซลใหม่ (`dotnet new console`). ตรวจสอบให้ไฟล์รูปภาพอยู่ข้างไฟล์ `.exe` ที่คอมไพล์หรือปรับเส้นทางให้เหมาะสม.

```csharp
using System;
using System.IO;
using Aspose.OCR;
using Aspose.OCR.Models;

class Program
{
    static void Main()
    {
        // 1️⃣ Install Aspose.OCR via NuGet before running this code.

        // 2️⃣ Load the image that contains Korean text.
        var imagePath = Path.Combine(Environment.CurrentDirectory, "korean_sign.jpg");
        var image = Image.Load(imagePath);

        // 3️⃣ Create the OCR engine and set it to recognize Korean.
        var ocrEngine = new OcrEngine
        {
            Language = Language.Korean   // 👈 This enables Hangul support.
        };

        // 4️⃣ Run the OCR process.
        OcrResult ocrResult = ocrEngine.Recognize(image);

        // 5️⃣ Output the extracted Korean string.
        Console.WriteLine("Recognized Korean text:");
        Console.WriteLine(ocrResult.Text);
    }
}
```

เรียกโปรแกรมด้วย `dotnet run` แล้วดูอักขระภาษาเกาหลีปรากฏในคอนโซลของคุณ.

## คำถามทั่วไปและกรณีขอบ

### ถ้า OCR คืนค่าตัวอักษรที่ผิดพลาด?

- **ตรวจสอบการตั้งค่าภาษา.** ลืมตั้งค่า `Language.Korean` เป็นข้อผิดพลาดที่พบบ่อยที่สุด.
- **ปรับปรุงคุณภาพภาพ.** ภาพที่คมชัด, DPI สูง, และแสงที่เหมาะสมช่วยเพิ่มความแม่นยำ.
- **ทำการประมวลผลล่วงหน้าภาพ.** Aspose OCR มีฟิลเตอร์ในตัว (`image.Binarize()`, `image.Deskew()`) ที่สามารถทำความสะอาดสแกนที่มีเสียงรบกวนได้.

### ฉันสามารถ **แปลงข้อความจากรูปภาพ** เป็นจำนวนมากได้หรือไม่?

ได้เลย. ห่อขั้นตอนข้างต้นในลูป `foreach` ที่วนผ่านโฟลเดอร์ของรูปภาพ นี่คือตัวอย่างสั้น ๆ:

```csharp
foreach (var file in Directory.GetFiles(@"C:\KoreanImages", "*.jpg"))
{
    var img = Image.Load(file);
    var result = ocrEngine.Recognize(img);
    File.WriteAllText(Path.ChangeExtension(file, ".txt"), result.Text);
}
```

สคริปต์นี้ **ดึงข้อความจากรูปภาพ** จากทุกไฟล์และเขียนไฟล์ `.txt` ข้าง ๆ กัน.

### ฉันจะจัดการหลายภาษาในภาพเดียวอย่างไร?

Aspose OCR สามารถตรวจจับภาษาด้วยตนเองได้หากคุณตั้งค่า `Language = Language.Auto`. อย่างไรก็ตาม การตรวจจับอัตโนมัติอาจช้ากว่าและความแม่นยำอาจต่ำกว่าการระบุภาษาที่แน่นอน หากคุณรู้ว่าภาพมีทั้งภาษาเกาหลีและอังกฤษ คุณสามารถรันสองครั้ง—ครั้งแรกกับ `Language.Korean`, ครั้งที่สองกับ `Language.English`—และต่อผลลัพธ์เข้าด้วยกัน.

## เคล็ดลับสำหรับ OCR ที่พร้อมใช้งานในผลิตภัณฑ์

- **แคช OcrEngine.** การสร้าง engine ใหม่สำหรับแต่ละคำขอเพิ่มภาระงาน. ควรใช้ singleton หากคุณประมวลผลหลายภาพ.
- **จำกัดขนาดภาพ.** ภาพขนาดใหญ่ใช้หน่วยความจำ; ลดขนาดลงประมาณ 1500 px ความกว้างก่อนส่งให้ engine.
- **จัดการข้อยกเว้น.** ห่อการเรียก `Recognize` ด้วย try/catch เพื่อจัดการไฟล์ที่เสียหายอย่างราบรื่น.

## สรุป

เราพึ่งอธิบาย **วิธีใช้ Aspose** เพื่อ **แปลงข้อความจากรูปภาพ**, **ดึงข้อความจากรูปภาพ**, และโดยเฉพาะ **ดึงข้อความภาษาเกาหลี** ด้วยไม่กี่บรรทัดของโค้ด C#. ขั้นตอนง่าย ๆ ดังนี้:

1. ติดตั้ง Aspose OCR.  
2. โหลดรูปภาพของคุณ.  
3. ตั้งค่า engine สำหรับภาษาเกาหลี.  
4. เรียก `Recognize`.  
5. แสดงผลลัพธ์.

ตอนนี้คุณสามารถนำโค้ดนี้ไปใส่ในเวิร์กโฟลว์ที่ใหญ่ขึ้น—การประมวลผลเป็นชุด, การเก็บเอกสาร, หรือแม้กระทั่งแอปแปลแบบเรียลไทม์. อยากทำต่อ? ลองเพิ่มเมธอด `Image.Preprocess()` ของ Aspose, ทดลองกับภาษาต่าง ๆ, หรือรวมผลลัพธ์กับ Azure Cognitive Services เพื่อการแปล.

มีคำถามเพิ่มเติมเกี่ยวกับ **การจดจำข้อความภาษาเกาหลี** หรือฟีเจอร์อื่นของ Aspose? แสดงความคิดเห็นได้เลย, ขอให้เขียนโค้ดอย่างสนุก!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}