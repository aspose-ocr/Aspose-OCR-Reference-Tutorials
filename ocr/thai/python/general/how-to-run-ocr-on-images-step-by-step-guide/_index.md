---
category: general
date: 2026-01-02
description: วิธีรัน OCR และดึงข้อความจากภาพอย่างรวดเร็ว เรียนรู้วิธีโหลดภาพสำหรับ
  OCR ปรับปรุงความแม่นยำของ OCR และได้ผลลัพธ์ที่เชื่อถือได้
draft: false
keywords:
- how to run OCR
- extract text from image
- how to load image
- improve OCR accuracy
- load image for OCR
language: th
og_description: วิธีใช้ OCR กับรูปภาพใดก็ได้ คู่มือนี้จะแสดงวิธีโหลดภาพสำหรับ OCR
  ดึงข้อความจากภาพ และปรับปรุงความแม่นยำของ OCR ด้วยการประมวลผลหลังจาก AI.
og_title: วิธีใช้ OCR – คู่มือเต็มสำหรับการสกัดข้อความที่แม่นยำ
tags:
- OCR
- Python
- image processing
title: วิธีใช้ OCR กับภาพ – คู่มือขั้นตอนโดยละเอียด
url: /th/python/general/how-to-run-ocr-on-images-step-by-step-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# วิธีการรัน OCR – บทเรียนครบถ้วนสำหรับการสกัดข้อความที่แม่นยำ

เคยสงสัย **how to run OCR** บนภาพหน้าจอที่เต็มไปด้วยการพิมพ์ผิดหรือไม่? คุณไม่ได้เป็นคนเดียว ในหลายโครงการ นักพัฒนาต้องการดึงข้อความที่สะอาดและค้นหาได้จากเอกสารสแกน, ใบเสร็จ, หรือแม้แต่ meme, และผลลัพธ์ดิบมักจะยุ่งยาก ข่าวดีคือ? ด้วยไม่กี่บรรทัดของ Python คุณสามารถโหลดภาพ, รัน OCR engine, แล้วเพิ่มผลลัพธ์ด้วย post‑processor ที่เสริมด้วย AI  

ในบทเรียนนี้เราจะพาคุณผ่านทุกอย่างที่ต้องรู้: ตั้งแต่ **how to load image** เข้าไปใน engine, การสกัดข้อความจากภาพ, และสุดท้ายการปรับปรุงความแม่นยำของ OCR ด้วย post‑processor อัจฉริยะ ไม่ต้องใช้บริการภายนอก เพียงตัวอย่างที่ทำงานได้เองที่คุณสามารถรันได้วันนี้

---

## สิ่งที่คุณต้องมี

- **Python 3.9+** (เวอร์ชันล่าสุดใดก็ได้)
- อินสแตนซ์ของ OCR engine (สำหรับการสาธิตเราจะสมมติว่าเป็นอ็อบเจ็กต์ `engine` ที่ทำตามรูปแบบทั่วไป `load_image → recognize → run_postprocessor`)
- รูปตัวอย่าง เช่น `sample_with_typos.png` ที่วางไว้ในโฟลเดอร์ที่คุณอ้างอิงได้
- ทางเลือก: สภาพแวดล้อมเสมือน (virtual environment) เพื่อจัดการ dependencies ให้เป็นระเบียบ

> **Pro tip:** หากคุณใช้ Tesseract ให้ติดตั้งผ่านตัวจัดการแพ็กเกจของ OS แล้วห่อด้วย Python wrapper อย่าง `pytesseract`. โค้ดด้านล่างทำ abstraction ให้กับ engine จึงสามารถสลับ implementation ได้โดยไม่ต้องเปลี่ยนโค้ดส่วนรอบข้าง

---

## ขั้นตอนที่ 1 – How to Load Image for OCR

สิ่งแรกที่คุณต้องทำคือชี้ OCR engine ไปที่ไฟล์ที่ต้องการอ่าน นี่คือจุดที่วลี **how to load image** มีความหมายตามตัวอักษร: คุณให้ engine พาธของไฟล์และมันจะเตรียม bitmap สำหรับการจดจำ

```python
# Step 1: Load the image into the OCR engine
ocr_engine = engine               # assume the OCR engine instance is already created
ocr_engine.load_image("YOUR_DIRECTORY/sample_with_typos.png")
```

**ทำไมเรื่องนี้สำคัญ:**  
การโหลดภาพอย่างถูกต้องทำให้ engine เห็นข้อมูลพิกเซลที่คุณต้องการประมวลผลอย่างแม่นยำ การข้ามขั้นตอน preprocessing (เช่น การปรับขนาดหรือแปลงเป็น grayscale) อาจทำให้ engine แปลอักษรผิดพลาด โดยเฉพาะในสแกนที่คอนทราสต์ต่ำ

---

## ขั้นตอนที่ 2 – Run OCR to Extract Text from Image

เมื่อภาพพร้อมแล้ว เราจะเรียกใช้ฟังก์ชันหลักของ OCR เมธอดจะคืนค่าอ็อบเจ็กต์ที่มีแอตทริบิวต์ `.text` เก็บสตริงดิบ

```python
# Step 2: Run the basic OCR to obtain the raw text output
raw_result = ocr_engine.recognize()   # returns an object with a .text attribute
```

**สิ่งที่คุณจะได้:**  
`raw_result.text` จะมีทุกคำที่ engine ตรวจจับได้ รวมถึงข้อผิดพลาดการสะกดหรือ artefacts ที่เกิดจาก noise คิดว่าเป็น **raw extraction**—พื้นฐานสำหรับการปรับปรุงต่อไป

---

## ขั้นตอนที่ 3 – Improve OCR Accuracy with AI‑Enhanced Post‑Processing

หลาย pipeline ของ OCR สมัยใหม่ให้ hook สำหรับ post‑processing ในตัวอย่างของเรา `run_postprocessor` จะใช้โมเดล AI ขนาดเล็กที่แก้ไขการพิมพ์ผิดทั่วไป, ทำให้เครื่องหมายวรรคตอนเป็นมาตรฐาน, และแม้กระทั่งจัดลำดับคำใหม่เมื่อเลย์เอาต์สับสน

```python
# Step 3: Apply the AI‑enhanced post‑processor to improve accuracy
enhanced_result = ocr_engine.run_postprocessor(raw_result)
```

**ทำไมต้องใช้ post‑processor?**  
แม้ OCR engine ที่ดีที่สุดก็อาจพลาดกับฟอนต์ที่บิดเบี้ยวหรือพื้นหลังที่มี noise ชั้นที่ขับเคลื่อนด้วย AI สามารถเรียนรู้จากคอร์ปัสของข้อความที่แก้ไขแล้ว, ทำให้ **improve OCR accuracy** อย่างมากโดยไม่ต้องแทรกแซงด้วยมือ

---

## ขั้นตอนที่ 4 – Print Both Raw and AI‑Enhanced OCR Results

การเห็นความแตกต่างแบบข้างเคียงช่วยให้คุณประเมินประสิทธิภาพของ post‑processor และตัดสินใจว่าต้องปรับแต่งเพิ่มเติมหรือไม่

```python
# Step 4: Print the raw and AI‑enhanced OCR results
print("Raw OCR:      ", raw_result.text)
print("AI‑enhanced:  ", enhanced_result.text)
```

### ผลลัพธ์ที่คาดหวัง

```
Raw OCR:       Th1s 1s 4  s@mple w1th typ0s.
AI‑enhanced:   This is a sample with typos.
```

ในผลลัพธ์ดิบคุณสามารถสังเกตข้อผิดพลาดชัดเจน (`Th1s` → `This`, `4` → `a`, `s@mple` → `sample`). เวอร์ชันที่ AI‑enhanced จะทำความสะอาดข้อความเหล่านั้น ให้ได้ประโยคที่มนุษย์อ่านได้

---

## ตัวอย่างทำงานเต็ม (รวมทุกขั้นตอน)

ด้านล่างเป็นสคริปต์เต็มที่คุณสามารถคัดลอก‑วางลงในไฟล์ชื่อ `ocr_demo.py`. อย่าลืมแทนที่ `"YOUR_DIRECTORY"` ด้วยพาธจริงของภาพของคุณ

```python
# ocr_demo.py
# Complete, runnable example that shows how to run OCR,
# extract text from image, and improve OCR accuracy.

# -------------------------------------------------
# 1️⃣ Import the OCR engine (replace with your actual import)
# -------------------------------------------------
# Example placeholder:
# from my_ocr_lib import OCRengine
# engine = OCRengine()

# For this tutorial we assume `engine` is already instantiated.
# -------------------------------------------------

# -------------------------------------------------
# 2️⃣ Load the image
# -------------------------------------------------
ocr_engine = engine                     # existing OCR engine instance
ocr_engine.load_image("YOUR_DIRECTORY/sample_with_typos.png")

# -------------------------------------------------
# 3️⃣ Recognize raw text
# -------------------------------------------------
raw_result = ocr_engine.recognize()    # returns an object with .text

# -------------------------------------------------
# 4️⃣ Post‑process to improve accuracy
# -------------------------------------------------
enhanced_result = ocr_engine.run_postprocessor(raw_result)

# -------------------------------------------------
# 5️⃣ Display both results
# -------------------------------------------------
print("Raw OCR:      ", raw_result.text)
print("AI‑enhanced:  ", enhanced_result.text)
```

รันด้วยคำสั่ง:

```bash
python ocr_demo.py
```

คุณควรเห็นสตริงดิบและสตริงที่ทำความสะอาดแล้วพิมพ์ออกที่คอนโซล เหมือนกับในส่วน “ผลลัพธ์ที่คาดหวัง” ด้านบน

---

## คำถามทั่วไป & กรณีขอบ

### ถ้าภาพของฉันอยู่ในรูปแบบอื่น (เช่น PDF หรือ TIFF) จะทำอย่างไร?

OCR engine ส่วนใหญ่รับพาธไฟล์, แต่บางครั้งต้องแปลงสำหรับ PDF หลายหน้า คุณสามารถใช้ `pdf2image` เพื่อแปลงแต่ละหน้าเป็น PNG ก่อนส่งให้ engine

### จะจัดการกับภาษาที่ไม่ใช่อังกฤษอย่างไร?

ส่งรหัสภาษาให้กับ engine ตอนเริ่มต้น, เช่น `engine = OCRengine(lang='fra')`. post‑processor อาจต้องการโมเดลเฉพาะภาษาสำหรับการแก้ไข diacritics อย่างถูกต้อง

### ผลลัพธ์ OCR ยังมีอักขระแปลก ๆ อยู่—ทำอย่างไรต่อ?

พิจารณา preprocessing ภาพ:  
- **Resize** ให้ DPI สูงขึ้น (300 dpi เป็นค่าเริ่มต้นที่ดี)  
- **Convert to grayscale** เพื่อลด noise สี  
- **Apply thresholding** (`cv2.threshold`) เพื่อเพิ่มคอนทราสต์

ขั้นตอนเหล่านี้มักจะ **improve OCR accuracy** ก่อนที่ AI post‑processor จะทำงาน

---

## เคล็ดลับเพื่อให้ได้ประโยชน์สูงสุดจาก OCR Workflow ของคุณ

- **Batch processing:** วนลูปผ่านไดเรกทอรีของภาพและเก็บผลลัพธ์แต่ละรายการใน CSV เพื่อการวิเคราะห์ต่อไป  
- **Caching:** หากคุณรันภาพเดียวหลายครั้ง ให้แคชผลลัพธ์ดิบเพื่อหลีกเลี่ยงการคำนวณซ้ำ  
- **Model updates:** ปรับปรุงหรืออัปเดต AI post‑processor อย่างสม่ำเสมอด้วยตัวอย่างที่แก้ไขใหม่; โมเดลจะดีขึ้นตามเวลา  
- **Error logging:** จับข้อยกเว้นจาก `recognize()` และ `run_postprocessor()` เพื่อระบุไฟล์ที่มีปัญหาในภายหลัง

---

## สรุป

ตอนนี้คุณรู้ **how to run OCR** บนรูปภาพใด ๆ ตั้งแต่การโหลดภาพ, การสกัดข้อความ, จนถึงการขัดเกลาเอาต์พุตด้วย AI‑enhanced post‑processor. หากทำตามขั้นตอนข้างต้น คุณจะได้สตริงที่สะอาดและเชื่อถือได้เสมอ—ไม่ว่าจะเป็นการสร้าง receipt‑scanner, document‑archiver, หรือโปรเจกต์งานอดิเรกง่าย ๆ

พร้อมรับความท้าทายต่อไปหรือยัง? ลองผสาน **extract text from image** เข้าไปในฐานข้อมูลที่ค้นหาได้, หรือทดลองกฎ post‑processing ที่กำหนดเองตามโดเมนของคุณ. ไม่มีขีดจำกัด, และด้วย pipeline ที่เหมาะสม คุณจะแทบไม่เห็นข้อพิมพ์ผิดหลุดผ่านอีกเลย

Happy coding! 🚀

![how to run OCR example](https://example.com/ocr-demo.png "how to run OCR example")

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}