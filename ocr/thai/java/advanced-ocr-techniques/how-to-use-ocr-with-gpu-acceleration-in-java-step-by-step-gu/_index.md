---
category: general
date: 2026-02-09
description: วิธีใช้ OCR อย่างรวดเร็วด้วย Aspose OCR, จดจำข้อความจากภาพและดึงข้อความจาก
  PNG พร้อมตั้งค่าโหมดและขีดจำกัดหน่วยความจำ GPU.
draft: false
keywords:
- how to use ocr
- recognize text from image
- extract text from png
- how to set mode
- set gpu memory limit
language: th
og_description: วิธีใช้ OCR อย่างมีประสิทธิภาพ – เรียนรู้การจดจำข้อความจากภาพ, แยกข้อความจาก
  PNG, ตั้งค่าโหมด, และควบคุมขีดจำกัดหน่วยความจำ GPU ใน Java.
og_title: วิธีใช้ OCR พร้อมการเร่งความเร็วด้วย GPU ใน Java
tags:
- OCR
- Java
- GPU
- Aspose
title: วิธีใช้ OCR พร้อมเร่งความเร็วด้วย GPU ใน Java – คู่มือขั้นตอนโดยละเอียด
url: /th/java/advanced-ocr-techniques/how-to-use-ocr-with-gpu-acceleration-in-java-step-by-step-gu/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# วิธีใช้ OCR พร้อมการเร่งความเร็วด้วย GPU ใน Java – การสอนโปรแกรมเต็มรูปแบบ

เคยสงสัย **วิธีใช้ OCR** เพื่อดึงข้อความจากรูปภาพโดยไม่ต้องเขียนโค้ดเป็นล้านบรรทัดหรือไม่? คุณไม่ได้เป็นคนเดียว ในหลายโครงการ—การสแกนใบแจ้งหนี้, การประมวลผลใบเสร็จ, หรือเพียงแค่ดิจิไทซ์เอกสารเก่า—นักพัฒนาต้องการวิธีที่เชื่อถือได้ในการ **recognize text from image** ไฟล์, โดยเฉพาะ PNG ที่มักมีกราฟิกที่คมชัดและความละเอียดสูง.  

ข่าวดี? Aspose OCR ทำให้เรื่องนี้ง่ายเหมือนเค้ก, และด้วยการปรับแต่งการตั้งค่าเล็กน้อยคุณก็สามารถถ่ายงานหนักไปยัง GPU ของคุณได้ ในบทเรียนนี้เราจะเดินผ่านกระบวนการทั้งหมด: ตั้งแต่การโหลด PNG, ไปจนถึง **setting mode** สำหรับการประมวลผลด้วย GPU, ไปจนถึง **setting GPU memory limit**, และสุดท้ายพิมพ์ข้อความที่สกัดออกมา เมื่อเสร็จคุณจะมีโปรแกรม Java ที่รันได้ซึ่งทำในสิ่งที่คุณต้องการ.

## สิ่งที่คุณจะได้เรียนรู้

- วิธีติดตั้งและนำเข้า Aspose OCR สำหรับ Java.
- วิธี **recognize text from image** ไฟล์โดยใช้ไลบรารี
- วิธี **extract text from PNG** อย่างมีประสิทธิภาพ
- วิธี **set mode** เป็น GPU และควบคุมการใช้หน่วยความจำด้วย **set GPU memory limit**
- ข้อผิดพลาดทั่วไปและเคล็ดลับสำหรับการใช้งานในโลกจริง

### ข้อกำหนดเบื้องต้น

- Java 8 หรือใหม่กว่า (โค้ดนี้คอมไพล์ได้กับ JDK 11 ด้วย)
- NVIDIA GPU ที่มีไดรเวอร์รองรับ CUDA หากคุณต้องการการเร่งด้วย GPU
- Aspose OCR for Java JAR (ดาวน์โหลดจากเว็บไซต์ Aspose หรือเพิ่มผ่าน Maven/Gradle)
- ตัวอย่างภาพ PNG (เช่น `sample1.png`) ที่วางไว้ในโฟลเดอร์ที่คุณอ้างอิงได้

---

## วิธีใช้ OCR – เปิดโหมด GPU

สิ่งแรกที่คุณต้องทำคือบอก Aspose OCR ว่าคุณต้องการให้มันทำงานบน GPU แทน CPU นี่คือจุดที่คีย์เวิร์ด **how to set mode** เข้ามามีบทบาท.

```java
// Step 1: Create the OCR engine
OcrEngine ocrEngine = new OcrEngine();

// Step 2: Grab the configuration object
OcrEngineConfiguration config = ocrEngine.getConfiguration();

// Step 3: Switch processing mode to GPU
config.setProcessingMode(ProcessingMode.GPU);   // requires a CUDA‑compatible driver

// (Optional) Step 4: Limit GPU memory usage to 1024 MB
config.setGpuMemoryLimit(1024);                 // set gpu memory limit (MB)
```

**ทำไมเรื่องนี้ถึงสำคัญ:**  
การประมวลผลด้วย GPU สามารถเร็วขึ้นอย่างมากสำหรับชุดข้อมูลขนาดใหญ่หรือภาพความละเอียดสูง, แต่ก็ใช้หน่วยความจำวิดีโอด้วย การเรียก `setGpuMemoryLimit` จะป้องกันแอปพลิเคชันของคุณไม่ให้ใช้ GPU ทั้งหมด, ซึ่งสำคัญเมื่ออุปกรณ์เดียวกันทำงานอื่น ๆ (เช่น UI หรือโมเดลแมชชีนเลิร์นนิง) อยู่.

---

## การจดจำข้อความจากภาพด้วย Aspose OCR

ตอนนี้เอนจินได้รับการตั้งค่าแล้ว, เราต้องชี้ไปที่ไฟล์ที่ต้องการอ่าน นี่คือหัวใจของ **recognize text from image**.

```java
// Step 5: Define the image to be processed
ImageRecognitionResult imageInfo = new ImageRecognitionResult();
imageInfo.setImagePath("YOUR_DIRECTORY/sample1.png");

// Step 6: Run the OCR operation
RecognitionResult ocrResult = ocrEngine.recognize(imageInfo);
```

**อะไรเกิดขึ้นภายใน?**  
Aspose OCR โหลด PNG, ทำการประมวลผลล่วงหน้า (การทำไบนารี, การแก้ไขการเอียง, ฯลฯ), จากนั้นรันเครือข่ายประสาทเทียม OCR บน GPU วัตถุผลลัพธ์จะมีข้อความดิบพร้อมคะแนนความเชื่อมั่นสำหรับแต่ละบรรทัด

---

## สกัดข้อความจาก PNG ด้วยการจำกัดหน่วยความจำ GPU

หลังจากการจดจำ, การสกัดสตริงธรรมดานั้นง่ายมาก, แต่หลายคนลืมตรวจสอบผลลัพธ์ นี่คือวิธีที่คุณสามารถ **extract text from PNG** อย่างปลอดภัยและแสดงผล

```java
// Step 7: Output the recognized text
System.out.println("Recognized text:");
System.out.println(ocrResult.getText());
```

**ผลลัพธ์ที่คาดหวัง (ตัวอย่าง):**

```
Recognized text:
Invoice #12345
Date: 2026-02-09
Total: $1,250.00
Thank you for your business!
```

หากภาพมีสัญญาณรบกวนหรือฟอนต์แปลกประหลาด, คุณอาจเห็นอักขระผิดรูป ในกรณีนั้นให้พิจารณาปรับตัวเลือกการประมวลผลล่วงหน้า (เช่น `config.setLanguage(Language.ENGLISH)` หรือ `config.setAutoSkewCorrection(true)`)

---

## ตัวอย่างเต็มที่สามารถรันได้

ด้านล่างเป็นโปรแกรม Java ฉบับเต็มที่รวมทุกอย่างเข้าด้วยกัน คัดลอกและวางลงในไฟล์ชื่อ `GpuExample.java`, ปรับเส้นทางภาพ, แล้วรันด้วย `javac`/`java` หรือจาก IDE ของคุณ

```java
import com.aspose.ocr.*;
import com.aspose.ocr.configuration.*;

public class GpuExample {
    public static void main(String[] args) throws Exception {

        // Step 1: Specify the image to be processed
        ImageRecognitionResult imageInfo = new ImageRecognitionResult();
        imageInfo.setImagePath("YOUR_DIRECTORY/sample1.png");

        // Step 2: Create the OCR engine and enable GPU processing
        OcrEngine ocrEngine = new OcrEngine();
        OcrEngineConfiguration config = ocrEngine.getConfiguration();

        // Step 3: Set processing mode to GPU (requires CUDA driver)
        config.setProcessingMode(ProcessingMode.GPU);

        // Step 4 (optional): Limit GPU memory usage to 1024 MB
        config.setGpuMemoryLimit(1024);

        // Step 5: Perform recognition
        RecognitionResult ocrResult = ocrEngine.recognize(imageInfo);

        // Step 6: Print the extracted text
        System.out.println("Recognized text:");
        System.out.println(ocrResult.getText());
    }
}
```

**การรันโปรแกรม**

```bash
javac -cp "path/to/aspose-ocr.jar" GpuExample.java
java -cp ".:path/to/aspose-ocr.jar" GpuExample
```

ตรวจสอบให้แน่ใจว่า JAR อยู่ใน classpath; มิฉะนั้นคุณจะได้รับ `ClassNotFoundException`.

---

## เคล็ดลับระดับมืออาชีพ & ข้อผิดพลาดทั่วไป

- **เวอร์ชันไดรเวอร์ GPU:** ธง `ProcessingMode.GPU` จะโยนข้อยกเว้นหากไดรเวอร์ CUDA หายไปหรือไม่เข้ากัน ตรวจสอบอีกครั้งด้วย `nvidia-smi`.
- **การจัดสรรหน่วยความจำ:** หากคุณประมวลผลหลายภาพพร้อมกัน, เพิ่มค่าของ `setGpuMemoryLimit` หรือรันงานแบบต่อเนื่องเพื่อหลีกเลี่ยงข้อผิดพลาด out‑of‑memory.
- **รูปแบบภาพ:** แม้ PNG จะทำงานได้ดี, JPEG ที่บีบอัดสูงอาจทำให้เกิดข้อผิดพลาดในการจดจำ พิจารณาแปลงเป็น PNG แบบไม่มีการสูญเสียก่อน OCR.
- **การสนับสนุนภาษา:** โดยค่าเริ่มต้น Aspose OCR สมมติเป็นภาษาอังกฤษ สำหรับภาษาอื่น, เรียก `config.setLanguage(Language.SPANISH)` (หรือ enum ที่เหมาะสม) ก่อน `recognize`.
- **การทดสอบประสิทธิภาพ:** รันเบนช์มาร์คอย่างรวดเร็ว (`System.nanoTime()`) กับและโดยไม่มี GPU เพื่อยืนยันว่าการเร่งความเร็วคุ้มค่ากับความซับซ้อนที่เพิ่มขึ้น

---

## คำถามที่พบบ่อย

**ทำงานบน macOS หรือ Linux ได้หรือไม่?**  
ใช่—Aspose OCR รองรับหลายแพลตฟอร์ม เพียงตรวจสอบว่าคุณมี GPU ที่รองรับ CUDA และไดรเวอร์ที่เหมาะสมติดตั้งบน OS ของคุณ

**ถ้าฉันไม่มี GPU จะทำอย่างไร?**  
คุณสามารถละเว้นบรรทัด `setProcessingMode(ProcessingMode.GPU)` ได้; เอนจินจะกลับไปใช้โหมด CPU โดยอัตโนมัติ

**ฉันสามารถประมวลผล PDF โดยตรงได้หรือไม่?**  
Aspose OCR มุ่งเน้นที่ภาพเรสเตอร์ สำหรับ PDF ให้แยกแต่ละหน้าเป็นภาพก่อน (เช่น ใช้ Aspose PDF) แล้วจึงส่ง PNG เข้าไปในกระบวนการ OCR

---

## สรุป

โดยสรุป, **how to use OCR** กับ Aspose ใน Java สรุปได้เป็นสามขั้นตอนชัดเจน: ตั้งค่าเอนจิน (รวมถึง **how to set mode** และ **set GPU memory limit**), ชี้ไปที่ PNG ของคุณ, แล้วอ่านสตริงผลลัพธ์ โค้ดตัวอย่างข้างบนเป็นโซลูชันที่ทำงานเต็มรูปแบบจากต้นจนจบที่คุณสามารถนำไปใช้ในโปรเจกต์ Java ใดก็ได้

ตอนนี้คุณได้เชี่ยวชาญ **recognize text from image** และ **extract text from PNG** แล้ว, คุณสามารถขยายเวิร์กโฟลว์: ประมวลผลโฟลเดอร์เป็นชุด, เก็บผลลัพธ์ในฐานข้อมูล, หรือแม้กระทั่งป้อนข้อความเข้าสู่สายงาน NLP ต่อไปได้ ไม่จำกัดอะไร—แค่จำไว้ว่าให้ตรวจสอบหน่วยความจำ GPU และความเข้ากันของไดรเวอร์

มีคำถามเพิ่มเติมเกี่ยวกับ OCR, การเร่งด้วย GPU, หรือฟีเจอร์ของ Aspose หรือไม่? อย่าลังเลที่จะฝากคอมเมนต์หรือสำรวจเอกสารอย่างเป็นทางการของ Aspose OCR เพื่อดูตัวเลือกการปรับแต่งเพิ่มเติม ขอให้เขียนโค้ดอย่างสนุกสนาน! 🚀

![how to use ocr diagram](https://example.com/images/ocr-gpu-diagram.png "how to use ocr diagram")

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}