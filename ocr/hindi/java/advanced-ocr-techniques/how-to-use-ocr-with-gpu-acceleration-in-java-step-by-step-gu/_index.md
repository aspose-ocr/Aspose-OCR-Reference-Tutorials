---
category: general
date: 2026-02-09
description: Aspose OCR के साथ OCR को जल्दी से कैसे उपयोग करें, छवि से टेक्स्ट पहचानें
  और PNG से टेक्स्ट निकालें, साथ ही मोड और GPU मेमोरी सीमा सेट करें।
draft: false
keywords:
- how to use ocr
- recognize text from image
- extract text from png
- how to set mode
- set gpu memory limit
language: hi
og_description: OCR को प्रभावी ढंग से कैसे उपयोग करें – छवि से टेक्स्ट पहचानना सीखें,
  PNG से टेक्स्ट निकालें, मोड सेट करें, और जावा में GPU मेमोरी सीमा को नियंत्रित करें।
og_title: जावा में GPU त्वरण के साथ OCR का उपयोग कैसे करें
tags:
- OCR
- Java
- GPU
- Aspose
title: जावा में GPU एक्सेलेरेशन के साथ OCR का उपयोग कैसे करें – चरण‑दर‑चरण गाइड
url: /hi/java/advanced-ocr-techniques/how-to-use-ocr-with-gpu-acceleration-in-java-step-by-step-gu/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# Java में GPU एक्सेलेरेशन के साथ OCR कैसे उपयोग करें – पूर्ण प्रोग्रामिंग ट्यूटोरियल

क्या आपने कभी सोचा है **OCR का उपयोग करके** तस्वीर से टेक्स्ट निकालने के लिए लाखों लाइनों का कोड लिखे बिना? आप अकेले नहीं हैं। कई प्रोजेक्ट्स—इनवॉइस स्कैनिंग, रसीद प्रोसेसिंग, या सिर्फ पुरानी दस्तावेज़ों को डिजिटल बनाना—में डेवलपर्स को **इमेज फ़ाइलों से टेक्स्ट पहचानने** का भरोसेमंद तरीका चाहिए, ख़ासकर PNG फ़ाइलों के लिए जो अक्सर साफ़, हाई‑रेज़ोल्यूशन ग्राफ़िक्स रखती हैं।

अच्छी खबर? Aspose OCR इसे एक आसान काम बना देता है, और कुछ कॉन्फ़िगरेशन बदलावों के साथ आप भारी काम को अपने GPU को सौंप सकते हैं। इस ट्यूटोरियल में हम पूरी प्रक्रिया को कवर करेंगे: PNG लोड करना, **GPU प्रोसेसिंग के लिए मोड सेट करना**, **GPU मेमोरी लिमिट सेट करना**, और अंत में निकाले गए टेक्स्ट को प्रिंट करना। अंत तक आपके पास एक रन करने योग्य Java प्रोग्राम होगा जो ठीक वही करेगा जिसकी आपको ज़रूरत है।

## आप क्या सीखेंगे

- Aspose OCR for Java को कैसे इंस्टॉल और इम्पोर्ट करें।
- लाइब्रेरी का उपयोग करके **इमेज फ़ाइलों से टेक्स्ट पहचानना**।
- **PNG से टेक्स्ट निकालना** प्रभावी तरीके से।
- **GPU मोड सेट करना** और **GPU मेमोरी लिमिट सेट करना** के साथ मेमोरी फुटप्रिंट को नियंत्रित करना।
- वास्तविक उपयोग में आम समस्याएँ और टिप्स।

### पूर्वापेक्षाएँ

- Java 8 या नया (कोड JDK 11 पर भी कंपाइल होता है)।
- NVIDIA GPU जिसमें CUDA‑संगत ड्राइवर हो, यदि आप GPU एक्सेलेरेशन चाहते हैं।
- Aspose OCR for Java JAR (Aspose साइट से डाउनलोड करें या Maven/Gradle के ज़रिए जोड़ें)।
- एक सैंपल PNG इमेज (जैसे `sample1.png`) जिसे आप रेफ़रेंस फ़ोल्डर में रख सकते हैं।

---

## OCR कैसे उपयोग करें – GPU मोड सक्षम करें

सबसे पहले आपको Aspose OCR को बताना होगा कि आप इसे CPU की बजाय GPU पर चलाना चाहते हैं। यही वह जगह है जहाँ **how to set mode** कीवर्ड काम आती है।

```java
// Step 1: Create the OCR engine
OcrEngine ocrEngine = new OcrEngine();

// Step 2: Grab the configuration object
OcrEngineConfiguration config = ocrEngine.getConfiguration();

// Step 3: Switch processing mode to GPU
config.setProcessingMode(ProcessingMode.GPU);   // requires a CUDA‑compatible driver

// (Optional) Step 4: Limit GPU memory usage to 1024 MB
config.setGpuMemoryLimit(1024);                 // set gpu memory limit (MB)
```

**यह क्यों महत्वपूर्ण है:**  
GPU प्रोसेसिंग बड़े बैच या हाई‑रेज़ोल्यूशन इमेज के लिए बहुत तेज़ हो सकती है, लेकिन यह वीडियो मेमोरी भी खपत करती है। `setGpuMemoryLimit` को कॉल करके आप अपने एप्लिकेशन को पूरे GPU को हॉग करने से रोकते हैं, जो तब ज़रूरी होता है जब वही डिवाइस अन्य वर्कलोड (जैसे UI या मशीन‑लर्निंग मॉडल) चलाता है।

---

## Aspose OCR का उपयोग करके इमेज से टेक्स्ट पहचानें

इंजन कॉन्फ़िगर हो जाने के बाद, हमें उस फ़ाइल की ओर इशारा करना है जिसे हम पढ़ना चाहते हैं। यही **recognize text from image** का मुख्य भाग है।

```java
// Step 5: Define the image to be processed
ImageRecognitionResult imageInfo = new ImageRecognitionResult();
imageInfo.setImagePath("YOUR_DIRECTORY/sample1.png");

// Step 6: Run the OCR operation
RecognitionResult ocrResult = ocrEngine.recognize(imageInfo);
```

**आंतरिक रूप से क्या होता है?**  
Aspose OCR PNG को लोड करता है, प्री‑प्रोसेस करता है (बाइनराइज़ेशन, डेस्क्यू आदि), फिर GPU पर OCR न्यूरल नेटवर्क चलाता है। परिणाम ऑब्जेक्ट में कच्चा टेक्स्ट और प्रत्येक लाइन के लिए कॉन्फिडेंस स्कोर होते हैं।

---

## GPU मेमोरी लिमिट के साथ PNG से टेक्स्ट निकालें

पहचान के बाद, साधारण स्ट्रिंग निकालना बहुत आसान है, फिर भी कई डेवलपर्स आउटपुट को वेरिफ़ाई करना भूल जाते हैं। यहाँ आप सुरक्षित रूप से **PNG से टेक्स्ट निकाल सकते** हैं और प्रदर्शित कर सकते हैं।

```java
// Step 7: Output the recognized text
System.out.println("Recognized text:");
System.out.println(ocrResult.getText());
```

**अपेक्षित आउटपुट (उदाहरण):**

```
Recognized text:
Invoice #12345
Date: 2026-02-09
Total: $1,250.00
Thank you for your business!
```

यदि इमेज में शोर या असामान्य फ़ॉन्ट हैं, तो आपको गड़बड़ अक्षर दिख सकते हैं। ऐसे में प्री‑प्रोसेसिंग विकल्पों को समायोजित करें (जैसे `config.setLanguage(Language.ENGLISH)` या `config.setAutoSkewCorrection(true)`)।

---

## पूर्ण, रन करने योग्य उदाहरण

नीचे पूरा Java प्रोग्राम दिया गया है जो सभी चरणों को जोड़ता है। इसे `GpuExample.java` नाम की फ़ाइल में कॉपी‑पेस्ट करें, इमेज पाथ को समायोजित करें, और `javac`/`java` या अपने IDE से चलाएँ।

```java
import com.aspose.ocr.*;
import com.aspose.ocr.configuration.*;

public class GpuExample {
    public static void main(String[] args) throws Exception {

        // Step 1: Specify the image to be processed
        ImageRecognitionResult imageInfo = new ImageRecognitionResult();
        imageInfo.setImagePath("YOUR_DIRECTORY/sample1.png");

        // Step 2: Create the OCR engine and enable GPU processing
        OcrEngine ocrEngine = new OcrEngine();
        OcrEngineConfiguration config = ocrEngine.getConfiguration();

        // Step 3: Set processing mode to GPU (requires CUDA driver)
        config.setProcessingMode(ProcessingMode.GPU);

        // Step 4 (optional): Limit GPU memory usage to 1024 MB
        config.setGpuMemoryLimit(1024);

        // Step 5: Perform recognition
        RecognitionResult ocrResult = ocrEngine.recognize(imageInfo);

        // Step 6: Print the extracted text
        System.out.println("Recognized text:");
        System.out.println(ocrResult.getText());
    }
}
```

**प्रोग्राम चलाना**

```bash
javac -cp "path/to/aspose-ocr.jar" GpuExample.java
java -cp ".:path/to/aspose-ocr.jar" GpuExample
```

सुनिश्चित करें कि JAR आपके क्लासपाथ में है; अन्यथा आपको `ClassNotFoundException` मिलेगा।

---

## प्रो टिप्स और सामान्य समस्याएँ

- **GPU ड्राइवर संस्करण:** `ProcessingMode.GPU` फ़्लैग तब एक्सेप्शन फेंकेगा जब CUDA ड्राइवर गायब या असंगत हो। `nvidia-smi` से डबल‑चेक करें।
- **मेमोरी बजटिंग:** यदि आप कई इमेज एक साथ प्रोसेस करते हैं, तो `setGpuMemoryLimit` का मान बढ़ाएँ या मेमोरी‑ओवरफ़्लो से बचने के लिए जॉब्स को क्रमिक चलाएँ।
- **इमेज फ़ॉर्मेट:** PNG बहुत अच्छा काम करता है, लेकिन हाई कॉम्प्रेशन वाले JPEG में पहचान त्रुटियाँ आ सकती हैं। OCR से पहले उन्हें लॉसलेस PNG में बदलने पर विचार करें।
- **भाषा समर्थन:** डिफ़ॉल्ट रूप से Aspose OCR अंग्रेज़ी मानता है। अन्य भाषाओं के लिए `config.setLanguage(Language.SPANISH)` (या उपयुक्त एन्‍युम) को `recognize` से पहले कॉल करें।
- **परफ़ॉर्मेंस टेस्टिंग:** `System.nanoTime()` के साथ GPU और CPU दोनों मोड में बेंचमार्क चलाएँ ताकि गति में सुधार की पुष्टि हो सके।

---

## अक्सर पूछे जाने वाले प्रश्न

**क्या यह macOS या Linux पर काम करता है?**  
हाँ—Aspose OCR क्रॉस‑प्लेटफ़ॉर्म है। बस सुनिश्चित करें कि आपके OS के लिए CUDA‑संगत GPU और उचित ड्राइवर इंस्टॉल हो।

**अगर मेरे पास GPU नहीं है तो?**  
आप बस `setProcessingMode(ProcessingMode.GPU)` लाइन को हटा दें; इंजन स्वचालित रूप से CPU मोड पर फ़ॉल्बैक कर देगा।

**क्या मैं सीधे PDFs प्रोसेस कर सकता हूँ?**  
Aspose OCR रास्टर इमेज पर केंद्रित है। PDFs के लिए पहले प्रत्येक पेज को इमेज (जैसे Aspose PDF से) में बदलें, फिर PNG को OCR फ़्लो में फीड करें।

---

## निष्कर्ष

संक्षेप में, Aspose के साथ Java में **OCR का उपयोग कैसे करें** तीन स्पष्ट चरणों में संकलित होता है: इंजन कॉन्फ़िगर करें (जिसमें **how to set mode** और **set GPU memory limit** शामिल हैं), अपने PNG को पॉइंट करें, और परिणामी स्ट्रिंग पढ़ें। ऊपर दिया गया स्निपेट एक पूरी तरह कार्यशील, एंड‑टू‑एंड समाधान है जिसे आप किसी भी Java प्रोजेक्ट में डाल सकते हैं।

अब जब आप **इमेज से टेक्स्ट पहचानना** और **PNG से टेक्स्ट निकालना** में निपुण हो गए हैं, तो वर्कफ़्लो को विस्तारित करें: फ़ोल्डर‑बैच प्रोसेसिंग, परिणामों को डेटाबेस में स्टोर करना, या टेक्स्ट को डाउनस्ट्रीम NLP पाइपलाइन में फीड करना। संभावनाएँ असीमित हैं—बस GPU मेमोरी और ड्राइवर संगतता पर ध्यान रखें।

OCR, GPU एक्सेलेरेशन, या Aspose फीचर्स के बारे में और सवाल हैं? टिप्पणी छोड़ें या आधिकारिक Aspose OCR डॉक्यूमेंटेशन में गहरी कस्टमाइज़ेशन विकल्पों के लिए देखें। Happy coding! 🚀

![how to use ocr diagram](https://example.com/images/ocr-gpu-diagram.png "how to use ocr diagram")

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}