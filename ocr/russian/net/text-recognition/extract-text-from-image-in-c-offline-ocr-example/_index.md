---
category: general
date: 2026-02-09
description: Извлечение текста из изображения с помощью офлайн OCR на C#. Полный пример
  OCR на C# показывает, как загрузить изображение для OCR, распознать кириллический
  текст и извлечь текст из паспорта.
draft: false
keywords:
- extract text from image
- c# ocr example
- load image for ocr
- recognize cyrillic text
- recognize text from passport
language: ru
og_description: Извлеките текст из изображения с помощью офлайн OCR на C#. Изучите
  пошаговый пример OCR на C#, который загружает изображение для OCR, распознаёт кириллический
  текст и извлекает текст из паспорта.
og_title: Извлечение текста из изображения в C# – Руководство по офлайн OCR
tags:
- OCR
- C#
- Aspose
title: Извлечение текста из изображения в C# – пример офлайн OCR
url: /ru/net/text-recognition/extract-text-from-image-in-c-offline-ocr-example/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# Извлечение текста из изображения в C# – Пример офлайн OCR

Когда‑то вам нужно **извлечь текст из изображения**, но вы зависаете из‑за сетевых API? Вы не одиноки. Многие разработчики сталкиваются с тем, что сервис OCR пытается загрузить языковые пакеты во время выполнения, особенно в ограниченных средах.

В этом руководстве мы пройдём через **c# ocr example**, который работает полностью офлайн, загружает изображение для OCR и распознаёт кириллический текст из паспорта. К концу вы получите готовую к запуску программу, которая выводит обычный текст любого поддерживаемого изображения прямо в консоль.

## Что вы узнаете

- Как настроить Aspose.OCR для офлайн‑обработки.  
- Точный код для **load image for OCR** с диска.  
- Как сконфигурировать движок для **recognize cyrillic text**.  
- Полный, готовый к копированию **c# ocr example**, который извлекает текст из фотографии в стиле паспорта.  

Опыт работы с Aspose не требуется; достаточно .NET 6 (или новее) SDK и Visual Studio 2022 (или VS Code).

---

![Извлечение текста из изображения с помощью Aspose OCR на фотографии паспорта](/images/ocr-passport.jpg "извлечение текста из изображения")

## Шаг 1: Настройка проекта для извлечения текста из изображения

Прежде чем писать код, убедитесь, что пакет Aspose.OCR добавлен в ваш проект через NuGet:

```bash
dotnet add package Aspose.OCR
```

> **Pro tip:** Используйте флаг `--version`, чтобы зафиксировать последнюю стабильную версию (например, `13.9.0`). Это гарантирует совместимость с .NET 6.

Создать новое консольное приложение так же просто:

```bash
dotnet new console -n OfflineOcrDemo
cd OfflineOcrDemo
```

Теперь у вас чистый шаблон, где мы будем **extract text from image** без обращения к интернету.

## Шаг 2: Load Image for OCR – чтение фотографии паспорта

Первому движку OCR нужен bitmap или поток, представляющий изображение. В нашем случае мы **load image for OCR** из локального файла `cyrillic_passport.jpg`.

```csharp
using Aspose.OCR;
using Aspose.OCR.Models;
using System;

// Step 2: Load the image file (this is the “load image for ocr” part)
var imagePath = @"YOUR_DIRECTORY\cyrillic_passport.jpg";

// Validate the file exists – helpful when the path is wrong.
if (!System.IO.File.Exists(imagePath))
{
    Console.WriteLine($"❌ Image not found at {imagePath}");
    return;
}

// ImageStream abstracts the underlying format; it works with JPEG, PNG, etc.
var image = ImageStream.FromFile(imagePath);
```

> **Why this matters:** Передача потока вместо сырого `Bitmap` позволяет Aspose самостоятельно определить формат, уменьшая количество шаблонного кода и потенциальных ошибок.

## Шаг 3: Конфигурация офлайн‑режима и выбор кириллического языка

Aspose.OCR может загружать языковые модели «на лету», но это противоречит цели офлайн‑решения. Отключите сетевые вызовы и явно укажите движку, какой язык использовать.

```csharp
// Step 3: Create the OCR engine and switch to offline mode
var ocrEngine = new OcrEngine
{
    Configuration =
    {
        OfflineMode = true,               // No network traffic – perfect for secure environments
        Language = new[] { OcrLanguage.Cyrillic } // We want to **recognize cyrillic text**
    }
};
```

> **Edge case:** Если позже понадобится распознавать латинские символы в том же документе, просто добавьте `OcrLanguage.English` в массив. Движок автоматически обработает многоязычное обнаружение.

## Шаг 4: Запуск OCR‑движка и распознавание кириллического текста

Теперь мы действительно **recognize text from passport**‑style images. Метод `Recognize` возвращает богатый объект результата, содержащий обычный текст, оценки уверенности и ограничивающие рамки.

```csharp
// Step 4: Perform the OCR operation
OcrResult result = ocrEngine.Recognize(image);

// Step 5: Output the plain text – this is where we finally **extract text from image**
Console.WriteLine("📝 Extracted Text:");
Console.WriteLine("-------------------");
Console.WriteLine(result.PlainText);
```

### Ожидаемый вывод в консоль

```
📝 Extracted Text:
-------------------
ПАСПОРТ РФ
Иванов Иван Иванович
01.01.1990
...
```

Если результат выглядит искажённым, проверьте, что исходное изображение чёткое и что пакет языка `OfflineMode` для кириллицы присутствует в папке установки Aspose (обычно `\Aspose.OCR\resources\languages`).

## Полный пример C# OCR – весь исходный код

Ниже представлен **c# ocr example** полностью. Скопируйте его в `Program.cs` и запустите `dotnet run`. Всё, что нужно для **extract text from image**, уже здесь.

```csharp
using Aspose.OCR;
using Aspose.OCR.Models;
using System;

class OfflineExample
{
    static void Main()
    {
        // --------------------------------------------------------------
        // Step 1: Create the OCR engine (offline mode)
        // --------------------------------------------------------------
        var ocrEngine = new OcrEngine
        {
            Configuration =
            {
                OfflineMode = true,                     // No network calls
                Language = new[] { OcrLanguage.Cyrillic } // Recognize Cyrillic text
            }
        };

        // --------------------------------------------------------------
        // Step 2: Load the image for OCR (passport photo)
        // --------------------------------------------------------------
        var imagePath = @"YOUR_DIRECTORY\cyrillic_passport.jpg";

        if (!System.IO.File.Exists(imagePath))
        {
            Console.WriteLine($"❌ Image not found at {imagePath}");
            return;
        }

        var image = ImageStream.FromFile(imagePath);

        // --------------------------------------------------------------
        // Step 3: Recognize the text
        // --------------------------------------------------------------
        var result = ocrEngine.Recognize(image);

        // --------------------------------------------------------------
        // Step 4: Output the plain text (the final extraction)
        // --------------------------------------------------------------
        Console.WriteLine("📝 Extracted Text:");
        Console.WriteLine("-------------------");
        Console.WriteLine(result.PlainText);
    }
}
```

### Запуск примера

```bash
dotnet run
```

Вы должны увидеть, как консоль выводит данные паспорта кириллицей. Это момент, когда вы понимаете, что ваш конвейер **extract text from image** работает.

## Распространённые проблемы и их решения

| Симптом | Вероятная причина | Решение |
|---------|-------------------|---------|
| Empty `PlainText` | Неправильная языковая модель или изображение слишком тёмное | Убедитесь, что язык `OfflineMode` включает `Cyrillic`, и увеличьте контраст изображения |
| `System.DllNotFoundException` | Отсутствуют нативные бинарные файлы Aspose OCR | Переустановите пакет NuGet или скопируйте `Aspose.OCR.Native.dll` в папку вывода |
| Медленная работа на больших изображениях | Движок обрабатывает полное разрешение | Уменьшите размер изображения до ≤ 1500 px по ширине перед передачей в `ImageStream` |
| Искажённые символы | Изображение повернуто неправильно | Используйте `Image.RotateFlip(RotateFlipType.Rotate90FlipNone)` перед созданием потока |

## Следующие шаги – расширение офлайн‑рабочего процесса OCR

- **Load image for OCR** из `MemoryStream`, когда работаете с загруженными файлами в ASP.NET Core.  
- Перейдите к **recognize text from passport** в пакетном режиме, перебирая папку со сканами паспортов.  
- Скомбинируйте результат с **regular expressions**, чтобы извлекать такие поля, как номер паспорта или дата рождения.  
- Поэкспериментируйте с `ocrEngine.Configuration.UseParallelProcessing = true` для ускорения на многопоточном процессоре.

---

### Заключение

Мы только что показали, как **extract text from image** с помощью полностью офлайн‑конвейера OCR на C#. Краткий, самодостаточный **c# ocr example** загружает изображение, конфигурирует движок для **recognize cyrillic text** и выводит извлечённые данные паспорта — без единого сетевого запроса.

Не стесняйтесь менять код, добавлять новые языки или сохранять вывод в базу данных. Возможности безграничны, как только вы освоите основы **load image for OCR** и распознавания текста из фотографии в стиле паспорта.

Есть вопросы или хотите поделиться своими доработками? Оставляйте комментарий ниже, и счастливого кодинга!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}