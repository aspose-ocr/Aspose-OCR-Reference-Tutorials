---
category: general
date: 2025-12-29
description: Как использовать Aspose OCR для преобразования текста изображения и извлечения
  корейского текста. Пошаговое руководство по извлечению текста из изображения и распознаванию
  корейского текста на C#.
draft: false
keywords:
- how to use aspose
- convert image text
- extract text image
- extract korean text
- recognize korean text
language: ru
og_description: Узнайте, как использовать Aspose OCR для преобразования текста на
  изображениях, извлечения корейского текста и распознавания корейского текста на
  фотографиях с полным примером на C#.
og_title: Как использовать Aspose OCR – распознавать корейский текст в C#
tags:
- Aspose
- OCR
- C#
- Image Processing
title: Как использовать Aspose OCR в C# – распознавать корейский текст на изображениях
url: /ru/net/text-recognition/how-to-use-aspose-ocr-in-c-recognize-korean-text-from-images/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# Как использовать Aspose OCR в C# – Распознавание корейского текста на изображениях

Когда‑нибудь задавались вопросом **как использовать Aspose**, чтобы извлечь корейские символы из фотографии? Возможно, у вас есть скриншот уличного знака, отсканированный чек или мем, который нужно превратить в поисковый текст. Хорошая новость в том, что Aspose OCR делает это проще простого, и вам не придётся возиться с низкоуровневыми приёмами обработки изображений.

В этом руководстве мы пройдём через **полный, исполняемый пример**, который покажет, как **конвертировать текст изображения**, **извлекать текст из изображения**, и конкретно **извлекать корейский текст** с помощью библиотеки Aspose OCR. К концу у вас будет консольное приложение, выводящее распознанную корейскую строку, и вы поймёте, почему важна каждая строка.

## Что понадобится

- **.NET 6+** (любой современный .NET SDK подходит – Visual Studio, Rider или `dotnet` CLI)
- **Aspose.OCR for .NET** пакет NuGet  
  ```bash
  dotnet add package Aspose.OCR
  ```
- Файл изображения, содержащий корейские символы (например, `korean_sign.jpg`).  
- Небольшие знания C# – если вы уже писали «Hello World», то всё готово.

> **Совет:** Aspose OCR поддерживает более 50 языков «из коробки». Мы сосредоточимся на корейском, потому что его хангыль часто ставит в тупик обычные OCR‑движки.

## Шаг 1 – Установить и подключить Aspose OCR

Сначала добавьте библиотеку в проект. Команда NuGet выше делает всю тяжёлую работу, но если вы предпочитаете графический интерфейс, просто найдите *Aspose.OCR* в менеджере пакетов NuGet.

```csharp
// No code needed here – the package reference is enough.
// The using directives below will bring the types into scope.
using Aspose.OCR;
using Aspose.OCR.Models;
```

> **Почему это важно:** Операторы `using` дают доступ к `OcrEngine`, `Language` и вспомогательному классу `Image`. Без них компилятор будет ругаться на неизвестные типы.

## Шаг 2 – Загрузить изображение для обработки

Aspose OCR работает со своим обёрткой `Image`, которая может читать JPEG, PNG, BMP и многие другие форматы. Укажите ей файл, содержащий корейский текст.

```csharp
// Step 2: Load the image containing Korean characters
var imagePath = Path.Combine(Environment.CurrentDirectory, "korean_sign.jpg");
var image = Image.Load(imagePath);
```

Если файл находится не в той же папке, что и исполняемый файл, скорректируйте путь соответственно. Вызов `Image.Load` **конвертирует текст изображения** во внутреннее представление, понятное OCR‑движку.

![пример использования aspose OCR](/images/aspose-ocr-korean.png "как использовать aspose OCR для распознавания корейского текста")

*Текст alt изображения: «пример использования aspose OCR, показывающий корейский уличный знак». *

## Шаг 3 – Настроить OCR‑движок для корейского языка

Движку необходимо знать, какой язык искать; иначе он по умолчанию использует английский и пропустит символы хангыль.

```csharp
// Step 3: Create and configure the OCR engine
var ocrEngine = new OcrEngine
{
    // Tell Aspose we want to recognize Korean (Hangul)
    Language = Language.Korean
};
```

> **Почему это важно:** Установка `Language = Language.Korean` заставляет движок загрузить пакет корейского языка, что значительно повышает точность распознавания глифов хангыль. Пропуск этого шага часто приводит к искажённому выводу.

## Шаг 4 – Запустить процесс распознавания

Теперь мы действительно просим Aspose прочитать изображение. Метод `Recognize` возвращает объект `OcrResult`, содержащий извлечённую строку и оценки уверенности.

```csharp
// Step 4: Run OCR on the loaded image
OcrResult ocrResult = ocrEngine.Recognize(image);
```

Если вам нужно **извлечь текст из изображения** из более крупного фото (например, скриншота с несколькими элементами UI), вы можете сначала обрезать интересующую область с помощью `image.Crop(...)` перед вызовом `Recognize`. Это удобный приём, когда важна только определённая часть картинки.

## Шаг 5 – Вывести распознанный корейский текст

Наконец, отобразите результат. В реальном приложении вы можете сохранить его в базе данных или передать в API перевода, но для этого руководства вывод в консоль упрощает задачу.

```csharp
// Step 5: Print the recognized Korean text
Console.WriteLine("Recognized Korean text:");
Console.WriteLine(ocrResult.Text);
```

### Ожидаемый вывод

```
Recognized Korean text:
서울특별시 강남구 테헤란로 123
```

Ваш реальный вывод, конечно, будет соответствовать тем корейским символам, которые находятся в `korean_sign.jpg`.

## Полный рабочий пример

Ниже представлен **полный код программы**, который можно скопировать и вставить в новый консольный проект (`dotnet new console`). Убедитесь, что файл изображения находится рядом с скомпилированным `.exe`, либо скорректируйте путь.

```csharp
using System;
using System.IO;
using Aspose.OCR;
using Aspose.OCR.Models;

class Program
{
    static void Main()
    {
        // 1️⃣ Install Aspose.OCR via NuGet before running this code.

        // 2️⃣ Load the image that contains Korean text.
        var imagePath = Path.Combine(Environment.CurrentDirectory, "korean_sign.jpg");
        var image = Image.Load(imagePath);

        // 3️⃣ Create the OCR engine and set it to recognize Korean.
        var ocrEngine = new OcrEngine
        {
            Language = Language.Korean   // 👈 This enables Hangul support.
        };

        // 4️⃣ Run the OCR process.
        OcrResult ocrResult = ocrEngine.Recognize(image);

        // 5️⃣ Output the extracted Korean string.
        Console.WriteLine("Recognized Korean text:");
        Console.WriteLine(ocrResult.Text);
    }
}
```

Запустите программу командой `dotnet run` и наблюдайте, как корейские символы появляются в консоли.

## Часто задаваемые вопросы и особые случаи

### Что делать, если OCR возвращает искажённые символы?

- **Проверьте настройку языка.** Забвение `Language.Korean` — самая распространённая ошибка.
- **Улучшите качество изображения.** Более чёткие картинки, более высокое DPI и правильное освещение повышают точность.
- **Предобработайте изображение.** Aspose OCR предлагает встроенные фильтры (`image.Binarize()`, `image.Deskew()`), которые могут очистить шумные сканы.

### Можно ли **конвертировать текст изображения** пакетно?

Absolutely. Wrap the steps above in a `foreach` loop that iterates over a folder of images. Here’s a quick snippet:

```csharp
foreach (var file in Directory.GetFiles(@"C:\KoreanImages", "*.jpg"))
{
    var img = Image.Load(file);
    var result = ocrEngine.Recognize(img);
    File.WriteAllText(Path.ChangeExtension(file, ".txt"), result.Text);
}
```

Этот скрипт **извлекает текст из изображения** из каждого файла и записывает рядом файл `.txt`.

### Как обрабатывать несколько языков на одном изображении?

Aspose OCR может автоматически определять язык, если установить `Language = Language.Auto`. Однако автоопределение может быть медленнее и немного менее точным, чем указание конкретного языка. Если вы знаете, что изображение содержит и корейский, и английский, можно выполнить два прохода — сначала с `Language.Korean`, затем с `Language.English` — и объединить результаты.

## Советы для OCR в продакшене

- **Кешируйте OcrEngine.** Создание нового движка для каждого запроса добавляет накладные расходы. Храните его как синглтон, если обрабатываете много изображений.
- **Ограничьте размер изображения.** Большие изображения потребляют память; уменьшайте их до ширины ~1500 px перед передачей в движок.
- **Обрабатывайте исключения.** Оберните вызов `Recognize` в try/catch, чтобы корректно справляться с повреждёнными файлами.

## Заключение

Мы только что рассмотрели **как использовать Aspose** для **конвертации текста изображения**, **извлечения текста из изображения**, и конкретно **извлечения корейского текста** с помощью нескольких строк кода на C#. Шаги просты:

1. Установите Aspose OCR.  
2. Загрузите изображение.  
3. Настройте движок для корейского.  
4. Запустите `Recognize`.  
5. Выведите результат.

Теперь вы можете внедрить этот фрагмент в более крупные рабочие процессы — пакетную обработку, архивирование документов или даже приложения реального времени для перевода. Хотите идти дальше? Попробуйте добавить методы `Image.Preprocess()` от Aspose, поэкспериментировать с разными языками или интегрировать вывод с Azure Cognitive Services для перевода.

Есть дополнительные вопросы о **распознавании корейского текста** или других возможностях Aspose? Оставьте комментарий, и удачной разработки!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}