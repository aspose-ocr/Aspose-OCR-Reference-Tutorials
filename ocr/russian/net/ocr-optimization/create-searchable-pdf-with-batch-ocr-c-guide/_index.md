---
category: general
date: 2025-12-29
description: Создайте поисковый PDF из отсканированных изображений с помощью пакетной
  обработки Aspose OCR. Узнайте, как преобразовать изображения в PDF, подготовить
  их к OCR и выпрямить отсканированные документы.
draft: false
keywords:
- create searchable pdf
- batch ocr processing
- convert images to pdf
- preprocess images for ocr
- deskew scanned documents
language: ru
og_description: Создайте поисковый PDF из отсканированных изображений с помощью пакетной
  обработки OCR от Aspose. Узнайте, как конвертировать изображения в PDF, предобрабатывать
  их для OCR и исправлять наклон сканированных документов.
og_title: Создание поискового PDF с пакетным OCR – руководство по C#
tags:
- OCR
- C#
- PDF/A
- Aspose
title: Создание поискового PDF с пакетным OCR — руководство по C#
url: /ru/net/ocr-optimization/create-searchable-pdf-with-batch-ocr-c-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# Создание поискового PDF с пакетным OCR – Руководство C#

Когда‑то вам нужно **создать поисковые PDF**‑файлы из огромного количества отсканированных изображений, но вы застряли уже на первом шаге? Вы не одиноки — большинство разработчиков сталкиваются с теми же проблемами: плохие сканы, неровные страницы или просто массовая конверсия.  

Хорошие новости? С Aspose OCR вы можете построить **пакетный процесс OCR**, который не только **конвертирует изображения в PDF**, но и **предобрабатывает изображения для OCR** и даже **выравнивает отсканированные документы** автоматически. В этом руководстве мы пройдем весь процесс от настройки движка до полировки результата, чтобы вы могли запустить его на папке с файлами и получить готовые PDF/A‑2b, пригодные для поиска.

> **Что вы получите:** единое, готовое к запуску консольное приложение C#, которое принимает каталог изображений (или PDF), очищает каждую страницу, запускает OCR и сохраняет поисковый PDF/A‑2b рядом с исходником. Никаких разрозненных фрагментов, только одно цельное решение.

---

## Требования

- .NET 6 SDK или новее (код также компилируется под .NET Core).  
- NuGet‑пакет Aspose OCR (`Aspose.OCR`).  
- Папка со сканированными изображениями (TIFF, JPEG, PNG) или PDF, которые нужно превратить в поисковые PDF.  
- (Опционально) настоящая лицензия — в режиме триала будет добавлен водяной знак, но для тестов этого достаточно.

Если всё готово, приступим.

---

## Обзор — Как весь конвейер создаёт поисковый PDF

1. **Активировать режим триала** (или загрузить вашу лицензию).  
2. **Настроить `OcrBatchProcessor`** — указать, откуда читать файлы, куда писать PDF, какой формат использовать и сколько потоков запускать параллельно.  
3. **Предобработать каждое изображение** — выравнивание, удаление шума и фона, чтобы OCR‑движок видел чистую страницу.  
4. **Запустить пакет** — Aspose обрабатывает каждый файл, выполняет OCR и записывает поисковый PDF/A‑2b.  
5. **Уведомить о завершении** — простое сообщение в консоли, но при желании можно подключить логгер или webhook.

Это высокоуровневый поток. Приведённый ниже код реализует каждый шаг с множеством комментариев, так что вы сможете изменить любую часть, не ломая всё приложение.

---

## Шаг 1 — Активировать режим триала (или загрузить лицензию)

Прежде чем вы сможете вызвать любой класс Aspose, нужно сообщить библиотеке, что у вас есть лицензия. Для быстрых экспериментов режима триала достаточно.

```csharp
using Aspose.OCR;
using Aspose.OCR.Models;
using Aspose.OCR.Filters;
using Aspose.OCR.Batch;

// Activate trial mode – replace with OcrEngine.SetLicense("YourLicenseFile.lic") for production
OcrEngine.EnableTrialMode();
```

> **Pro tip:** разместите активацию лицензии в самом верху `Program.cs`. Если забыть, движок бросит исключение при первом вызове `Process()`.

---

## Шаг 2 — Настроить движок пакетного OCR

Здесь мы создаём объект **пакетной обработки OCR**. Обратите внимание, что `InputFolder` и `OutputFolder` в этом примере одинаковы, но при желании их можно разделить.

```csharp
// Define where your source images live and where the searchable PDFs should be saved
var ocrBatch = new OcrBatchProcessor
{
    // Folder that contains the images or PDFs to be processed
    InputFolder = @"C:\Scans\Incoming",

    // Folder where searchable PDF/A‑2b files will be saved
    OutputFolder = @"C:\Scans\Processed",

    // Choose the output format – searchable PDF/A‑2b (perfect for archiving)
    OutputFormat = SaveFormat.SearchablePdf,

    // Limit the number of concurrent OCR operations to avoid CPU spikes
    MaxDegreeOfParallelism = 3,

    // Pre‑process each image: deskew, denoise, and remove background
    Preprocess = img => ImageFilters
                            .Deskew(img)          // fixes rotated pages
                            .Denoise()            // reduces speckles
                            .RemoveBackground()   // clears colored backgrounds
};
```

### Почему эти настройки важны

- **`MaxDegreeOfParallelism`**: Слишком много OCR‑потоков может перегрузить процессор, особенно на скромных рабочих станциях. Три потока — оптимальный вариант для большинства ноутбуков с четырёхъядерным процессором.  
- **Конвейер `Preprocess`**: Три фильтра вместе значительно повышают точность OCR. Выравнивание исправляет типичную проблему «наклонного скана», удаление шума убирает случайные артефакты, а удаление фона гарантирует, что движок видит только чёрный текст на белом фоне.  
- **`SaveFormat.SearchablePdf`**: Создаёт файлы PDF/A‑2b, которые одновременно подходят для архивирования и поиска — требование многих нормативных стандартов.

---

## Шаг 3 — Запустить пакет и наблюдать за магией

Запуск пакета сводится к вызову `Process()`. Метод блокирует выполнение, пока не обработает все файлы, затем возвращается. Если нужен прогресс, можно подписаться на событие `ProgressChanged` (не показано здесь).

```csharp
// Start processing – this will walk through every file in InputFolder
ocrBatch.Process();

// Let the user (or calling script) know we’re finished
Console.WriteLine("All files processed. Searchable PDFs are ready.");
```

Когда консоль выведет последнюю строку, в папке `C:\Scans\Processed` появится поисковый PDF для каждого входного изображения. Откройте любой из них в Adobe Reader, нажмите **Ctrl+F**, и вы сможете искать текст, только что извлечённый из скана.

---

## Шаг 4 — Полная готовая к запуску программа (копировать‑вставить)

Ниже представлен **полный, автономный** код, который можно поместить в новый консольный проект (`dotnet new console`). Сначала добавьте NuGet‑пакет Aspose.OCR (`dotnet add package Aspose.OCR`).

```csharp
// Program.cs
using Aspose.OCR;
using Aspose.OCR.Models;
using Aspose.OCR.Filters;
using Aspose.OCR.Batch;

namespace CreateSearchablePdfDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            // 1️⃣ Activate trial mode (replace with real license for production)
            OcrEngine.EnableTrialMode();

            // 2️⃣ Configure batch OCR processing
            var ocrBatch = new OcrBatchProcessor
            {
                InputFolder = @"C:\Scans\Incoming",   // 👉 change to your source folder
                OutputFolder = @"C:\Scans\Processed", // 👉 change to your target folder
                OutputFormat = SaveFormat.SearchablePdf,
                MaxDegreeOfParallelism = 3,
                Preprocess = img => ImageFilters
                                        .Deskew(img)          // fixes rotated pages
                                        .Denoise()            // cleans up noise
                                        .RemoveBackground()   // strips colored backgrounds
            };

            // 3️⃣ Run the batch
            ocrBatch.Process();

            // 4️⃣ Notify completion
            Console.WriteLine("All files processed. Searchable PDFs are ready.");
        }
    }
}
```

### Ожидаемый вывод

```
All files processed. Searchable PDFs are ready.
```

После выполнения перейдите в `C:\Scans\Processed` — вы увидите набор файлов `.pdf`, каждый из которых поисковый и соответствует стандарту PDF/A‑2b. Откройте любой файл, введите слово, которое точно есть в оригинальном скане, и вуаля, текст будет найден.

---

## Часто задаваемые вопросы и обработка граничных случаев

### Что если в моей исходной папке уже есть PDF?

Aspose OCR умеет напрямую принимать PDF; он растеризует каждую страницу, применит те же **фильтры предобработки** и внедрит слой OCR. Дополнительный код не требуется.

### Как изменить формат вывода на обычный PDF (не‑поисковый)?

Замените `SaveFormat.SearchablePdf` на `SaveFormat.Pdf`. Вы потеряете слой поискового текста, но визуальная точность останется.

### Мои сканы цветные — повлияет ли удаление фона?

`RemoveBackground()` удаляет не‑белый фон, сохраняя основной текст. Если нужно оставить цветные графики, можно опустить этот фильтр:

```csharp
.Preprocess = img => ImageFilters.Deskew(img).Denoise()
```

### Я запускаю процесс на сервере с ограниченной ОЗУ — можно ли уменьшить количество потоков?

Конечно. Установите `MaxDegreeOfParallelism` в `1` или `2`. Пакет будет работать дольше, но потребление памяти останется низким.

---

## Визуальное резюме (по желанию)

Если вам нужен быстрый обзор, представьте следующую схему:

![Создание поискового PDF workflow – показывает входную папку → предобработку → OCR → вывод поискового PDF](/images/ocr-workflow.png)

*Текст альтернативного изображения:* **Схема создания поискового PDF** — иллюстрирует пакетную обработку OCR, конверсию и шаги выравнивания.

---

## Заключение

Теперь у вас есть **полное, готовое к продакшену** решение для **создания поисковых PDF** из любой партии сканированных изображений. Используя **пакетную обработку OCR**, вы можете **конвертировать изображения в PDF**, **предобрабатывать изображения для OCR** и автоматически **выравнивать сканированные документы** — всё это несколькими строками C#.

Что дальше? Попробуйте добавить собственную схему именования, подключить систему логирования для захвата оценок уверенности OCR или поэкспериментировать с другими `ImageFilters`, например `Sharpen()` для слабого текста. API Aspose OCR достаточно гибок, чтобы расти вместе с вашими потребностями.

Счастливого кодинга, и пусть ваши PDF всегда будут поисковыми!

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}