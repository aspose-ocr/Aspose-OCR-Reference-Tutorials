---
category: general
date: 2026-01-02
description: Изучите, как создать конвейер предобработки OCR, который автоматически
  исправляет наклон изображения, подготавливает его для OCR и считывает текст из JPG
  с помощью Aspose.OCR — пошаговое руководство.
draft: false
keywords:
- ocr preprocessing pipeline
- recognize text from image
- auto deskew image
- preprocess image for ocr
- read text from jpg
language: ru
og_description: Откройте для себя конвейер предобработки OCR, который автоматически
  исправляет наклон изображений и позволяет распознавать текст из файлов изображений,
  таких как JPG. Полный код, объяснения и советы.
og_title: Конвейер предобработки OCR — Полное руководство по C#
tags:
- OCR
- C#
- Image Processing
title: Конвейер предобработки OCR – Как распознать текст на изображении в C#
url: /ru/net/ocr-optimization/ocr-preprocessing-pipeline-how-to-recognize-text-from-image/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# ocr preprocessing pipeline – Полное руководство C#

Когда‑то вам приходилось **распознавать текст из файлов‑изображений**, которые наклонены, шумные или просто трудно читаемы? Вы не одиноки. Во многих реальных проектах необработанное фото, полученное со сканера или камеры телефона, нуждается в небольшом уходе, прежде чем OCR‑движок сможет выполнить свою работу.  

Именно здесь вступает в действие **ocr preprocessing pipeline**. Автоматически исправляя наклон изображения, уменьшая фоновые пятна и в целом очищая его, вы значительно повышаете точность. В этом руководстве мы пройдём через полностью рабочий пример, который **предобрабатывает изображение для OCR**, автоматически исправляет наклон и, наконец, **читает текст из jpg** с помощью Aspose.OCR.

> **Что вы получите:** готовое к запуску консольное приложение C#, которое загружает наклонённый, шумный JPG, пропускает его через умный pipeline предобработки и выводит извлечённый текст в консоль.

## Требования

- .NET 6 SDK или новее (код также компилируется с .NET Core)
- Visual Studio 2022 или любой другой IDE
- NuGet‑пакет Aspose.OCR (`Install-Package Aspose.OCR`)
- Пример изображения, например `skewed_noisy.jpg`, размещённый в папке, к которой вы можете обратиться

Никаких дополнительных внешних библиотек не требуется; всё остальное находится внутри Aspose.OCR.

---

## Шаг 1 – Создание проекта и загрузка изображения

Сначала создайте новый консольный проект и добавьте ссылку на Aspose.OCR. Затем загрузите изображение, которое хотите обработать.

```csharp
using Aspose.OCR;
using System.Drawing;

class PreprocessExample
{
    static void Main()
    {
        // Load the image that needs OCR
        var imagePath = "YOUR_DIRECTORY/skewed_noisy.jpg";
        var image = new Bitmap(imagePath);
```

> **Почему это важно:** класс `Bitmap` даёт прямой доступ к пикселям, что необходимо OCR‑движку на этапе предобработки. Если путь указан неверно, вы получите `FileNotFoundException`, поэтому проверьте расположение файла.

---

## Шаг 2 – Создание экземпляра OCR‑движка

Далее создаём объект `OcrEngine`. Этот объект будет управлять всей **ocr preprocessing pipeline**.

```csharp
        // Create the OCR engine instance
        var ocrEngine = new OcrEngine();
```

> **Совет:** один и тот же `OcrEngine` можно переиспользовать для нескольких изображений; просто сбрасывайте `RecognitionOptions` каждый раз.

---

## Шаг 3 – Настройка параметров предобработки (ядро pipeline)

Здесь мы включаем две самых мощных функции: **автоматическое выравнивание изображения** и **снижение шума**. Обе они являются частью pipeline, подготавливающего картинку к точному извлечению текста.

```csharp
        // Configure recognition options with the new preprocessing pipeline
        var recognitionOptions = new RecognitionOptions
        {
            Preprocess = new PreprocessSettings
            {
                EnableSmartDeskew = true,          // Auto‑detect and correct rotation
                EnableNoiseReduction = true,      // Apply AI‑based denoising
                NoiseReductionLevel = NoiseLevel.Medium
            },
            Language = Language.English
        };
```

> **Как это работает:**  
> - `EnableSmartDeskew` анализирует углы базовых линий изображения и вращает его обратно к 0°, что критично для наклонённых сканов.  
> - `EnableNoiseReduction` запускает лёгкий AI‑фильтр, удаляющий пятна без стирания слабых символов.  
> - `NoiseReductionLevel` позволяет балансировать между скоростью и качеством; `Medium` — хороший компромисс для большинства JPG.

---

## Шаг  – Запуск OCR и получение результата

Теперь передаём изображение и параметры движку. Метод возвращает объект `OcrResult`, содержащий извлечённую строку и оценки уверенности.

```csharp
        // Perform OCR on the image using the configured options
        var ocrResult = ocrEngine.Recognize(image, recognitionOptions);
```

> **Особый случай:** если изображение полностью пустое, `ocrResult.Text` будет пустой строкой. В продакшн‑коде имеет смысл проверять `ocrResult.HasText` перед дальнейшей обработкой.

---

## Шаг 5 – Вывод распознанного текста

Наконец, выводим результат в консоль. Это демонстрирует, что мы можем **распознавать текст из файлов‑изображений** всего в несколько строк кода.

```csharp
        // Output the recognized text
        System.Console.WriteLine("=== Extracted Text ===");
        System.Console.WriteLine(ocrResult.Text);
    }
}
```

**Ожидаемый вывод (пример):**

```
=== Extracted Text ===
Invoice #12345
Date: 01/01/2024
Total: $1,250.00
Thank you for your business!
```

Еслиение было шумным или сильно повернутым, вы бы заметили искажённые символы. Благодаря **ocr preprocessing pipeline** такие проблемы значительно снижаются.

---

## Шаг 6 – Полный рабочий пример (готов к копированию)

Ниже представлен полный исходный файл, готовый к компиляции. Замените `YOUR_DIRECTORY` реальным путём к вашему JPG.

```csharp
using Aspose.OCR;
using System.Drawing;

class PreprocessExample
{
    static void Main()
    {
        // 1️⃣ Load the image that needs OCR
        var imagePath = "YOUR_DIRECTORY/skewed_noisy.jpg";
        var image = new Bitmap(imagePath);

        // 2️⃣ Create the OCR engine instance
        var ocrEngine = new OcrEngine();

        // 3️⃣ Configure the preprocessing pipeline (auto deskew + noise reduction)
        var recognitionOptions = new RecognitionOptions
        {
            Preprocess = new PreprocessSettings
            {
                EnableSmartDeskew = true,          // Auto‑detect and correct rotation
                EnableNoiseReduction = true,      // AI‑based denoising
                NoiseReductionLevel = NoiseLevel.Medium
            },
            Language = Language.English
        };

        // 4️⃣ Run OCR with the configured pipeline
        var ocrResult = ocrEngine.Recognize(image, recognitionOptions);

        // 5️⃣ Print the extracted text
        System.Console.WriteLine("=== Extracted Text ===");
        System.Console.WriteLine(ocrResult.Text);
    }
}
```

Сохраните как `Program.cs`, выполните `dotnet run` и наблюдайте, как консоль заполняется очищенным текстом.

---

## Шаг 7 – Дальнейшее развитие – настройка pipeline

**ocr preprocessing pipeline** гибок. Вот несколько распространённых вариантов, которые вы можете попробовать:

| Вариация | Когда использовать | Фрагмент кода |
||---------------------|----------------|
| **Более сильное снижение шума** (например, `NoiseLevel.High`) | Очень зернистые сканы с камер низкого разрешения | `NoiseReductionLevel = NoiseLevel.High` |
| **Отключить выравнивание** | Изображения уже идеально выровнены | `EnableSmartDeskew = false` |
| **Поддержка нескольких языков** | Документы содержат английский и испанский | `Language = Language.English | Language.Spanish` |
| **Пользовательское масштабирование DPI** | Очень маленький шрифт требует увеличения | `recognitionOptions.Dpi = 300;` |

Экспериментируя с этими настройками, вы сможете точно подогнать шаг **предобработки изображения для OCR** под особенности вашего набора данных.

---

## Заключение

Мы только что создали **ocr preprocessing pipeline** на C#, который **автоматически выравнивает изображение**, уменьшает шум и, наконец, **распознаёт текст из файлов‑изображений** таких как JPG. Настроив `PreprocessSettings` внутри `RecognitionOptions` Aspose.OCR, мы превратили нечеткую, пятнистую картинку в чистый, индексируемый текст всего несколькими строками кода.

> **Главные выводы:**  
> - Сначала всегда очищайте изображение – OCR‑движок лучше работает со straight, low‑noise входами.  
> - Pipeline полностью настраиваем; регулируйте выравнивание и подавление шума под свои задачи.  
> - Тот же подход работает с PDF, TIFF или любыми другими bitmap‑источниками, которые вы передаёте в Aspose.OCR.

Готовы к следующему шагу? Попробуйте обработать пакет файлов через pipeline или интегрировать код в веб‑API, чтобы пользователи могли загружать картинки и мгновенно получать текст. Вы также можете изучить возможности конвертации документов Aspose, чтобы превратить извлечённый текст в поисковые PDF.

Счастливого кодинга, и пусть ваши результаты OCR всегда будут точными! 🚀

---

![Диаграмма ocr preprocessing pipeline, показывающая шаги: загрузка изображения → умное выравнивание → снижение шума → OCR → вывод текста](ocr-preprocessing-pipeline.png "ocr preprocessing pipeline diagram")

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}