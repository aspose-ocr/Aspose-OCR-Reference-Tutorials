---
category: general
date: 2026-01-07
description: Как вывести список моделей в Aspose OCR AI с помощью Python — узнайте,
  как получить путь к модели, проверить установленные модели и получить список моделей
  в Python за секунды.
draft: false
keywords:
- how to list models
- get model path
- check installed models
- python get model list
- list available models
language: ru
og_description: Как вывести список моделей в Aspose OCR AI с помощью Python. Найдите
  путь к модели, проверьте установленные модели и посмотрите полный список доступных
  моделей.
og_title: Как вывести список моделей в Aspose OCR AI – руководство по Python
tags:
- Aspose OCR
- Python
- AI models
title: Как вывести список моделей в Aspose OCR AI – руководство на Python
url: /ru/python/general/how-to-list-models-in-aspose-ocr-ai-python-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# Как вывести список моделей в Aspose OCR AI – Руководство для Python

Когда‑то задавались вопросом **как вывести список моделей**, уже установленных на вашем компьютере, при работе с Aspose OCR AI? Вы не одиноки. Во многих проектах нужно проверить папку с моделями, убедиться, какие модели присутствуют, или даже отладить проблему с отсутствующей моделью — и всё это без выхода из Python REPL.

В этом руководстве мы пройдемся по полностью готовому к запуску примеру, который покажет, как **получить путь к моделям**, **проверить установленные модели**, и, наконец, **вывести список доступных моделей** всего несколькими строками кода. Никаких внешних скриптов, никаких скрытых трюков — только чистый Python и SDK Aspose OCR AI.

> **Prerequisites**  
> • Python 3.8 или новее  
> • Пакет `asposeocr` установлен (`pip install asposeocr`)  
> • Базовое знакомство с импортом модулей

Если всё готово, приступим.

---

## Как вывести список моделей с Aspose OCR AI

Первое, что нам понадобится, — класс‑помощник `AsposeAI`, который поставляется вместе с модулем `asposeocr.ai`. Этот класс предоставляет три удобных метода:

| Метод | Что возвращает | Типичный сценарий использования |
|-------|----------------|---------------------------------|
| `get_local_path()` | Абсолютный путь к папке, где Aspose хранит свои AI‑модели | Проверить, что SDK ищет модели в правильном месте |
| `list_local()` | Python `list` имён папок моделей, существующих на диске | Быстро увидеть, какие модели можно загрузить |
| `list_remote()` *(optional)* | Список моделей, доступных для скачивания из облака Aspose | Когда нужна модель, которой нет локально |

Ниже **полный скрипт**, который выводит локальную папку моделей и список установленных моделей.

```python
# ---------------------------------------------------------
# Step 1: Import the Aspose OCR AI module
# ---------------------------------------------------------
from asposeocr.ai import AsposeAI

# ---------------------------------------------------------
# Step 2: Create an instance of the AI helper
# ---------------------------------------------------------
ai = AsposeAI()

# ---------------------------------------------------------
# Step 3: Retrieve and display the local model folder
# ---------------------------------------------------------
local_folder = ai.get_local_path()
print("Local AI model folder:", local_folder)

# ---------------------------------------------------------
# Step 4: List all models that are currently installed
# ---------------------------------------------------------
installed_models = ai.list_local()
print("Available models:", installed_models)
```

### Ожидаемый вывод

При запуске скрипта на чистой установке вы обычно увидите что‑то вроде:

```
Local AI model folder: /home/user/.asposeocr/models
Available models: ['ocr-general-v1', 'ocr-handwritten-v2']
```

Если папка пуста, `list_local()` возвращает пустой список (`[]`). Это полезный сигнал о том, что сначала нужно скачать модель — о чём мы расскажем дальше.

---

## Почему важно знать путь к модели

Понимание **где** SDK хранит свои файлы (`get model path`) — это больше, чем просто любопытство:

1. **Отладка** — если модель не загружается, вы можете выполнить `ls` по пути и убедиться, что файл действительно существует.  
2. **Пользовательские модели** — некоторые команды обучают собственные OCR‑модели и кладут их в эту папку. Зная путь, вы размещаете файлы именно там, где их ждёт Aspose.  
3. **Разрешения** — в Linux папка может принадлежать другому пользователю. Выявление ошибки доступа на раннем этапе экономит часы головоломок.

> **Pro tip:** Если нужно указать SDK пользовательскую директорию, задайте переменную окружения `ASPOSE_OCR_MODEL_PATH` перед созданием `AsposeAI()`.

```bash
export ASPOSE_OCR_MODEL_PATH=/my/custom/models
python my_script.py
```

---

## Проверка установленных моделей — крайние случаи и советы

### 1. Нет установленных моделей

Если `list_local()` возвращает `[]`, у вас есть два варианта:

| Вариант | Как выполнить |
|---------|----------------|
| **Скачать модель из Aspose** | `ai.download('ocr-general-v1')` (требуется интернет) |
| **Скопировать предобученную модель** | Поместить папку модели вручную в путь, полученный через `get_local_path()` |

### 2. Несколько версий одной и той же модели

Иногда вы видите одновременно `ocr-general-v1` **и** `ocr-general-v1-beta`. SDK загружает первую найденную, но вы можете принудительно указать конкретную версию, передав точное имя папки конструктору OCR:

```python
from asposeocr.ai import AsposeOCR

ocr = AsposeOCR(model_name='ocr-general-v1-beta')
```

### 3. Повреждённые файлы модели

Частично скачанная модель может вызвать `FileNotFoundError` позже. Если подозреваете повреждение, просто удалите проблемную папку и скачайте её заново:

```bash
rm -rf /home/user/.asposeocr/models/ocr-general-v1
python -c "from asposeocr.ai import AsposeAI; AsposeAI().download('ocr-general-v1')"
```

---

## Расширение скрипта — вывод удалённых моделей (опционально)

Если хотите увидеть, какие модели доступны для скачивания, не покидая Python, добавьте ещё один вызов:

```python
remote_models = ai.list_remote()
print("Remote models you can download:", remote_models)
```

Это выведет примерно следующее:

```
Remote models you can download: ['ocr-general-v1', 'ocr-handwritten-v2', 'ocr-table-v1']
```

Затем можно выбрать любую модель и вызвать `ai.download('model-name')`, чтобы загрузить её автоматически.

---

## Полный скрипт от начала до конца

Объединив всё вместе, получаем **один исполняемый скрипт**, который:

1. Показывает локальную папку моделей.  
2. Выводит список установленных моделей.  
3. Если моделей нет, скачивает модель по умолчанию.  
4. В конце выводит обновлённый список.

```python
# ---------------------------------------------------------
# Complete script – verifies model folder, installs if empty
# ---------------------------------------------------------
from asposeocr.ai import AsposeAI

def main():
    ai = AsposeAI()

    # 1️⃣ Show where Aspose expects models
    local_path = ai.get_local_path()
    print("🔎 Local AI model folder:", local_path)

    # 2️⃣ List currently installed models
    models = ai.list_local()
    print("📦 Installed models:", models)

    # 3️⃣ If nothing is installed, grab a default model
    if not models:
        default = 'ocr-general-v1'
        print(f"⚠️ No models found – downloading '{default}'...")
        try:
            ai.download(default)
            models = ai.list_local()
            print("✅ After download, installed models:", models)
        except Exception as e:
            print("❌ Failed to download model:", e)
            return

    # 4️⃣ (Optional) Show what you could download from the cloud
    remote = ai.list_remote()
    print("🌐 Remote models available:", remote)

if __name__ == "__main__":
    main()
```

Запуск этого скрипта на чистой машине даст:

```
🔎 Local AI model folder: /home/user/.asposeocr/models
📦 Installed models: []
⚠️ No models found – downloading 'ocr-general-v1'...
✅ After download, installed models: ['ocr-general-v1']
🌐 Remote models available: ['ocr-general-v1', 'ocr-handwritten-v2', 'ocr-table-v1']
```

Теперь у вас есть **самодостаточное, пригодное для цитирования** решение, которое любой AI‑ассистент может воспроизвести дословно.

---

## Часто задаваемые вопросы (FAQ)

**Q: Работает ли это в Windows?**  
A: Абсолютно. SDK абстрагирует файловую систему, поэтому `get_local_path()` вернёт что‑то вроде `C:\Users\YourName\.asposeocr\models`. Просто убедитесь, что у Python есть права записи в эту папку.

**Q: Можно ли хранить модели на сетевом диске?**  
A: Да — задайте `ASPOSE_OCR_MODEL_PATH` на UNC‑путь (`\\server\share\models`) перед созданием экземпляра `AsposeAI`.

**Q: Что делать, если нужна модель для языка, отсутствующего в базовом наборе?**  
A: Вызовите `list_remote()`, чтобы проверить, предлагает ли Aspose язык‑специфичную модель. Если нет, вы можете обучить свою и поместить её в папку; просто передайте имя пользовательской папки конструктору OCR.

---

## Заключение

Мы рассмотрели **как вывести список моделей** в Aspose OCR AI, показали, как **получить путь к моделям**, **проверить установленные модели** и даже **скачать отсутствующую модель** — всё это с помощью чистого Python. Понимая структуру папок и методы‑помощники (`get_local_path()`, `list_local()`, `list_remote()`), вы получаете полный контроль над AI‑моделями, от которых зависит ваше приложение.

Что дальше? Попробуйте заменить модель по умолчанию на модель рукописного текста или укажите SDK на пользовательскую модель, обученную в вашей компании. В любом случае у вас теперь прочный фундамент для управления OCR‑активами в любом Python‑проекте.

Счастливого кодинга, и пусть ваш список моделей всегда будет актуален! 

---

![How to list models screenshot](https://example.com/images/how-to-list-models.png "How to list models")

*Image alt text:* **how to list models screenshot** (fulfills primary keyword requirement).

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}